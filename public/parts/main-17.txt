const renderCalendar_v131=renderCalendar;
renderCalendar=function(){const e=$('calendar');if(!e)return;const y=CAL_MONTH.getFullYear(),m=CAL_MONTH.getMonth(),days=new Date(y,m+1,0).getDate(),today=day();e.style.gridTemplateColumns=`120px repeat(${days},minmax(42px,1fr))`;let h='<div class="head">Villa</div>',ds=[];for(let i=1;i<=days;i++){const d=new Date(y,m,i),k=calKey(d);ds.push(k);h+=`<div class="head ${k===today?'today':''}">${String(i).padStart(2,'0')}</div>`}for(const v of VILLAS){h+=`<div class="head">${esc(v.name)}</div>`;for(const k of ds){const bookings=D.bookings.filter(x=>x.villa===v.code&&x.status!=='cancelled'&&k>=x.in&&k<x.out),blocks=(D.availabilityBlocks||[]).filter(x=>x.active&&String(x.villa||vcode(x.villaId))===v.code&&k>=x.start&&k<=x.end),labels=[...bookings.map(x=>`${x.guest} · ${x.code}`),...blocks.map(x=>`${blockTypeLabel(x.type)}${x.note?' · '+x.note:''}`)];h+=`<div class="${bookings.length?'booked':''} ${blocks.length?'blocked':''} ${k===today?'today':''}" title="${esc(labels.join(' | '))}">${bookings.length?bookings.map(x=>esc(x.guest).split(' ')[0]).join(', '):blocks.length?'🔒':''}</div>`}}e.innerHTML=h};
document.addEventListener('click',e=>{const btn=e.target.closest('#addBooking');if(!btn)return;const villa=$('bVilla')?.value,cin=$('bIn')?.value,cout=$('bOut')?.value,guests=+$('bGuests')?.value||1,st=$('bStatus')?.value;if(!villa||!cin||!cout||!['confirmed','staying'].includes(st))return;const p=policyFor(villa),block=blockConflict(villa,cin,cout);let message='';if(p.maxGuests&&guests>p.maxGuests)message=`${vname(villa)} tối đa ${p.maxGuests} khách`;else if(block)message=`${vname(villa)} đang bị khóa: ${blockTypeLabel(block.type)}${block.note?' · '+block.note:''}`;if(message){e.preventDefault();e.stopImmediatePropagation();toast(message,true)}},true);
render();
