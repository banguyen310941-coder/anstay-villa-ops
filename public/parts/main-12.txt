D.suppliers=D.suppliers||[];D.purchaseOrders=D.purchaseOrders||[];D.purchaseOrderItems=D.purchaseOrderItems||[];
function hkTypeLabel(t){return {pre_checkin:'Dọn trước check-in',post_checkout:'Dọn sau check-out',minibar:'Kiểm minibar',maintenance:'Bảo trì',incident:'Sự cố',pool:'Hồ bơi',landscape:'Cảnh quan'}[t]||t}
function hkStatusLabel(s){return {open:'Mở',in_progress:'Đang làm',completed:'Hoàn tất',cancelled:'Đã hủy'}[s]||s}
function hkOverdue(t){return t.status!=='completed'&&t.status!=='cancelled'&&t.due&&new Date(t.due)<new Date()}
function hkChecklistHtml(t){const a=Array.isArray(t.checklist)?t.checklist:[];return a.length?`<ul style="margin:5px 0 0;padding-left:18px">${a.map(x=>`<li>${esc(typeof x==='string'?x:(x.label||x.name||''))}</li>`).join('')}</ul>`:'<span class="muted">Chưa có checklist</span>'}
function poStatusLabel(s){return {draft:'Nháp',approved:'Đã duyệt',ordered:'Đã đặt',received:'Đã nhận',cancelled:'Hủy'}[s]||s}
function poStatusClass(s){return s==='received'?'ok':s==='approved'||s==='ordered'?'info':s==='cancelled'?'bad':'warning'}
function supplierName(id){return D.suppliers.find(x=>String(x.id)===String(id))?.name||'—'}
function poLineTotal(poId){return D.purchaseOrderItems.filter(x=>String(x.poId)===String(poId)).reduce((n,x)=>n+(+x.qty||0)*(+x.unitPrice||0),0)}
async function syncOpsProcurementRemote(){
 if(MODE!=='auth'){D.suppliers=D.suppliers||[];D.purchaseOrders=D.purchaseOrders||[];D.purchaseOrderItems=D.purchaseOrderItems||[];return}
 const [tasks,suppliers,pos,lines]=await Promise.all([safeQ('housekeeping_tasks'),safeQ('suppliers'),safeQ('purchase_orders'),safeQ('purchase_order_items')]);
 if(tasks.length||['admin','ops','housekeeping'].includes(ROLE))D.tasks=tasks.map(x=>({id:+x.id,villa:vcode(x.villa_id),bookingId:x.booking_id,type:x.task_type,employeeId:x.assigned_employee_id,owner:D.employees.find(e=>String(e.id)===String(x.assigned_employee_id))?.name||'',due:x.due_at||'',note:x.notes||'',done:x.status==='completed',status:x.status,priority:x.priority||'normal',checklist:Array.isArray(x.checklist)?x.checklist:[],startedAt:x.started_at,inspection:x.inspection_status||'pending',inspectedAt:x.inspected_at}));
 D.suppliers=suppliers.map(x=>({id:+x.id,code:x.supplier_code||'',name:x.name,phone:x.phone||'',email:x.email||'',terms:+x.payment_terms_days||0,active:x.active!==false}));
 D.purchaseOrders=pos.map(x=>({id:+x.id,code:x.po_code,supplierId:+x.supplier_id,villa:vcode(x.villa_id),orderDate:x.order_date,expectedDate:x.expected_date,due:x.payment_due_date,status:x.status,total:+x.total_amount||0,paid:+x.paid_amount||0,financeId:x.finance_transaction_id,notes:x.notes||''}));
 D.purchaseOrderItems=lines.map(x=>({id:+x.id,poId:+x.purchase_order_id,itemId:x.item_id==null?null:+x.item_id,description:x.description,qty:+x.quantity||0,unit:x.unit||'',unitPrice:+x.unit_price||0,receivedQty:+x.received_quantity||0}));
}
const loadRemote_v11=loadRemote;loadRemote=async function(){await loadRemote_v11();await syncOpsProcurementRemote();render()};

ops=function(){const today=day(),open=D.tasks.filter(t=>!['completed','cancelled'].includes(t.status)),over=open.filter(hkOverdue),dueToday=open.filter(t=>t.due&&String(t.due).slice(0,10)===today),unassigned=open.filter(t=>!t.employeeId);const empOpts=`<option value="">Chưa phân công</option>${D.employees.filter(e=>e.active!==false).map(e=>`<option value="${e.id}">${esc(e.code)} · ${esc(e.name)}</option>`).join('')}`;return `<div class="view"><div class="grid kpis"><div class="kpi"><span>Việc đang mở</span><b>${open.length}</b></div><div class="kpi"><span>Quá hạn</span><b>${over.length}</b></div><div class="kpi"><span>Đến hạn hôm nay</span><b>${dueToday.length}</b></div><div class="kpi"><span>Chưa phân công</span><b>${unassigned.length}</b></div></div><div class="grid two"><div class="card"><h3>Tạo việc vận hành</h3><div class="form"><label>Villa<select id="hkVilla">${vopts()}</select></label><label>Loại<select id="hkType"><option value="pre_checkin">Dọn trước check-in</option><option value="post_checkout">Dọn sau check-out</option><option value="minibar">Kiểm minibar</option><option value="maintenance">Bảo trì</option><option value="incident">Sự cố</option><option value="pool">Hồ bơi</option><option value="landscape">Cảnh quan</option></select></label><label>Phụ trách<select id="hkEmployee">${empOpts}</select></label><label>Ưu tiên<select id="hkPriority"><option value="normal">Bình thường</option><option value="high">Cao</option><option value="urgent">Khẩn</option></select></label><label>Hạn<input id="hkDue" type="datetime-local"></label><label class="full">Ghi chú<input id="hkNote"></label></div><button class="primary" id="hkCreate">Tạo việc</button></div><div class="card"><h3>Housekeeping tự sinh từ booking</h3><p class="muted">Booking xác nhận tạo 3 việc: dọn trước check-in, kiểm minibar và dọn sau check-out. Khi đổi ngày booking, deadline tự cập nhật; khi hủy booking, việc chưa hoàn tất được hủy theo.</p><div class="note ${over.length?'warn':''}"><b>${over.length?'Có '+over.length+' việc quá hạn':'Không có việc quá hạn'}</b><br><span class="muted">Ưu tiên xử lý việc khách sắp đến và việc chưa được phân công.</span></div></div></div><div class="card" style="margin-top:14px"><h3>Bảng điều phối housekeeping</h3><div class="scroll"><table><tr><th>Villa / Booking</th><th>Việc & checklist</th><th>Phụ trách</th><th>Hạn</th><th>TT</th><th>Kiểm tra</th><th>Thao tác</th></tr>${D.tasks.slice().sort((a,b)=>String(a.due||'9999').localeCompare(String(b.due||'9999'))).map(t=>{const overdue=hkOverdue(t),booking=D.bookings.find(b=>String(b.id)===String(t.bookingId));return `<tr><td><b>${vname(t.villa)}</b>${booking?`<br><small>${esc(booking.code)} · ${esc(booking.guest)}</small>`:''}</td><td><b>${esc(hkTypeLabel(t.type))}</b> ${t.priority==='urgent'?'<span class="tag bad">Khẩn</span>':t.priority==='high'?'<span class="tag warning">Cao</span>':''}<br>${hkChecklistHtml(t)}${t.note?`<small>${esc(t.note)}</small>`:''}</td><td><select data-hk-emp="${t.id}" style="max-width:180px">${empOpts.replace(`value="${t.employeeId}"`,`value="${t.employeeId}" selected`)}</select><br><button data-hk-assign="${t.id}">Gán</button></td><td>${t.due?new Date(t.due).toLocaleString('vi-VN'):'—'}${overdue?'<br><span class="tag bad">QUÁ HẠN</span>':''}</td><td><span class="tag ${t.status==='completed'?'ok':t.status==='in_progress'?'info':t.status==='cancelled'?'bad':'warning'}">${esc(hkStatusLabel(t.status))}</span></td><td>${t.status==='completed'?`<span class="tag ${t.inspection==='passed'?'ok':t.inspection==='rework'?'bad':'warning'}">${t.inspection==='passed'?'Đạt':t.inspection==='rework'?'Làm lại':'Chờ kiểm'}</span>`:'—'}</td><td><div class="actions">${t.status==='open'?`<button data-hk-start="${t.id}">Bắt đầu</button>`:''}${t.status==='in_progress'||t.status==='open'?`<button class="primary" data-hk-done="${t.id}">Hoàn tất</button>`:''}${t.status==='completed'?`<button data-hk-pass="${t.id}">✓ Đạt</button><button data-hk-rework="${t.id}">Làm lại</button>`:''}</div></td></tr>`}).join('')||'<tr><td colspan="7" class="empty">Chưa có việc vận hành</td></tr>'}</table></div></div></div>`}

const stock_v10=stock;stock=function(){const base=stock_v10(),activePO=D.purchaseOrders.filter(p=>p.status!=='cancelled'),draftPO=activePO.filter(p=>['draft','approved'].includes(p.status));const proc=`<div class="grid two" style="margin-top:14px"><div class="card"><h3>Nhà cung cấp</h3><div class="form"><label>Mã NCC<input id="supCode" placeholder="NCC-001"></label><label>Tên NCC<input id="supName"></label><label>Điện thoại<input id="supPhone"></label><label>Điều khoản trả (ngày)<input id="supTerms" type="number" min="0" value="0"></label></div><button class="primary" id="supCreate">Thêm nhà cung cấp</button><div class="scroll" style="margin-top:12px"><table><tr><th>Mã</th><th>NCC</th><th>SĐT</th><th>Điều khoản</th></tr>${D.suppliers.map(s=>`<tr><td>${esc(s.code||'—')}</td><td><b>${esc(s.name)}</b></td><td>${esc(s.phone||'—')}</td><td>${s.terms} ngày</td></tr>`).join('')||'<tr><td colspan="4" class="empty">Chưa có nhà cung cấp</td></tr>'}</table></div></div><div class="card"><h3>Tạo Purchase Order</h3><div class="form"><label>Mã PO<input id="poCode" placeholder="PO-${day().replaceAll('-','')}-01"></label><label>Nhà cung cấp<select id="poSupplier">${D.suppliers.map(s=>`<option value="${s.id}">${esc(s.name)}</option>`).join('')}</select></label><label>Villa<select id="poVilla">${vopts()}</select></label><label>Ngày đặt<input id="poDate" type="date" value="${day()}"></label><label>Dự kiến nhận<input id="poExpected" type="date"></label><label>Hạn thanh toán<input id="poDue" type="date"></label><label class="full">Ghi chú<input id="poNote"></label></div><button class="primary" id="poCreate" ${D.suppliers.length?'':'disabled'}>Tạo PO nháp</button></div></div><div class="card" style="margin-top:14px"><h3>Thêm dòng hàng vào PO</h3><div class="form"><label>PO<select id="polPO">${draftPO.map(p=>`<option value="${p.id}">${esc(p.code)} · ${supplierName(p.supplierId)}</option>`).join('')}</select></label><label>Vật tư<select id="polItem"><option value="">Không gắn SKU</option>${D.items.map(i=>`<option value="${i.id}">${esc(i.sku)} · ${esc(i.name)}</option>`).join('')}</select></label><label>Mô tả<input id="polDesc"></label><label>ĐVT<input id="polUnit" value="đv"></label><label>Số lượng<input id="polQty" type="number" min="0.01" step="0.01" value="1"></label><label>Đơn giá<input id="polPrice" type="number" min="0" value="0"></label></div><button class="primary" id="polAdd" ${draftPO.length?'':'disabled'}>Thêm dòng hàng</button></div><div class="card" style="margin-top:14px"><div class="row" style="justify-content:space-between"><div><h3>Purchase Order & công nợ nhà cung cấp</h3><p class="muted">PO đã duyệt có thể ghi công nợ sang Tài chính một lần, tránh cộng trùng dự chi.</p></div><span class="tag info">${activePO.length} PO</span></div><div class="scroll"><table><tr><th>PO</th><th>NCC</th><th>Villa</th><th>Ngày / Hạn trả</th><th>Giá trị</th><th>TT</th><th>Công nợ</th><th>Thao tác</th></tr>${D.purchaseOrders.slice().reverse().map(p=>{const total=p.total||poLineTotal(p.id);return `<tr><td><b>${esc(p.code)}</b><br><small>${D.purchaseOrderItems.filter(x=>x.poId===p.id).length} dòng hàng</small></td><td>${esc(supplierName(p.supplierId))}</td><td>${vname(p.villa)}</td><td>${p.orderDate||'—'}<br><small>Hạn: ${p.due||'—'}</small></td><td><b>${fmt(total)}</b></td><td><span class="tag ${poStatusClass(p.status)}">${esc(poStatusLabel(p.status))}</span></td><td>${p.financeId?'<span class="tag ok">Đã ghi AP</span>':'<span class="tag warning">Chưa ghi AP</span>'}</td><td><div class="actions">${p.status==='draft'&&['admin','ops'].includes(ROLE)||MODE==='demo'?`<button data-po-approve="${p.id}">Duyệt</button>`:''}${p.status==='approved'?`<button data-po-order="${p.id}">Đã đặt</button>`:''}${['approved','ordered'].includes(p.status)?`<button data-po-receive="${p.id}">Nhận hàng</button>`:''}${['approved','ordered','received'].includes(p.status)&&!p.financeId?`<button data-po-ap="${p.id}">Ghi công nợ</button>`:''}</div></td></tr>`}).join('')||'<tr><td colspan="8" class="empty">Chưa có Purchase Order</td></tr>'}</table></div></div>`;return base.replace(/<\/div>$/,'')+proc+'</div>'}

const managementForecast_v10=managementForecast;managementForecast=function(){const f=managementForecast_v10(),b=reportBounds(f.month),po=D.purchaseOrders.filter(p=>['approved','ordered','received'].includes(p.status)&&!p.financeId&&p.due&&p.due>=b.s&&p.due<=b.e).reduce((n,p)=>n+(+p.total||poLineTotal(p.id)),0);return {...f,po,total:f.total+po}}
const forecast_v10=forecast;forecast=function(){const f=forecast_v10(),n=new Date(),s=new Date(n.getFullYear(),n.getMonth()+1,1).toLocaleDateString('en-CA'),e=new Date(n.getFullYear(),n.getMonth()+2,0).toLocaleDateString('en-CA'),po=D.purchaseOrders.filter(p=>['approved','ordered','received'].includes(p.status)&&!p.financeId&&p.due&&p.due>=s&&p.due<=e).reduce((x,p)=>x+(+p.total||poLineTotal(p.id)),0);return {...f,po,total:f.total+po}}
const reports_v11=reports;reports=function(){let h=reports_v11(),f=managementForecast();if(f.po)h=h.replace('<tr><td>Nghĩa vụ đối soát đã khóa nhưng chưa trả</td>',`<tr><td>PO đã duyệt chưa ghi công nợ</td><td>${fmt(f.po)}</td></tr><tr><td>Nghĩa vụ đối soát đã khóa nhưng chưa trả</td>`);return h}

async function v11Action(fn,msg){try{await fn();toast(msg);if(MODE==='auth')await syncOpsProcurementRemote();if(MODE==='demo')localStorage.setItem(STORE,JSON.stringify(D));render()}catch(e){console.error(e);toast(e?.message||String(e),true)}}
document.addEventListener('click',e=>{
 const create=e.target.closest('#hkCreate');if(create){const due=$('hkDue')?.value;if(!due)return toast('Nhập hạn hoàn thành',true);return v11Action(async()=>{const row={villa:$('hkVilla').value,type:$('hkType').value,employeeId:+$('hkEmployee').value||null,due:new Date(due).toISOString(),priority:$('hkPriority').value,note:$('hkNote').value.trim(),status:'open',checklist:[]};if(MODE==='auth')await insert('housekeeping_tasks',{villa_id:vid(row.villa),task_type:row.type,assigned_employee_id:row.employeeId,due_at:row.due,priority:row.priority,notes:row.note||null,status:'open'});else D.tasks.push({id:Date.now(),...row,owner:D.employees.find(x=>x.id===row.employeeId)?.name||'',done:false,inspection:'pending'})},'Đã tạo việc')}
 const assign=e.target.closest('[data-hk-assign]');if(assign){const id=+assign.dataset.hkAssign,sel=document.querySelector(`[data-hk-emp="${id}"]`),emp=+sel?.value||null,t=D.tasks.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('housekeeping_tasks',id,{assigned_employee_id:emp,updated_at:new Date().toISOString()});else{t.employeeId=emp;t.owner=D.employees.find(x=>x.id===emp)?.name||''}},'Đã phân công')}
 const start=e.target.closest('[data-hk-start]');if(start){const id=+start.dataset.hkStart,t=D.tasks.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('housekeeping_tasks',id,{status:'in_progress',started_at:new Date().toISOString(),updated_at:new Date().toISOString()});else{t.status='in_progress';t.startedAt=new Date().toISOString()}},'Đã bắt đầu việc')}
 const done=e.target.closest('[data-hk-done]');if(done){const id=+done.dataset.hkDone,t=D.tasks.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('housekeeping_tasks',id,{status:'completed',completed_at:new Date().toISOString(),inspection_status:'pending',updated_at:new Date().toISOString()});else{t.status='completed';t.done=true;t.inspection='pending'}},'Đã hoàn tất việc')}
 const pass=e.target.closest('[data-hk-pass]');if(pass){const id=+pass.dataset.hkPass,t=D.tasks.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('housekeeping_tasks',id,{inspection_status:'passed',inspected_at:new Date().toISOString(),updated_at:new Date().toISOString()});else t.inspection='passed'},'Đã xác nhận đạt')}
 const rework=e.target.closest('[data-hk-rework]');if(rework){const id=+rework.dataset.hkRework,t=D.tasks.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('housekeeping_tasks',id,{status:'open',inspection_status:'rework',completed_at:null,updated_at:new Date().toISOString()});else{t.status='open';t.done=false;t.inspection='rework'}},'Đã trả lại để làm lại')}
 const sup=e.target.closest('#supCreate');if(sup){const name=$('supName')?.value.trim();if(!name)return toast('Nhập tên nhà cung cấp',true);return v11Action(async()=>{const row={code:$('supCode').value.trim(),name,phone:$('supPhone').value.trim(),terms:+$('supTerms').value||0};if(MODE==='auth')await insert('suppliers',{supplier_code:row.code||null,name:row.name,phone:row.phone||null,payment_terms_days:row.terms});else D.suppliers.push({id:Date.now(),...row,active:true})},'Đã thêm nhà cung cấp')}
 const poc=e.target.closest('#poCreate');if(poc){const code=$('poCode')?.value.trim(),sid=+$('poSupplier')?.value;if(!code||!sid)return toast('Nhập mã PO và chọn nhà cung cấp',true);return v11Action(async()=>{const row={code,supplierId:sid,villa:$('poVilla').value,orderDate:$('poDate').value||day(),expectedDate:$('poExpected').value||null,due:$('poDue').value||null,status:'draft',total:0,financeId:null,notes:$('poNote').value.trim()};if(MODE==='auth')await insert('purchase_orders',{po_code:row.code,supplier_id:row.supplierId,villa_id:vid(row.villa),order_date:row.orderDate,expected_date:row.expectedDate,payment_due_date:row.due,notes:row.notes||null,status:'draft'});else D.purchaseOrders.push({id:Date.now(),...row})},'Đã tạo PO nháp')}
 const line=e.target.closest('#polAdd');if(line){const poId=+$('polPO')?.value,qty=+$('polQty')?.value,price=+$('polPrice')?.value,itemId=+$('polItem')?.value||null,item=D.items.find(x=>x.id===itemId),desc=$('polDesc')?.value.trim()||item?.name||'';if(!poId||!desc||qty<=0||price<0)return toast('Kiểm tra PO, mô tả, số lượng và đơn giá',true);return v11Action(async()=>{const row={poId,itemId,description:desc,qty,unit:$('polUnit').value.trim()||item?.unit||'đv',unitPrice:price,receivedQty:0};if(MODE==='auth')await insert('purchase_order_items',{purchase_order_id:poId,item_id:itemId,description:row.description,quantity:row.qty,unit:row.unit,unit_price:row.unitPrice});else{D.purchaseOrderItems.push({id:Date.now(),...row});const p=D.purchaseOrders.find(x=>x.id===poId);if(p)p.total=poLineTotal(poId)}} ,'Đã thêm dòng hàng')}
 const approve=e.target.closest('[data-po-approve]');if(approve){const id=+approve.dataset.poApprove,p=D.purchaseOrders.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('purchase_orders',id,{status:'approved',updated_at:new Date().toISOString()});else p.status='approved'},'Đã duyệt PO')}
 const order=e.target.closest('[data-po-order]');if(order){const id=+order.dataset.poOrder,p=D.purchaseOrders.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth')await update('purchase_orders',id,{status:'ordered',updated_at:new Date().toISOString()});else p.status='ordered'},'Đã chuyển PO sang đã đặt')}
 const receive=e.target.closest('[data-po-receive]');if(receive){const id=+receive.dataset.poReceive,p=D.purchaseOrders.find(x=>x.id===id);if(!confirm('Xác nhận đã nhận đủ hàng? Các dòng có SKU sẽ tự nhập kho.'))return;return v11Action(async()=>{if(MODE==='auth'){const {error}=await neon.rpc('receive_purchase_order',{p_po_id:id});if(error)throw new Error(dbError(error))}else{p.status='received';for(const l of D.purchaseOrderItems.filter(x=>x.poId===id)){l.receivedQty=l.qty;if(l.itemId)D.moves.push({id:Date.now()+Math.random(),item:l.itemId,villa:p.villa,type:'in',qty:l.qty,cost:l.unitPrice,at:new Date().toISOString(),note:'Nhập từ PO '+p.code})}}},'Đã nhận hàng và cập nhật kho')}
 const ap=e.target.closest('[data-po-ap]');if(ap){const id=+ap.dataset.poAp,p=D.purchaseOrders.find(x=>x.id===id);return v11Action(async()=>{if(MODE==='auth'){const {error}=await neon.rpc('post_purchase_order_payable',{p_po_id:id});if(error)throw new Error(dbError(error))}else{p.financeId='demo-'+Date.now();D.finance.push({id:Date.now(),date:p.orderDate,villa:p.villa,dir:'out',cat:'supplier',amount:p.total||poLineTotal(p.id),source:'PO '+p.code,paid:false,due:p.due,invoice:'undetermined'})}},'Đã ghi công nợ nhà cung cấp')}
});
render();
