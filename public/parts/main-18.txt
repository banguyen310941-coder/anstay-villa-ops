D.websiteRequests=D.websiteRequests||[];D.websitePaymentIntents=D.websitePaymentIntents||[];D.channelOutbox=D.channelOutbox||[];
if(!NAV.some(x=>x[0]==='websales'))NAV.splice(Math.max(0,NAV.findIndex(x=>x[0]==='reports')),0,['websales','↗','Website & OTA','Yêu cầu đặt phòng, tiền cọc và hàng đợi đồng bộ kênh']);
for(const r of ['admin','sales','ops','accounting'])if(ACCESS[r]&&!ACCESS[r].includes('websales'))ACCESS[r].push('websales');
function webReqStatus(s){return {needs_manual_quote:'Chờ chốt phụ thu',pending_review:'Chờ duyệt',confirmed:'Đã xác nhận',rejected:'Từ chối',cancelled:'Đã hủy'}[s]||s}
function webReqClass(s){return s==='confirmed'?'ok':s==='rejected'||s==='cancelled'?'bad':s==='needs_manual_quote'?'warning':'info'}
function outboxStatus(s){return {pending:'Chờ gửi',sent:'Đã gửi',failed:'Lỗi',waiting_mapping:'Chờ mapping'}[s]||s}
function outboxClass(s){return s==='sent'?'ok':s==='failed'?'bad':s==='waiting_mapping'?'warning':'info'}
async function syncWebSalesRemote(){if(MODE!=='auth'){D.websiteRequests=D.websiteRequests||[];D.websitePaymentIntents=D.websitePaymentIntents||[];D.channelOutbox=D.channelOutbox||[];return}const [requests,payments,outbox]=await Promise.all([safeQ('website_booking_requests'),safeQ('website_payment_intents'),safeQ('channel_sync_outbox')]);D.websiteRequests=requests.map(x=>({id:+x.id,code:x.request_code,hold:x.hold_token,villa:vcode(x.villa_id),channel:x.channel_code,plan:x.rate_plan_code,in:x.check_in_date,out:x.check_out_date,guests:+x.guests||1,extraAdults:+x.extra_adults||0,children612:+x.children_6_12||0,childrenUnder6:+x.children_under_6||0,name:x.guest_name,email:x.guest_email,phone:x.guest_phone||'',note:x.guest_note||'',room:x.room_amount==null?null:+x.room_amount,surcharge:x.surcharge_amount==null?null:+x.surcharge_amount,total:x.total_amount==null?null:+x.total_amount,manual:!!x.requires_manual_quote,depositRequired:x.deposit_required==null?null:+x.deposit_required,depositPaid:+x.deposit_paid||0,paymentStatus:x.payment_status,status:x.status,bookingId:x.confirmed_booking_id,expires:x.expires_at,created:x.created_at}));D.websitePaymentIntents=payments.map(x=>({id:+x.id,requestId:+x.request_id,provider:x.provider,method:x.method,amount:+x.amount||0,status:x.status,reference:x.external_reference||'',paidAt:x.paid_at,created:x.created_at}));D.channelOutbox=outbox.map(x=>({id:+x.id,bookingId:x.booking_id==null?null:+x.booking_id,villa:vcode(x.villa_id),channel:x.channel_code,event:x.event_type,status:x.status,attempts:+x.attempts||0,next:x.next_attempt_at,error:x.last_error||'',created:x.created_at,sent:x.sent_at}))}
const loadRemote_v14=loadRemote;loadRemote=async function(){await loadRemote_v14();await syncWebSalesRemote();if(current==='websales')render()};
function websales(){const req=D.websiteRequests.slice().sort((a,b)=>String(b.created).localeCompare(String(a.created))),pending=req.filter(x=>!['confirmed','rejected','cancelled'].includes(x.status)),manual=req.filter(x=>x.status==='needs_manual_quote'),paid=req.filter(x=>x.depositPaid>0),out=D.channelOutbox.slice().sort((a,b)=>String(b.created).localeCompare(String(a.created))),queue=out.filter(x=>['pending','failed','waiting_mapping'].includes(x.status));return `<div class="view"><div class="note"><b>Website Booking + OTA Outbox:</b> khách chỉ gửi yêu cầu sau khi hệ thống giữ phòng tạm thời. Booking thật chỉ được tạo khi nhân sự ANSTAY xác nhận. OTA outbound dùng hàng đợi riêng và chưa gửi nếu thiếu mapping/credential.</div><div class="grid kpis"><div class="kpi"><span>Yêu cầu chờ xử lý</span><b>${pending.length}</b></div><div class="kpi"><span>Chờ chốt phụ thu</span><b>${manual.length}</b></div><div class="kpi"><span>Yêu cầu đã nhận cọc</span><b>${paid.length}</b></div><div class="kpi"><span>OTA outbox chưa xong</span><b>${queue.length}</b></div></div><div class="card"><div class="row" style="justify-content:space-between;align-items:center"><div><h3>Yêu cầu đặt villa từ website</h3><p class="muted">Trang khách: <a href="/booking.html" target="_blank" rel="noopener">/booking.html ↗</a></p></div><span class="tag info">${req.length} yêu cầu</span></div><div class="scroll"><table><tr><th>Mã / khách</th><th>Villa / ngày</th><th>Giá</th><th>Cọc</th><th>TT</th><th>Thao tác</th></tr>${req.map(r=>{const due=r.depositRequired,paid=r.depositPaid,depositOk=due==null||due<=0||paid>=due;return `<tr><td><b>${esc(r.code)}</b><br>${esc(r.name)}<br><small>${esc(r.phone)} · ${esc(r.email)}</small></td><td><b>${vname(r.villa)}</b><br>${r.in} → ${r.out}<br><small>${r.guests} khách${r.extraAdults+r.children612+r.childrenUnder6?` · phụ thu khai báo ${r.extraAdults}/${r.children612}/${r.childrenUnder6}`:''}</small></td><td>Phòng: ${r.room==null?'—':fmt(r.room)}<br>Phụ thu: ${r.surcharge==null?'Chờ xác nhận':fmt(r.surcharge)}<br><b>Tổng: ${r.total==null?'—':fmt(r.total)}</b></td><td>${due==null?'Chưa đặt mức cọc':`Yêu cầu ${fmt(due)}`}<br>Đã ghi ${fmt(paid)}<br>${due!=null?`<span class="tag ${depositOk?'ok':'warning'}">${depositOk?'Đủ cọc':'Chưa đủ cọc'}</span>`:''}</td><td><span class="tag ${webReqClass(r.status)}">${esc(webReqStatus(r.status))}</span><br><small>${esc(r.paymentStatus||'')}</small></td><td><div class="actions">${!['confirmed','rejected','cancelled'].includes(r.status)?`<button data-web-quote="${r.id}">Giá / cọc</button>${['admin','ops','accounting'].includes(ROLE)?`<button data-web-pay="${r.id}">Ghi tiền cọc</button>`:''}${!r.manual&&depositOk&&['admin','sales','ops'].includes(ROLE)?`<button class="primary" data-web-confirm="${r.id}">Xác nhận booking</button>`:''}<button class="danger-btn" data-web-reject="${r.id}">Từ chối</button>`:'—'}</div></td></tr>`}).join('')||'<tr><td colspan="6" class="empty">Chưa có yêu cầu website</td></tr>'}</table></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Nhật ký tiền cọc trước xác nhận</h3><div class="scroll"><table><tr><th>Yêu cầu</th><th>Số tiền</th><th>Phương thức</th><th>Tham chiếu</th><th>TT</th></tr>${D.websitePaymentIntents.slice().sort((a,b)=>String(b.created).localeCompare(String(a.created))).slice(0,50).map(p=>{const r=req.find(x=>x.id===p.requestId);return `<tr><td>${esc(r?.code||p.requestId)}</td><td><b>${fmt(p.amount)}</b></td><td>${esc(p.method)}</td><td>${esc(p.reference||'—')}</td><td><span class="tag ${p.status==='paid'?'ok':'warning'}">${esc(p.status)}</span></td></tr>`}).join('')||'<tr><td colspan="5" class="empty">Chưa có giao dịch cọc website</td></tr>'}</table></div></div><div class="card"><h3>OTA Channel Sync Outbox</h3><p class="muted">Booking xác nhận/thay đổi/hủy sẽ sinh sự kiện inventory cho các OTA. Dòng “Chờ mapping” chưa được gửi vì còn thiếu Property/Room ID của kênh.</p><div class="scroll"><table><tr><th>Kênh</th><th>Villa</th><th>Sự kiện</th><th>TT</th><th>Lần thử</th><th></th></tr>${out.slice(0,80).map(x=>`<tr><td><b>${esc(x.channel)}</b></td><td>${vname(x.villa)}</td><td>${esc(x.event)}</td><td><span class="tag ${outboxClass(x.status)}">${esc(outboxStatus(x.status))}</span>${x.error?`<br><small>${esc(x.error)}</small>`:''}</td><td>${x.attempts}</td><td>${x.status==='failed'&&['admin','ops'].includes(ROLE)?`<button data-outbox-retry="${x.id}">Gửi lại</button>`:x.status==='waiting_mapping'?'<small>Hoàn tất Channel Mapping trước</small>':'—'}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">Chưa có sự kiện outbound</td></tr>'}</table></div></div></div></div>`}
async function webSalesAction(fn,msg){try{await fn();toast(msg);if(MODE==='auth')await syncWebSalesRemote();await refresh()}catch(e){console.error(e);toast(e?.message||String(e),true)}}
function bindWebSales(){if(current!=='websales')return;document.querySelectorAll('[data-web-quote]').forEach(b=>b.onclick=()=>{const r=D.websiteRequests.find(x=>x.id===+b.dataset.webQuote);if(!r)return;const s=prompt('Nhập tổng phụ thu đã xác nhận (VND):',String(r.surcharge??0));if(s===null)return;const surcharge=Number(s);if(!Number.isFinite(surcharge)||surcharge<0)return toast('Phụ thu không hợp lệ',true);const d=prompt('Nhập mức cọc yêu cầu (VND). Để trống nếu chưa chốt:',r.depositRequired==null?'':String(r.depositRequired));if(d===null)return;const deposit=d.trim()===''?null:Number(d);if(deposit!=null&&(!Number.isFinite(deposit)||deposit<0))return toast('Mức cọc không hợp lệ',true);webSalesAction(async()=>{const {error}=await neon.rpc('admin_update_website_request_quote',{p_request_id:r.id,p_surcharge_amount:surcharge,p_deposit_required:deposit});if(error)throw new Error(dbError(error))},'Đã cập nhật giá phụ thu / mức cọc')});document.querySelectorAll('[data-web-pay]').forEach(b=>b.onclick=()=>{const r=D.websiteRequests.find(x=>x.id===+b.dataset.webPay);if(!r)return;const a=prompt('Nhập số tiền cọc đã kiểm tra thực nhận (VND):','');if(a===null)return;const amount=Number(a);if(!Number.isFinite(amount)||amount<=0)return toast('Số tiền không hợp lệ',true);const ref=prompt('Mã giao dịch / tham chiếu ngân hàng:','')||'';webSalesAction(async()=>{const {error}=await neon.rpc('admin_record_website_request_payment',{p_request_id:r.id,p_amount:amount,p_reference:ref||null,p_method:'transfer'});if(error)throw new Error(dbError(error))},'Đã ghi nhận tiền cọc')});document.querySelectorAll('[data-web-confirm]').forEach(b=>b.onclick=()=>{const r=D.websiteRequests.find(x=>x.id===+b.dataset.webConfirm);if(!r||!confirm(`Xác nhận ${r.code} thành booking thật?`))return;webSalesAction(async()=>{const {error}=await neon.rpc('admin_confirm_website_request',{p_request_id:r.id});if(error)throw new Error(dbError(error));await loadRemote()},'Đã tạo booking và khóa lịch')});document.querySelectorAll('[data-web-reject]').forEach(b=>b.onclick=()=>{const r=D.websiteRequests.find(x=>x.id===+b.dataset.webReject);if(!r)return;const why=prompt('Lý do từ chối / hủy yêu cầu:','')||'';if(!confirm(`Từ chối ${r.code}?`))return;webSalesAction(async()=>{const {error}=await neon.rpc('admin_reject_website_request',{p_request_id:r.id,p_reason:why||null});if(error)throw new Error(dbError(error))},'Đã từ chối yêu cầu')});document.querySelectorAll('[data-outbox-retry]').forEach(b=>b.onclick=()=>webSalesAction(async()=>{const {error}=await neon.rpc('admin_retry_channel_outbox',{p_outbox_id:+b.dataset.outboxRetry});if(error)throw new Error(dbError(error))},'Đã đưa sự kiện về hàng chờ gửi'))}
const render_v14=render;render=function(){if(current==='websales'){$('content').innerHTML=websales();bindWebSales();return}render_v14()};
renderNav();render();