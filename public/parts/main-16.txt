D.availabilityBlocks=D.availabilityBlocks||[];D.bookingPolicies=D.bookingPolicies||[];D.rateSeasons=D.rateSeasons||[];
if(!NAV.some(x=>x[0]==='inventoryControl'))NAV.splice(Math.max(0,NAV.findIndex(x=>x[0]==='rates')),0,['inventoryControl','◫','Phòng & Website','Block phòng, sức chứa, mùa giá và mô phỏng booking web']);
for(const r of ['admin','sales','ops'])if(ACCESS[r]&&!ACCESS[r].includes('inventoryControl'))ACCESS[r].push('inventoryControl');
function inventoryCanWrite(){return MODE==='demo'||ROLE==='admin'||ROLE==='ops'}
function blockTypeLabel(v){return {owner:'Chủ nhà giữ',maintenance:'Bảo trì',manual:'Khóa thủ công',internal:'Nội bộ',channel:'Kênh bán'}[v]||v}
function demoInventorySeed(){if(MODE!=='demo')return;if(!D.bookingPolicies.length)D.bookingPolicies=[['NHAN',8],['SOL',8],['SAM',8],['GIO',8],['NANG',6],['TIM',14]].map((x,i)=>({villaId:VILLAS.find(v=>v.code===x[0])?.id||x[0],maxGuests:x[1],standardGuests:null,checkIn:'14:00:00',checkOut:'12:00:00',sameDay:true}));if(!D.availabilityBlocks.length){const sol=VILLAS.find(v=>v.code==='SOL'),sam=VILLAS.find(v=>v.code==='SAM');D.availabilityBlocks=[{id:91001,villaId:sol?.id||'SOL',villa:'SOL',start:'2026-08-22',end:'2027-08-22',type:'manual',source:'villa_workbook_2026',note:'SOL: kín lịch theo dữ liệu villa đã cung cấp',active:true},{id:91002,villaId:sam?.id||'SAM',villa:'SAM',start:'2026-12-15',end:'2028-12-15',type:'manual',source:'villa_workbook_2026',note:'SAM: kín lịch theo dữ liệu villa đã cung cấp',active:true}]}}
async function syncInventoryRemote(){if(MODE!=='auth'){demoInventorySeed();return}const [blocks,policies,seasons]=await Promise.all([safeQ('availability_blocks'),safeQ('villa_booking_policies'),safeQ('rate_seasons')]);D.availabilityBlocks=blocks.map(x=>({id:+x.id,villaId:+x.villa_id,villa:vcode(x.villa_id),start:x.start_date,end:x.end_date,type:x.block_type,source:x.source||'',note:x.note||'',active:x.active!==false}));D.bookingPolicies=policies.map(x=>({villaId:+x.villa_id,maxGuests:x.max_guests==null?null:+x.max_guests,standardGuests:x.standard_guests==null?null:+x.standard_guests,checkIn:x.check_in_time||'14:00',checkOut:x.check_out_time||'12:00',sameDay:x.same_day_booking!==false}));D.rateSeasons=seasons.map(x=>({id:+x.id,name:x.name,villaId:+x.villa_id,villa:vcode(x.villa_id),planId:+x.rate_plan_id,start:x.start_date,end:x.end_date,weekday:+x.weekday_rate,saturday:+x.saturday_rate,sunday:+x.sunday_rate,minStay:+x.min_stay||1,published:!!x.published,stopSell:!!x.stop_sell,active:x.active!==false,note:x.note||'',appliedAt:x.applied_at||null}))}
const loadRemote_v13=loadRemote;loadRemote=async function(){await loadRemote_v13();await syncInventoryRemote();if(current==='inventoryControl')render()};
function policyFor(code){const v=VILLAS.find(x=>x.code===code),key=v?.id??v?.code;return D.bookingPolicies.find(x=>String(x.villaId)===String(key))||{villaId:key,maxGuests:null,standardGuests:null,checkIn:'14:00',checkOut:'12:00',sameDay:true}}
function activeBlocks(code){const v=VILLAS.find(x=>x.code===code),key=v?.id??v?.code;return D.availabilityBlocks.filter(x=>String(x.villaId)===String(key)&&x.active)}
function blockConflict(code,cin,cout){return activeBlocks(code).find(x=>x.start<cout&&x.end>=cin)}
function stayDatesExclusive(cin,cout){const out=[],d=new Date(cin+'T00:00:00');while(day(d)<cout&&out.length<62){out.push(day(d));d.setDate(d.getDate()+1)}return out}
function simulatorResults(cin,cout,guests,channel){const pid=D.ratePlans.find(x=>x.code==='BAR')?.id||D.ratePlans[0]?.id,dates=stayDatesExclusive(cin,cout);return VILLAS.map(v=>{const p=policyFor(v.code),block=blockConflict(v.code,cin,cout),booking=overlaps(v.code,cin,cout);if(p.maxGuests&&guests>p.maxGuests)return {villa:v,ok:false,reason:`Vượt sức chứa ${p.maxGuests} khách`};if(block)return {villa:v,ok:false,reason:blockTypeLabel(block.type),detail:block.note};if(booking)return {villa:v,ok:false,reason:'Đã có booking'};const map=mappingFor(channel,v.code,pid);if(!map)return {villa:v,ok:false,reason:'Chưa mapping kênh'};const rows=dates.map(date=>rateRow(v.code,pid,date));if(rows.some(x=>!x))return {villa:v,ok:false,reason:'Thiếu giá theo ngày'};if(rows.some(x=>!x.published))return {villa:v,ok:false,reason:'Giá chưa phát hành'};if(rows.some(x=>x.stopSell))return {villa:v,ok:false,reason:'Stop sell'};const arrival=rows[0],nights=dates.length;if(nights<(arrival?.minStay||1))return {villa:v,ok:false,reason:`Min stay ${arrival.minStay} đêm`};const nightly=rows.map(x=>adjustedRate(x,map));if(nightly.some(x=>!x||x<=0))return {villa:v,ok:false,reason:'Giá không hợp lệ'};return {villa:v,ok:true,total:nightly.reduce((a,b)=>a+b,0),nights,maxGuests:p.maxGuests,nightly}})}
function inventoryControl(){demoInventorySeed();const can=inventoryCanWrite(),now=day(),upcoming=D.availabilityBlocks.filter(x=>x.active&&x.end>=now).sort((a,b)=>String(a.start).localeCompare(String(b.start))),seasons=D.rateSeasons.slice().sort((a,b)=>String(b.start).localeCompare(String(a.start))),tomorrow=(()=>{const d=new Date();d.setDate(d.getDate()+1);return day(d)})(),after=(()=>{const d=new Date();d.setDate(d.getDate()+3);return day(d)})();return `<div class="view"><div class="note"><b>Availability Control:</b> Booking, chủ nhà giữ phòng, bảo trì, stop-sell và hold website đều phải đi qua cùng một lớp kiểm soát trước khi bán ra ngoài.</div><div class="grid kpis"><div class="kpi"><span>Villa đang hoạt động</span><b>${VILLAS.length}</b></div><div class="kpi"><span>Block đang hiệu lực / sắp tới</span><b>${upcoming.length}</b></div><div class="kpi"><span>Mùa giá đã lưu</span><b>${seasons.length}</b></div><div class="kpi"><span>Villa có giới hạn sức chứa</span><b>${VILLAS.filter(v=>policyFor(v.code).maxGuests).length}/${VILLAS.length}</b></div></div><div class="grid two"><div class="card"><h3>Khóa phòng / Owner Block</h3><div class="form"><label>Villa<select id="blockVilla">${vopts()}</select></label><label>Loại<select id="blockType"><option value="owner">Chủ nhà giữ</option><option value="maintenance">Bảo trì</option><option value="manual">Khóa thủ công</option><option value="internal">Nội bộ</option></select></label><label>Từ ngày<input id="blockFrom" type="date" value="${tomorrow}"></label><label>Đến ngày (có tính ngày này)<input id="blockTo" type="date" value="${after}"></label><label class="full">Ghi chú<input id="blockNote" placeholder="Lý do khóa phòng"></label></div><button class="primary" id="createBlock" ${can?'':'disabled'}>Khóa phòng</button><p class="muted">Block không xóa lịch sử. Khi mở lại, hệ thống chuyển block sang inactive.</p></div><div class="card"><h3>Sức chứa & giờ vận hành</h3><div class="scroll"><table><tr><th>Villa</th><th>Tối đa</th><th>Check-in</th><th>Check-out</th><th></th></tr>${VILLAS.map(v=>{const p=policyFor(v.code);return `<tr><td><b>${esc(v.name)}</b></td><td><input data-cap="${v.code}" type="number" min="1" value="${p.maxGuests||''}" style="width:75px"></td><td>${String(p.checkIn||'14:00').slice(0,5)}</td><td>${String(p.checkOut||'12:00').slice(0,5)}</td><td><button data-save-cap="${v.code}" ${can?'':'disabled'}>Lưu</button></td></tr>`}).join('')}</table></div><p class="muted">Giới hạn hiện tại lấy từ dữ liệu villa: Nhàn/SOL/SAM/Gió 8, Nắng 6, Tim 14 khách.</p></div></div><div class="card" style="margin-top:14px"><h3>Block đang hiệu lực & sắp tới</h3><div class="scroll"><table><tr><th>Villa</th><th>Khoảng ngày</th><th>Loại</th><th>Nguồn</th><th>Ghi chú</th><th></th></tr>${upcoming.map(b=>`<tr><td><b>${vname(b.villa||vcode(b.villaId))}</b></td><td>${b.start} → ${b.end}</td><td><span class="tag warning">${esc(blockTypeLabel(b.type))}</span></td><td>${esc(b.source||'—')}</td><td>${esc(b.note||'—')}</td><td><button data-open-block="${b.id}" ${can?'':'disabled'}>Mở lại</button></td></tr>`).join('')||'<tr><td colspan="6" class="empty">Không có block</td></tr>'}</table></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Mùa giá / dịp lễ</h3><div class="form"><label>Tên mùa<input id="seasonName" placeholder="Ví dụ: Tết 2027"></label><label>Villa<select id="seasonVilla">${vopts()}</select></label><label>Rate plan<select id="seasonPlan">${D.ratePlans.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.code)} · ${esc(x.name)}</option>`).join('')}</select></label><label>Min stay<input id="seasonMin" type="number" min="1" value="1"></label><label>Từ ngày<input id="seasonFrom" type="date" value="${tomorrow}"></label><label>Đến ngày<input id="seasonTo" type="date" value="${after}"></label><label>Giá T2–T6<input id="seasonWeekday" type="number" min="1" step="10000"></label><label>Giá Thứ 7<input id="seasonSaturday" type="number" min="1" step="10000"></label><label>Giá Chủ nhật<input id="seasonSunday" type="number" min="1" step="10000"></label><label><span><input id="seasonPublish" type="checkbox" style="width:auto;display:inline;margin-right:6px">Phát hành ngay</span></label><label><span><input id="seasonStop" type="checkbox" style="width:auto;display:inline;margin-right:6px">Stop sell</span></label><label class="full">Ghi chú<input id="seasonNote"></label></div><button class="primary" id="applySeason" ${can?'':'disabled'}>Lưu mùa & áp dụng vào lịch giá</button><p class="muted">Mùa giá được lưu riêng để audit, đồng thời materialize thành giá từng ngày trong Rate Calendar.</p></div><div class="card"><h3>Danh sách mùa giá</h3><div class="scroll"><table><tr><th>Mùa</th><th>Villa</th><th>Khoảng</th><th>Giá</th><th>TT</th></tr>${seasons.slice(0,20).map(s=>`<tr><td><b>${esc(s.name)}</b></td><td>${vname(s.villa||vcode(s.villaId))}</td><td>${s.start} → ${s.end}<br><small>Min ${s.minStay} đêm</small></td><td>${fmt(s.weekday)} / T7 ${fmt(s.saturday)} / CN ${fmt(s.sunday)}</td><td>${s.published?'<span class="tag ok">Published</span>':'<span class="tag warning">Draft</span>'}${s.stopSell?' <span class="tag bad">Stop sell</span>':''}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">Chưa có mùa giá</td></tr>'}</table></div></div></div><div class="card" style="margin-top:14px"><h3>Mô phỏng Website Booking Engine</h3><div class="form"><label>Check-in<input id="webCin" type="date" value="${tomorrow}"></label><label>Check-out<input id="webCout" type="date" value="${after}"></label><label>Số khách<input id="webGuests" type="number" min="1" value="2"></label><label>Kênh<select id="webChannel">${D.channels.filter(x=>x.active).map(x=>`<option value="${x.code}" ${x.code==='WEBSITE'?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label></div><button class="primary" id="runWebSearch">Tìm villa có thể bán</button><div id="webResults" style="margin-top:12px"><p class="muted">Mô phỏng dùng đúng booking, block, sức chứa, rate, min-stay và stop-sell đang có trong app. Hold 10 phút được xử lý ở Gateway production.</p></div></div></div>`}
async function inventoryAction(fn,msg){try{await fn();toast(msg);if(MODE==='auth'){await syncInventoryRemote();if(typeof syncRatesRemote==='function')await syncRatesRemote()}else localStorage.setItem(STORE,JSON.stringify(D));render()}catch(e){console.error(e);toast(e?.message||String(e),true)}}
function renderWebResults(){const cin=$('webCin')?.value,cout=$('webCout')?.value,guests=+$('webGuests')?.value||1,channel=$('webChannel')?.value||'WEBSITE',box=$('webResults');if(!box)return;if(!cin||!cout||cout<=cin){box.innerHTML='<div class="note warn">Ngày lưu trú không hợp lệ.</div>';return}const rows=simulatorResults(cin,cout,guests,channel);box.innerHTML=`<div class="scroll"><table><tr><th>Villa</th><th>Sức chứa</th><th>Trạng thái</th><th>Giá phòng</th></tr>${rows.map(r=>`<tr><td><b>${esc(r.villa.name)}</b></td><td>${policyFor(r.villa.code).maxGuests||'—'} khách</td><td>${r.ok?'<span class="tag ok">Có thể bán</span>':`<span class="tag bad">${esc(r.reason)}</span>${r.detail?`<br><small>${esc(r.detail)}</small>`:''}`}</td><td>${r.ok?`<b>${fmt(r.total)}</b><br><small>${r.nights} đêm · ${esc(channelLabel(channel))}</small>`:'—'}</td></tr>`).join('')}</table></div>`}
function bindInventoryControl(){if(current!=='inventoryControl')return;$('createBlock')?.addEventListener('click',()=>inventoryAction(async()=>{const villa=$('blockVilla').value,start=$('blockFrom').value,end=$('blockTo').value,type=$('blockType').value,note=$('blockNote').value.trim();if(!start||!end||end<start)throw new Error('Khoảng ngày block không hợp lệ');if(MODE==='auth')await insert('availability_blocks',{villa_id:vid(villa),start_date:start,end_date:end,block_type:type,source:'anstay_app',note:note||null,active:true});else D.availabilityBlocks.push({id:Date.now(),villaId:VILLAS.find(v=>v.code===villa)?.id||villa,villa,start,end,type,source:'anstay_app',note,active:true})},'Đã khóa phòng'));document.querySelectorAll('[data-open-block]').forEach(b=>b.addEventListener('click',()=>inventoryAction(async()=>{const id=+b.dataset.openBlock,row=D.availabilityBlocks.find(x=>x.id===id);if(MODE==='auth'){const {error}=await neon.from('availability_blocks').update({active:false,updated_at:new Date().toISOString()}).eq('id',id);if(error)throw new Error(dbError(error))}else if(row)row.active=false},'Đã mở lại phòng')));document.querySelectorAll('[data-save-cap]').forEach(b=>b.addEventListener('click',()=>inventoryAction(async()=>{const code=b.dataset.saveCap,v=VILLAS.find(x=>x.code===code),n=+document.querySelector(`[data-cap="${code}"]`)?.value;if(!n||n<1)throw new Error('Sức chứa phải lớn hơn 0');if(MODE==='auth'){const {error}=await neon.from('villa_booking_policies').upsert({villa_id:v.id,max_guests:n,updated_at:new Date().toISOString()},{onConflict:'villa_id'});if(error)throw new Error(dbError(error))}else{let p=policyFor(code);if(!D.bookingPolicies.includes(p)){p={villaId:v.id||code};D.bookingPolicies.push(p)}p.maxGuests=n}},'Đã lưu sức chứa')));$('applySeason')?.addEventListener('click',()=>inventoryAction(async()=>{const name=$('seasonName').value.trim(),villa=$('seasonVilla').value,pid=+$('seasonPlan').value,start=$('seasonFrom').value,end=$('seasonTo').value,weekday=+$('seasonWeekday').value,sat=+$('seasonSaturday').value,sun=+$('seasonSunday').value,minStay=+$('seasonMin').value||1,published=$('seasonPublish').checked,stopSell=$('seasonStop').checked,note=$('seasonNote').value.trim();if(!name)throw new Error('Nhập tên mùa giá');if(!start||!end||end<start)throw new Error('Khoảng ngày không hợp lệ');if(Math.min(weekday,sat,sun)<=0)throw new Error('Ba mức giá phải lớn hơn 0');if(MODE==='auth'){const {error}=await neon.rpc('create_and_apply_rate_season',{p_name:name,p_villa_id:vid(villa),p_rate_plan_id:pid,p_start_date:start,p_end_date:end,p_weekday:weekday,p_saturday:sat,p_sunday:sun,p_min_stay:minStay,p_published:published,p_stop_sell:stopSell,p_note:note||null});if(error)throw new Error(dbError(error))}else{const v=VILLAS.find(x=>x.code===villa),sid=Date.now();D.rateSeasons.push({id:sid,name,villaId:v.id||villa,villa,planId:pid,start,end,weekday,saturday:sat,sunday:sun,minStay,published,stopSell,active:true,note,appliedAt:new Date().toISOString()});for(const date of rateDates(start,end,367)){const d=new Date(date+'T00:00:00'),price=d.getDay()===6?sat:d.getDay()===0?sun:weekday;let r=rateRow(villa,pid,date);if(!r){r={id:Date.now()+D.rateCalendar.length,villaId:v.id||villa,planId:pid,date};D.rateCalendar.push(r)}Object.assign(r,{baseRate:price,minStay,published,stopSell,note:'Season: '+name})}}},published?'Đã áp dụng và phát hành mùa giá':'Đã áp dụng mùa giá ở trạng thái Draft'));$('runWebSearch')?.addEventListener('click',renderWebResults)}
const render_v13=render;render=function(){if(current==='inventoryControl'){demoInventorySeed();$('content').innerHTML=inventoryControl();bindInventoryControl();return}render_v13()};
renderNav();render();
