D.channels=D.channels||[];D.ratePlans=D.ratePlans||[];D.rateCalendar=D.rateCalendar||[];D.channelMappings=D.channelMappings||[];D.surcharges=D.surcharges||[];
if(!NAV.some(x=>x[0]==='rates'))NAV.splice(Math.max(0,NAV.length-1),0,['rates','↗','Giá & Kênh','Price Engine, Rate Plan và kết nối Website/OTA']);
for(const r of ['admin','sales','ops'])if(ACCESS[r]&&!ACCESS[r].includes('rates'))ACCESS[r].push('rates');
let RATE_UI={villa:'NHAN',planId:null,channel:'WEBSITE',from:day(),to:(()=>{const d=new Date();d.setDate(d.getDate()+30);return day(d)})()};
function rateCanWrite(){return MODE==='demo'||ROLE==='admin'||ROLE==='ops'}
function channelLabel(code){return D.channels.find(x=>x.code===code)?.name||code}
function planName(id){return D.ratePlans.find(x=>String(x.id)===String(id))?.name||'—'}
function surchargeFor(code){const v=VILLAS.find(x=>x.code===code);return D.surcharges.find(x=>String(x.villaId)===String(v?.id))||{villaId:v?.id,adult:0,child:0,infant:0,basis:'manual',note:''}}
function mappingFor(channelCode,villaCode,planId){const c=D.channels.find(x=>x.code===channelCode),v=VILLAS.find(x=>x.code===villaCode);return D.channelMappings.find(x=>String(x.channelId)===String(c?.id)&&String(x.villaId)===String(v?.id)&&String(x.planId)===String(planId))}
function rateRow(villaCode,planId,date){const v=VILLAS.find(x=>x.code===villaCode);return D.rateCalendar.find(x=>String(x.villaId)===String(v?.id)&&String(x.planId)===String(planId)&&x.date===date)}
function adjustedRate(row,mapping){if(!row||row.baseRate==null)return null;return Math.max(0,Math.round((+row.baseRate||0)*(1+(+mapping?.pct||0)/100)+(+mapping?.flat||0)))}
function rateDates(from,to,max=45){const a=[],d=new Date(from+'T00:00:00');for(let i=0;i<max&&day(d)<=to;i++){a.push(day(d));d.setDate(d.getDate()+1)}return a}
function ensureRateDemo(){if(MODE!=='demo')return;if(!D.channels.length)D.channels=[{id:1,code:'DIRECT',name:'Direct / Sales',type:'direct',active:true},{id:2,code:'WEBSITE',name:'ANSTAY Website',type:'website',active:true},{id:3,code:'BOOKING_COM',name:'Booking.com',type:'ota',active:true},{id:4,code:'AGODA',name:'Agoda',type:'ota',active:true},{id:5,code:'AIRBNB',name:'Airbnb',type:'ota',active:true},{id:6,code:'TRAVELOKA',name:'Traveloka',type:'ota',active:true}];if(!D.ratePlans.length)D.ratePlans=[{id:1,code:'BAR',name:'Best Available Rate',currency:'VND',minStay:1,active:true}];if(!D.surcharges.length)D.surcharges=VILLAS.map(v=>({villaId:v.id||v.code,adult:250000,child:150000,infant:0,basis:'manual',note:'Demo: xác nhận cách tính trước khi tự cộng vào báo giá.'}));if(!D.channelMappings.length){for(const c of D.channels.filter(x=>['DIRECT','WEBSITE'].includes(x.code)))for(const v of VILLAS)D.channelMappings.push({id:Date.now()+D.channelMappings.length,channelId:c.id,villaId:v.id||v.code,planId:1,pct:0,flat:0,active:true,externalProperty:'',externalRoom:'',externalPlan:''})}if(!D.rateCalendar.length){const base={NHAN:[2700000,3000000],SAM:[3000000,3500000],GIO:[3000000,3500000],NANG:[3000000,3500000]};for(const v of VILLAS){if(!base[v.code])continue;const d=new Date();d.setDate(d.getDate()-3);for(let i=0;i<60;i++){const k=day(d),dow=d.getDay(),price=dow===6?base[v.code][1]:base[v.code][0];D.rateCalendar.push({id:Date.now()+D.rateCalendar.length,villaId:v.id||v.code,planId:1,date:k,baseRate:price,minStay:1,published:false,stopSell:false,cta:false,ctd:false});d.setDate(d.getDate()+1)}}}}
async function syncRatesRemote(){if(MODE!=='auth'){ensureRateDemo();return}const from=(()=>{const d=new Date();d.setDate(d.getDate()-31);return day(d)})(),to=(()=>{const d=new Date();d.setDate(d.getDate()+400);return day(d)})();const [channels,plans,maps,surcharges]=await Promise.all([safeQ('sales_channels'),safeQ('rate_plans'),safeQ('channel_mappings'),safeQ('villa_guest_surcharges')]);const rr=await neon.from('rate_calendar').select('*').gte('stay_date',from).lte('stay_date',to).limit(5000);if(rr.error)throw new Error(dbError(rr.error));D.channels=channels.map(x=>({id:+x.id,code:x.code,name:x.name,type:x.channel_type,currency:x.currency,commission:+x.default_commission_pct||0,active:x.active!==false}));D.ratePlans=plans.map(x=>({id:+x.id,code:x.code,name:x.name,currency:x.currency,description:x.description||'',minStay:+x.default_min_stay||1,active:x.active!==false}));D.channelMappings=maps.map(x=>({id:+x.id,channelId:+x.channel_id,villaId:+x.villa_id,planId:+x.rate_plan_id,externalProperty:x.external_property_id||'',externalRoom:x.external_room_id||'',externalPlan:x.external_rate_plan_id||'',pct:+x.rate_adjustment_pct||0,flat:+x.rate_adjustment_flat||0,active:x.active!==false}));D.surcharges=surcharges.map(x=>({villaId:+x.villa_id,adult:+x.extra_adult_price||0,child:+x.child_6_12_price||0,infant:+x.child_under_6_price||0,basis:x.charge_basis||'manual',note:x.note||''}));D.rateCalendar=(rr.data||[]).map(x=>({id:+x.id,villaId:+x.villa_id,planId:+x.rate_plan_id,date:x.stay_date,baseRate:x.base_rate==null?null:+x.base_rate,minStay:+x.min_stay||1,published:!!x.published,stopSell:!!x.stop_sell,cta:!!x.closed_to_arrival,ctd:!!x.closed_to_departure,note:x.note||''}));if(!RATE_UI.planId)RATE_UI.planId=D.ratePlans[0]?.id||null;if(!VILLAS.some(v=>v.code===RATE_UI.villa))RATE_UI.villa=VILLAS[0]?.code||'NHAN'}
const loadRemote_rate12=loadRemote;loadRemote=async function(){await loadRemote_rate12();await syncRatesRemote();if(current==='rates')render()};
function ratePreviewRows(){const pid=RATE_UI.planId||D.ratePlans[0]?.id,mp=mappingFor(RATE_UI.channel,RATE_UI.villa,pid),dates=rateDates(RATE_UI.from,RATE_UI.to,45);return dates.map(date=>{const r=rateRow(RATE_UI.villa,pid,date),rate=adjustedRate(r,mp);return {date,row:r,rate,mp}})}
function rateChannelReadiness(c){const maps=D.channelMappings.filter(m=>String(m.channelId)===String(c.id)&&m.active),external=c.type==='ota'?maps.filter(m=>m.externalProperty&&m.externalRoom).length:maps.length;return {maps:maps.length,ready:external}}
function rates(){ensureRateDemo();if(!RATE_UI.planId)RATE_UI.planId=D.ratePlans[0]?.id||null;const can=rateCanWrite(),pid=RATE_UI.planId,preview=ratePreviewRows(),published=preview.filter(x=>x.row?.published&&x.rate!=null&&!x.row.stopSell).length,total=preview.length,s=surchargeFor(RATE_UI.villa),map=mappingFor(RATE_UI.channel,RATE_UI.villa,pid),channelRateNote=map?`${map.pct>=0?'+':''}${map.pct}% ${map.flat?'+ '+fmt(map.flat):''}`:'Chưa mapping';return `<div class="view"><div class="note"><b>ANSTAY Price & Channel Control:</b> quản lý một giá gốc theo ngày rồi điều chỉnh theo từng kênh. Chỉ ngày <b>Đã phát hành</b> mới được Gateway trả ra Website/OTA.</div><div class="grid kpis"><div class="kpi"><span>Rate plan</span><b>${D.ratePlans.filter(x=>x.active).length}</b></div><div class="kpi"><span>Kênh</span><b>${D.channels.filter(x=>x.active).length}</b></div><div class="kpi"><span>Ngày có giá trong khung xem</span><b>${preview.filter(x=>x.row?.baseRate!=null).length}/${total}</b></div><div class="kpi"><span>Ngày sẵn sàng phát</span><b>${published}/${total}</b></div></div><div class="grid two"><div class="card"><h3>Bảng giá theo ngày</h3><div class="form"><label>Villa<select id="rateVilla">${vopts()}</select></label><label>Rate plan<select id="ratePlan">${D.ratePlans.filter(x=>x.active).map(x=>`<option value="${x.id}" ${String(x.id)===String(pid)?'selected':''}>${esc(x.code)} · ${esc(x.name)}</option>`).join('')}</select></label><label>Từ ngày<input id="rateFrom" type="date" value="${RATE_UI.from}"></label><label>Đến ngày<input id="rateTo" type="date" value="${RATE_UI.to}"></label><label>Giá T2–T6<input id="rateWeekday" type="number" min="0" step="10000" value="0"></label><label>Giá Thứ 7<input id="rateSaturday" type="number" min="0" step="10000" value="0"></label><label>Giá Chủ nhật<input id="rateSunday" type="number" min="0" step="10000" value="0"></label><label>Min stay<input id="rateMinStay" type="number" min="1" value="1"></label><label><span><input id="ratePublished" type="checkbox" style="width:auto;display:inline;margin-right:6px">Phát hành ngay</span></label><label><span><input id="rateStopSell" type="checkbox" style="width:auto;display:inline;margin-right:6px">Stop sell cả khoảng</span></label></div><div class="row"><button class="primary" id="applyRateRange" ${can?'':'disabled'}>Áp dụng bảng giá</button><button id="newRatePlan" ${can?'':'disabled'}>Tạo Rate Plan</button></div><p class="muted">Tối đa 367 ngày/lần. Giá chưa phát hành chỉ dùng nội bộ, không đi ra Gateway.</p></div><div class="card"><h3>Preview Website / OTA</h3><div class="form"><label>Kênh<select id="rateChannel">${D.channels.filter(x=>x.active).map(x=>`<option value="${x.code}" ${x.code===RATE_UI.channel?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><label>Điều chỉnh kênh<input value="${esc(channelRateNote)}" disabled></label></div><div class="scroll"><table><tr><th>Ngày</th><th>Giá gốc</th><th>Giá kênh</th><th>Min stay</th><th>Trạng thái</th><th></th></tr>${preview.map(x=>`<tr><td>${x.date}</td><td>${x.row?.baseRate==null?'—':fmt(x.row.baseRate)}</td><td><b>${x.row?.published&&x.rate!=null&&!x.row.stopSell?fmt(x.rate):'—'}</b></td><td>${x.row?.minStay||'—'}</td><td>${!x.row?'<span class="tag warning">Chưa nhập</span>':x.row.stopSell?'<span class="tag bad">Stop sell</span>':x.row.published?'<span class="tag ok">Đã phát hành</span>':'<span class="tag warning">Nháp</span>'}</td><td>${x.row&&can?`<button data-rate-publish="${x.row.id}">${x.row.published?'Ẩn giá':'Phát hành'}</button> <button data-rate-stop="${x.row.id}">${x.row.stopSell?'Mở bán':'Stop sell'}</button>`:''}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">Chưa có bảng giá</td></tr>'}</table></div></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Mapping kênh phân phối</h3><div class="form"><label>Kênh<select id="mapChannel">${D.channels.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></label><label>Villa<select id="mapVilla">${vopts()}</select></label><label>Rate plan<select id="mapPlan">${D.ratePlans.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.code)} · ${esc(x.name)}</option>`).join('')}</select></label><label>Điều chỉnh %<input id="mapPct" type="number" step="0.1" value="0"></label><label>Điều chỉnh cố định<input id="mapFlat" type="number" step="10000" value="0"></label><label>External Property ID<input id="mapProperty" placeholder="Điền khi OTA cấp"></label><label>External Room ID<input id="mapRoom" placeholder="Điền khi OTA cấp"></label><label>External Rate Plan ID<input id="mapExternalPlan" placeholder="Điền khi OTA cấp"></label><label><span><input id="mapActive" type="checkbox" checked style="width:auto;display:inline;margin-right:6px">Bật mapping</span></label></div><button class="primary" id="saveChannelMap" ${can?'':'disabled'}>Lưu mapping</button><div class="scroll" style="margin-top:12px"><table><tr><th>Kênh</th><th>Mapping villa</th><th>OTA IDs sẵn sàng</th><th>Trạng thái</th></tr>${D.channels.filter(x=>x.active).map(c=>{const r=rateChannelReadiness(c);return `<tr><td><b>${esc(c.name)}</b><br><small>${esc(c.code)}</small></td><td>${r.maps}/${VILLAS.length}</td><td>${c.type==='ota'?r.ready+'/'+VILLAS.length:'—'}</td><td>${c.type!=='ota'&&r.maps>=VILLAS.length?'<span class="tag ok">Nội bộ sẵn sàng</span>':c.type==='ota'&&r.ready>=VILLAS.length?'<span class="tag ok">Sẵn sàng adapter</span>':'<span class="tag warning">Chờ cấu hình</span>'}</td></tr>`}).join('')}</table></div></div><div class="card"><h3>Phụ thu khách</h3><div class="form"><label>Villa<select id="surVilla">${vopts()}</select></label><label>Người lớn thêm<input id="surAdult" type="number" min="0" value="${s.adult}"></label><label>Trẻ 6–12<input id="surChild" type="number" min="0" value="${s.child}"></label><label>Trẻ dưới 6<input id="surInfant" type="number" min="0" value="${s.infant}"></label><label>Cách tính<select id="surBasis"><option value="manual" ${s.basis==='manual'?'selected':''}>Chưa tự động — xác nhận thủ công</option><option value="per_stay" ${s.basis==='per_stay'?'selected':''}>Mỗi kỳ lưu trú</option><option value="per_night" ${s.basis==='per_night'?'selected':''}>Mỗi đêm</option></select></label><label class="full">Ghi chú<input id="surNote" value="${esc(s.note)}"></label></div><button class="primary" id="saveSurcharge" ${can?'':'disabled'}>Lưu phụ thu</button><div class="note warn" style="margin-top:12px"><b>An toàn giá:</b> khi “Cách tính” còn là <b>Chưa tự động</b>, Gateway chỉ trả mức phụ thu tham khảo và không tự cộng vào tổng tiền website.</div></div></div><div class="card" style="margin-top:14px"><h3>Cổng kết nối</h3><div class="scroll"><table><tr><th>Dịch vụ</th><th>Endpoint</th><th>Dùng cho</th></tr><tr><td>ARI</td><td><code>/api/v1/ari</code></td><td>Availability + Rate + Restriction theo ngày</td></tr><tr><td>Quote</td><td><code>/api/v1/quote</code></td><td>Website hỏi tổng giá trước khi tạo booking</td></tr><tr><td>Reservations</td><td><code>/api/v1/reservations</code></td><td>Website/OTA đẩy booking vào ANSTAY</td></tr><tr><td>Webhook</td><td><code>/api/v1/webhook</code></td><td>Adapter Booking.com, Agoda, Airbnb, Traveloka...</td></tr></table></div><p class="muted">OTA thật vẫn giữ trạng thái chờ cho đến khi có Property/Room/Rate Plan ID và credential do từng đối tác cấp.</p></div></div>`}
function fillSurchargeForm(){const el=$('surVilla');if(!el)return;const s=surchargeFor(el.value);$('surAdult').value=s.adult;$('surChild').value=s.child;$('surInfant').value=s.infant;$('surBasis').value=s.basis||'manual';$('surNote').value=s.note||''}
function fillMappingForm(){const cid=+$('mapChannel')?.value,vcode2=$('mapVilla')?.value,pid=+$('mapPlan')?.value,c=D.channels.find(x=>x.id===cid),m=mappingFor(c?.code,vcode2,pid);$('mapPct').value=m?.pct||0;$('mapFlat').value=m?.flat||0;$('mapProperty').value=m?.externalProperty||'';$('mapRoom').value=m?.externalRoom||'';$('mapExternalPlan').value=m?.externalPlan||'';$('mapActive').checked=m?m.active:true}
async function rateAction(fn,msg){try{await fn();toast(msg);if(MODE==='auth')await syncRatesRemote();else localStorage.setItem(STORE,JSON.stringify(D));render()}catch(e){console.error(e);toast(e?.message||String(e),true)}}
function bindRates(){if(current!=='rates')return;const v=$('rateVilla');if(v)v.value=RATE_UI.villa;const chan=$('rateChannel');if(chan)chan.value=RATE_UI.channel;['rateVilla','ratePlan','rateFrom','rateTo','rateChannel'].forEach(id=>$(id)?.addEventListener('change',()=>{RATE_UI.villa=$('rateVilla').value;RATE_UI.planId=+$('ratePlan').value;RATE_UI.from=$('rateFrom').value;RATE_UI.to=$('rateTo').value;RATE_UI.channel=$('rateChannel').value;render()}));$('surVilla')?.addEventListener('change',fillSurchargeForm);$('mapChannel')?.addEventListener('change',fillMappingForm);$('mapVilla')?.addEventListener('change',fillMappingForm);$('mapPlan')?.addEventListener('change',fillMappingForm);fillMappingForm();$('applyRateRange')?.addEventListener('click',()=>rateAction(async()=>{const villa=$('rateVilla').value,pid=+$('ratePlan').value,from=$('rateFrom').value,to=$('rateTo').value,weekday=+$('rateWeekday').value,sat=+$('rateSaturday').value,sun=+$('rateSunday').value,minStay=+$('rateMinStay').value||1,published=$('ratePublished').checked,stopSell=$('rateStopSell').checked;if(!from||!to||to<from)throw new Error('Khoảng ngày không hợp lệ');if(weekday<0||sat<0||sun<0)throw new Error('Giá không hợp lệ');if(MODE==='auth'){const {error}=await neon.rpc('set_rate_range',{p_villa_id:vid(villa),p_rate_plan_id:pid,p_from:from,p_to:to,p_weekday:weekday,p_saturday:sat,p_sunday:sun,p_min_stay:minStay,p_published:published,p_stop_sell:stopSell});if(error)throw new Error(dbError(error))}else{for(const date of rateDates(from,to,367)){const d=new Date(date+'T00:00:00'),price=d.getDay()===6?sat:d.getDay()===0?sun:weekday,v=VILLAS.find(x=>x.code===villa);let r=rateRow(villa,pid,date);if(!r){r={id:Date.now()+D.rateCalendar.length,villaId:v.id||v.code,planId:pid,date};D.rateCalendar.push(r)}Object.assign(r,{baseRate:price,minStay,published,stopSell})}}},published?'Đã cập nhật và phát hành bảng giá':'Đã lưu bảng giá nháp'));$('newRatePlan')?.addEventListener('click',()=>{const code=(prompt('Mã Rate Plan, ví dụ BAR-NRF:','')||'').trim().toUpperCase();if(!code)return;const name=(prompt('Tên Rate Plan:','')||'').trim();if(!name)return;rateAction(async()=>{if(MODE==='auth')await insert('rate_plans',{code,name,currency:'VND',default_min_stay:1,active:true});else D.ratePlans.push({id:Date.now(),code,name,currency:'VND',minStay:1,active:true})},'Đã tạo Rate Plan')});$('saveChannelMap')?.addEventListener('click',()=>rateAction(async()=>{const cid=+$('mapChannel').value,villa=$('mapVilla').value,pid=+$('mapPlan').value,pct=+$('mapPct').value||0,flat=+$('mapFlat').value||0,prop=$('mapProperty').value.trim(),room=$('mapRoom').value.trim(),extPlan=$('mapExternalPlan').value.trim(),active=$('mapActive').checked;if(MODE==='auth'){const {error}=await neon.rpc('set_channel_mapping',{p_channel_id:cid,p_villa_id:vid(villa),p_rate_plan_id:pid,p_adjustment_pct:pct,p_adjustment_flat:flat,p_external_property_id:prop||null,p_external_room_id:room||null,p_external_rate_plan_id:extPlan||null,p_active:active});if(error)throw new Error(dbError(error))}else{const c=D.channels.find(x=>x.id===cid),v=VILLAS.find(x=>x.code===villa);let m=mappingFor(c.code,villa,pid);if(!m){m={id:Date.now(),channelId:cid,villaId:v.id||v.code,planId:pid};D.channelMappings.push(m)}Object.assign(m,{pct,flat,externalProperty:prop,externalRoom:room,externalPlan:extPlan,active})}},'Đã lưu mapping kênh'));$('saveSurcharge')?.addEventListener('click',()=>rateAction(async()=>{const villa=$('surVilla').value,adult=+$('surAdult').value||0,child=+$('surChild').value||0,infant=+$('surInfant').value||0,basis=$('surBasis').value,note=$('surNote').value.trim();if(MODE==='auth'){const {error}=await neon.rpc('set_villa_surcharge',{p_villa_id:vid(villa),p_extra_adult:adult,p_child_6_12:child,p_child_under_6:infant,p_basis:basis,p_note:note||null});if(error)throw new Error(dbError(error))}else{const v=VILLAS.find(x=>x.code===villa);let s=D.surcharges.find(x=>String(x.villaId)===String(v.id||v.code));if(!s){s={villaId:v.id||v.code};D.surcharges.push(s)}Object.assign(s,{adult,child,infant,basis,note})}},'Đã lưu quy tắc phụ thu'));document.querySelectorAll('[data-rate-publish]').forEach(b=>b.addEventListener('click',()=>{const r=D.rateCalendar.find(x=>x.id===+b.dataset.ratePublish);if(!r)return;rateAction(async()=>{if(MODE==='auth'){const {error}=await neon.rpc('set_rate_day_flags',{p_villa_id:r.villaId,p_rate_plan_id:r.planId,p_stay_date:r.date,p_stop_sell:r.stopSell,p_closed_to_arrival:r.cta,p_closed_to_departure:r.ctd,p_published:!r.published});if(error)throw new Error(dbError(error))}else r.published=!r.published},r.published?'Đã ẩn giá khỏi kênh':'Đã phát hành giá')}));document.querySelectorAll('[data-rate-stop]').forEach(b=>b.addEventListener('click',()=>{const r=D.rateCalendar.find(x=>x.id===+b.dataset.rateStop);if(!r)return;rateAction(async()=>{if(MODE==='auth'){const {error}=await neon.rpc('set_rate_day_flags',{p_villa_id:r.villaId,p_rate_plan_id:r.planId,p_stay_date:r.date,p_stop_sell:!r.stopSell,p_closed_to_arrival:r.cta,p_closed_to_departure:r.ctd,p_published:r.published});if(error)throw new Error(dbError(error))}else r.stopSell=!r.stopSell},r.stopSell?'Đã mở bán lại':'Đã stop sell')}))}
const render_rate12=render;render=function(){if(current==='rates'){ensureRateDemo();$('content').innerHTML=rates();bindRates();return}render_rate12()};
renderNav();render();