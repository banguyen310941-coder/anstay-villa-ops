D.commercial=D.commercial||{};
async function syncCommercialRemote(){if(MODE!=='auth')return;const rows=await safeQ('bookings');for(const x of rows){const id=+x.id;D.commercial[id]={gross:+x.gross_room_price||0,discount:+x.discount_amount||0,refund:+x.refund_amount||0,ota:+x.ota_commission||0,tax:+x.tax_fee||0,recognized:x.recognized_revenue==null?null:(+x.recognized_revenue||0),ownerBase:x.owner_share_base==null?null:(+x.owner_share_base||0)}}}
function commercialFor(b){const x=D.commercial?.[b.id]||{};return {gross:+x.gross||0,discount:+x.discount||0,refund:+x.refund||0,ota:+x.ota||0,tax:+x.tax||0,recognized:x.recognized==null?null:(+x.recognized||0),ownerBase:x.ownerBase==null?null:(+x.ownerBase||0)}}
function enhanceCommercial(){if(current!=='booking'||$('commercialControl'))return;const view=$('content')?.querySelector('.view');if(!view)return;const active=D.bookings.filter(b=>b.status!=='cancelled');const card=document.createElement('div');card.id='commercialControl';card.className='card';card.style.marginTop='14px';card.innerHTML=`<h3>Thành phần doanh thu & căn cứ chia chủ</h3><p class="muted">Không gộp giá bán, giảm giá, hoàn tiền, hoa hồng OTA, thuế/phí và doanh thu ghi nhận vào một con số. Các trường này được lưu riêng để kế toán và đối soát dùng đúng mục đích.</p><div class="grid two"><div><div class="form"><label>Booking<select id="cBooking">${active.map(b=>`<option value="${b.id}">${esc(b.code)} · ${vname(b.villa)} · ${esc(b.guest)}</option>`).join('')}</select></label><label>Giá phòng gộp<input id="cGross" type="number" min="0" value="0"></label><label>Giảm giá<input id="cDiscount" type="number" min="0" value="0"></label><label>Hoàn tiền<input id="cRefund" type="number" min="0" value="0"></label><label>Hoa hồng OTA<input id="cOta" type="number" min="0" value="0"></label><label>Thuế / phí<input id="cTax" type="number" min="0" value="0"></label><label>Doanh thu ghi nhận<input id="cRecognized" type="number" min="0" placeholder="Để trống = DT phòng"></label><label>Căn cứ chia chủ<input id="cOwnerBase" type="number" min="0" placeholder="Để trống = DT phòng"></label></div><div class="row"><button class="primary" id="saveCommercial" ${active.length?'':'disabled'}>Lưu cấu phần</button><button id="useRoomAsOwnerBase" ${active.length?'':'disabled'}>Lấy DT phòng làm căn cứ chia</button></div><p class="muted" id="commercialHint">Nhàn hiện tạm theo 50% doanh thu bán phòng; các khoản khấu trừ trước chia sẽ chỉ áp dụng sau khi hợp đồng xác nhận.</p></div><div><h3>Kiểm tra booking đang chọn</h3><div id="commercialPreview" class="note">Chọn booking để xem cấu phần.</div><p class="muted">“Doanh thu ghi nhận” và “căn cứ chia chủ” là trường nghiệp vụ độc lập; hệ thống không tự suy diễn từ hoa hồng OTA, giảm giá hay thuế/phí.</p></div></div><div class="scroll" style="margin-top:14px"><table><tr><th>Booking</th><th>Villa</th><th>Giá gộp</th><th>Giảm</th><th>Hoàn</th><th>OTA</th><th>Thuế/phí</th><th>DT ghi nhận</th><th>Căn cứ chia chủ</th></tr>${active.slice().sort((a,b)=>String(b.in).localeCompare(String(a.in))).map(b=>{const c=commercialFor(b);return `<tr><td><b>${esc(b.code)}</b></td><td>${vname(b.villa)}</td><td>${fmt(c.gross)}</td><td>${fmt(c.discount)}</td><td>${fmt(c.refund)}</td><td>${fmt(c.ota)}</td><td>${fmt(c.tax)}</td><td><b>${c.recognized==null?'—':fmt(c.recognized)}</b></td><td>${c.ownerBase==null?'—':fmt(c.ownerBase)}</td></tr>`}).join('')||'<tr><td colspan="9" class="empty">Chưa có booking</td></tr>'}</table></div>`;view.appendChild(card);fillCommercialForm()}
function fillCommercialForm(){const sel=$('cBooking');if(!sel)return;const b=D.bookings.find(x=>String(x.id)===String(sel.value));if(!b)return;const c=commercialFor(b);$('cGross').value=c.gross||b.room||0;$('cDiscount').value=c.discount;$('cRefund').value=c.refund;$('cOta').value=c.ota;$('cTax').value=c.tax;$('cRecognized').value=c.recognized==null?'':c.recognized;$('cOwnerBase').value=c.ownerBase==null?'':c.ownerBase;const after=Math.max(0,(+($('cGross').value)||0)-c.discount-c.refund);$('commercialPreview').innerHTML=`<p>Giá gộp: <b>${fmt(+($('cGross').value)||0)}</b></p><p>Sau giảm/hoàn: <b>${fmt(after)}</b></p><p>Hoa hồng OTA theo dõi riêng: <b>${fmt(c.ota)}</b></p><p>Thuế/phí theo dõi riêng: <b>${fmt(c.tax)}</b></p><p>Đã thu: <b>${fmt(paidForBooking(b))}</b> · Còn thu theo booking: <b>${fmt(Math.max(0,(+b.room||0)+(+b.other||0)-paidForBooking(b)))}</b></p>`;$('commercialHint').textContent=b.villa==='NHAN'?'Nhàn: căn cứ chia hiện tạm là doanh thu bán phòng; chưa tự trừ OTA/hoàn/thuế/phí.':`${vname(b.villa)}: ${VILLAS.find(v=>v.code===b.villa)?.rule||''}`}
const render_v08=render;render=function(){render_v08();enhanceCommercial()};
const enter_v08=enterAuthenticated;enterAuthenticated=async function(session){await enter_v08(session);if(MODE==='auth'){await syncCommercialRemote();render()}};
const refresh_v08=refresh;refresh=async function(){await refresh_v08();if(MODE==='auth'){await syncCommercialRemote();render()}};
document.addEventListener('change',e=>{if(e.target?.id==='cBooking')fillCommercialForm()});
document.addEventListener('input',e=>{if(['cGross','cDiscount','cRefund','cOta','cTax'].includes(e.target?.id))fillCommercialForm()});
document.addEventListener('click',e=>{if(e.target.closest('#useRoomAsOwnerBase')){const b=D.bookings.find(x=>String(x.id)===String($('cBooking')?.value));if(!b)return;$('cOwnerBase').value=+b.room||0;toast(b.villa==='NHAN'?'Đã lấy DT phòng làm căn cứ tạm tính Nhàn':'Đã lấy DT phòng làm căn cứ');return}if(e.target.closest('#saveCommercial')){const b=D.bookings.find(x=>String(x.id)===String($('cBooking')?.value));if(!b)return;const row={gross:+$('cGross').value||0,discount:+$('cDiscount').value||0,refund:+$('cRefund').value||0,ota:+$('cOta').value||0,tax:+$('cTax').value||0,recognized:$('cRecognized').value.trim()===''?null:(+$('cRecognized').value||0),ownerBase:$('cOwnerBase').value.trim()===''?null:(+$('cOwnerBase').value||0)};paymentAction(async()=>{if(MODE==='auth'){await update('bookings',b.id,{gross_room_price:row.gross,discount_amount:row.discount,refund_amount:row.refund,ota_commission:row.ota,tax_fee:row.tax,recognized_revenue:row.recognized,owner_share_base:row.ownerBase})}D.commercial[b.id]=row;if(MODE==='demo')localStorage.setItem(STORE,JSON.stringify(D))},'Đã lưu cấu phần doanh thu')}});
render();
