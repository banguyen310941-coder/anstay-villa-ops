D.settlements=D.settlements||[];
let REPORT_MONTH=mon();
function reportBounds(m=REPORT_MONTH){const [y,mm]=m.split('-').map(Number),s=`${y}-${String(mm).padStart(2,'0')}-01`,e=new Date(y,mm,0).toLocaleDateString('en-CA');return {s,e,y,mm,days:new Date(y,mm,0).getDate()}}
function inReportMonth(s,m=REPORT_MONTH){return !!s&&String(s).slice(0,7)===m}
function bookingsForMonth(m=REPORT_MONTH){return D.bookings.filter(b=>inReportMonth(b.in,m)&&b.status!=='cancelled')}
function financeForMonth(m=REPORT_MONTH){return D.finance.filter(f=>inReportMonth(f.date,m))}
function paymentsForMonth(m=REPORT_MONTH){return (D.payments||[]).filter(p=>inReportMonth(p.date,m))}
function commercialForV10(b){const x=D.commercial?.[b.id]||{};return {gross:x.gross==null?(+b.room||0):(+x.gross||0),discount:+x.discount||0,refund:+x.refund||0,ota:+x.ota||0,tax:+x.tax||0,recognized:x.recognized==null?null:+x.recognized,ownerBase:x.ownerBase==null?null:+x.ownerBase}}
function villaMonth(code,m=REPORT_MONTH){const bs=bookingsForMonth(m).filter(b=>b.villa===code),fs=financeForMonth(m).filter(f=>f.villa===code);let room=0,other=0,gross=0,recognizedRoom=0,ownerBase=0,ownerBaseConfigured=false,soldNights=0;const {s,e,days}=reportBounds(m),start=new Date(s+'T00:00:00'),end=new Date(new Date(s+'T00:00:00').getFullYear(),new Date(s+'T00:00:00').getMonth()+1,1);for(const b of bs){const c=commercialForV10(b);room+=+b.room||0;other+=+b.other||0;gross+=c.gross;recognizedRoom+=c.recognized==null?(+b.room||0):c.recognized;if(c.ownerBase!=null){ownerBase+=c.ownerBase;ownerBaseConfigured=true}const bi=new Date(b.in+'T00:00:00'),bo=new Date(b.out+'T00:00:00'),a=bi>start?bi:start,z=bo<end?bo:end;if(z>a)soldNights+=(z-a)/86400000}const expense=fs.filter(f=>f.dir==='out').reduce((n,f)=>n+(+f.amount||0),0),v=VILLAS.find(v=>v.code===code),owner=v?.model==='share'?(ownerBaseConfigured?ownerBase:room)*(v.pct/100):0,rent=v?.model==='rent'?(+v.rent||0):0,recognized=recognizedRoom+other,profit=recognized-expense-owner-rent;return {room,other,gross,recognized,expense,owner,rent,profit,soldNights,occupancy:days?Math.min(100,soldNights/days*100):0,ownerBase:ownerBaseConfigured?ownerBase:room}}
function monthChecklist(m=REPORT_MONTH){const bs=bookingsForMonth(m),ps=paymentsForMonth(m),fs=financeForMonth(m),open=bs.filter(b=>!['checked_out','cancelled'].includes(b.status)),unrec=ps.filter(p=>!p.reconciled),receivable=bs.reduce((n,b)=>n+Math.max(0,(+b.room||0)+(+b.other||0)-paidForBooking(b)),0),invoiceBookings=bs.filter(b=>!['issued','not_applicable','adjusted'].includes(b.invoice)),invoiceFinance=fs.filter(f=>!['issued','not_applicable','adjusted'].includes(f.invoice)),nhanMissing=bs.filter(b=>b.villa==='NHAN'&&(+b.room||0)>0&&(commercialForV10(b).ownerBase==null||commercialForV10(b).ownerBase===0)),unpaidDue=fs.filter(f=>f.dir==='out'&&!f.paid&&f.due&&f.due<=reportBounds(m).e);return {open,unrec,receivable,invoiceBookings,invoiceFinance,nhanMissing,unpaidDue,hard:open.length+unrec.length}}
function settlementFor(code,m=REPORT_MONTH){return (D.settlements||[]).find(x=>x.villa===code&&String(x.month).slice(0,7)===m)}
function settlementPaymentLabel(s,due){if(!due)return '<span class="tag">Không áp dụng</span>';if(!s)return '<span class="tag warning">Chưa khóa tháng</span>';return s.paymentStatus==='paid'?'<span class="tag ok">Đã trả</span>':s.paymentStatus==='partial'?'<span class="tag warning">Trả một phần</span>':'<span class="tag bad">Chưa trả</span>'}
function managementForecast(){const n=new Date(),next=new Date(n.getFullYear(),n.getMonth()+1,1),m=next.getFullYear()+'-'+String(next.getMonth()+1).padStart(2,'0'),b=reportBounds(m),fixed=VILLAS.reduce((n,v)=>n+(v.model==='rent'?(+v.rent||0):0),0),ap=D.finance.filter(f=>f.dir==='out'&&!f.paid&&f.due&&f.due>=b.s&&f.due<=b.e).reduce((n,f)=>n+(+f.amount||0),0),unpaidSettlement=(D.settlements||[]).filter(s=>['locked'].includes(s.status)&&!['paid','not_applicable'].includes(s.paymentStatus)).reduce((n,s)=>n+Math.max(0,(+s.ownerPayable||0)+(+s.rentPayable||0)-(+s.paidAmount||0)),0);return {month:m,fixed,ap,unpaidSettlement,total:fixed+ap+unpaidSettlement}}
async function syncSettlementsRemote(){if(MODE!=='auth'){D.settlements=D.settlements||[];return}const rows=await safeQ('monthly_settlements');D.settlements=rows.map(x=>({id:+x.id,villa:vcode(x.villa_id),month:x.month_start,room:+x.room_revenue||0,other:+x.other_revenue||0,ownerPayable:+x.owner_payable||0,rentPayable:+x.fixed_rent_payable||0,expense:+x.operating_expenses||0,profit:+x.contribution_profit||0,status:x.status,paymentStatus:x.payment_status||'unpaid',paidAmount:+x.paid_amount||0,paidAt:x.paid_at,reference:x.payment_reference||''}))}
async function syncPaymentsRemote(){if(MODE!=='auth'){D.payments=D.payments||[];return}const rows=await safeQ('booking_payments');D.payments=rows.map(x=>({id:+x.id,bookingId:+x.booking_id,date:x.payment_date,amount:+x.amount||0,method:x.method||'',account:x.receiving_account||'',reference:x.reference||'',reconciled:x.reconciliation_status==='reconciled',invoice:x.invoice_status||'undetermined',note:x.note||''}))}
async function syncCommercialRemote(){if(MODE!=='auth')return;const rows=await safeQ('bookings');for(const x of rows){const id=+x.id;D.commercial[id]={gross:x.gross_room_price==null?null:+x.gross_room_price,discount:+x.discount_amount||0,refund:+x.refund_amount||0,ota:+x.ota_commission||0,tax:+x.tax_fee||0,recognized:x.recognized_revenue==null?null:+x.recognized_revenue,ownerBase:x.owner_share_base==null?null:+x.owner_share_base}}}
reports=function(){const rows=VILLAS.map(v=>[v,villaMonth(v.code)]),c=monthChecklist(),lock=D.closures.find(x=>String(x.month).slice(0,7)===REPORT_MONTH),cash=paymentsForMonth().reduce((n,p)=>n+(+p.amount||0),0)+financeForMonth().filter(f=>f.dir==='in'&&!['room','room_revenue'].includes(f.cat)).reduce((n,f)=>n+(+f.amount||0),0),receivable=c.receivable,totalRec=rows.reduce((n,x)=>n+x[1].recognized,0),totalProfit=rows.reduce((n,x)=>n+x[1].profit,0),ownerDue=rows.reduce((n,x)=>n+x[1].owner+x[1].rent,0),forecast=managementForecast(),canClose=MODE==='auth'&&(ROLE==='admin'||ROLE==='accounting')&&!lock?.status?.includes('locked')&&!c.hard;const item=(ok,label,detail,hard=false)=>`<div class="note ${ok?'':'warn'}" style="margin:8px 0"><b>${ok?'✓':'!'} ${label}</b><br><span class="muted">${detail}${!ok&&hard?' · Cần xử lý trước khi khóa tháng.':''}</span></div>`;return `<div class="view"><div class="card" style="margin-bottom:14px"><div class="row" style="justify-content:space-between;align-items:end"><div><h3>Control Center tháng</h3><p class="muted">Chốt doanh thu, dòng tiền, công nợ, P&L và nghĩa vụ chủ nhà trên cùng một kỳ.</p></div><label style="min-width:180px">Kỳ báo cáo<input id="reportMonth" type="month" value="${REPORT_MONTH}"></label></div></div><div class="grid kpis"><div class="kpi"><span>DT ghi nhận</span><b>${fmt(totalRec)}</b><small>Phòng + doanh thu khác</small></div><div class="kpi"><span>Tiền thực thu</span><b>${fmt(cash)}</b><small>Theo ngày nhận tiền</small></div><div class="kpi"><span>Còn phải thu booking</span><b>${fmt(receivable)}</b><small>Không cộng trùng doanh thu</small></div><div class="kpi"><span>LN đóng góp</span><b>${fmt(totalProfit)}</b><small>Sau chi + thuê/chia chủ</small></div></div><div class="grid two"><div class="card"><h3>P&L & công suất theo villa</h3><div class="scroll"><table><tr><th>Villa</th><th>Công suất</th><th>DT ghi nhận</th><th>Chi</th><th>Thuê/chia</th><th>LN</th></tr>${rows.map(([v,x])=>`<tr><td><b>${esc(v.name)}</b><br>${badge(v)}</td><td><b>${x.occupancy.toFixed(0)}%</b><br>${x.soldNights.toFixed(0)} đêm</td><td>${fmt(x.recognized)}</td><td>${fmt(x.expense)}</td><td>${fmt(x.owner+x.rent)}</td><td><b>${fmt(x.profit)}</b></td></tr>`).join('')}</table></div></div><div class="card"><h3>Checklist trước đóng tháng</h3>${item(!c.open.length,'Booking đã hoàn tất',c.open.length?`${c.open.length} booking chưa check-out`:'Không còn booking trạng thái mở',true)}${item(!c.unrec.length,'Tiền thu đã đối soát',c.unrec.length?`${c.unrec.length} giao dịch thu chưa đối soát`:'Tất cả giao dịch thu đã đối soát',true)}${item(!c.receivable,'Công nợ khách',c.receivable?`Còn phải thu ${fmt(c.receivable)}`:'Không còn công nợ booking')}${item(!(c.invoiceBookings.length+c.invoiceFinance.length),'Hóa đơn / chứng từ',`${c.invoiceBookings.length+c.invoiceFinance.length} dòng chưa hoàn tất trạng thái hóa đơn`)}${item(!c.nhanMissing.length,'Căn cứ chia chủ Nhàn',c.nhanMissing.length?`${c.nhanMissing.length} booking đang dùng DT phòng làm căn cứ tạm`:'Đã có căn cứ chia chủ cho booking Nhàn')}${item(!c.unpaidDue.length,'Khoản chi đến hạn',c.unpaidDue.length?`${c.unpaidDue.length} khoản đến hạn chưa đánh dấu đã trả`:'Không có khoản chi quá hạn trong kỳ')}</div></div><div class="grid two" style="margin-top:14px"><div class="card"><div class="row" style="justify-content:space-between"><div><h3>Đối soát chủ nhà / tiền thuê</h3><p class="muted">Nhàn chỉ tính phần chia trên căn cứ doanh thu phòng; doanh thu dịch vụ không đưa vào căn cứ chia.</p></div><span class="tag info">Nghĩa vụ kỳ ${fmt(ownerDue)}</span></div><div class="scroll"><table><tr><th>Villa</th><th>DT phòng</th><th>Căn cứ chia</th><th>Phải trả</th><th>Thanh toán</th><th></th></tr>${rows.map(([v,x])=>{const s=settlementFor(v.code),due=s?(s.ownerPayable+s.rentPayable):(x.owner+x.rent),paid=s?.paidAmount||0;return `<tr><td><b>${esc(v.name)}</b><br>${badge(v)}</td><td>${fmt(s?.room??x.room)}</td><td>${v.model==='share'?fmt(x.ownerBase):'—'}</td><td><b>${fmt(due)}</b></td><td>${settlementPaymentLabel(s,due)}${paid?`<br><small>${fmt(paid)}${s?.reference?' · '+esc(s.reference):''}</small>`:''}</td><td>${MODE==='auth'&&s&&due>0&&(ROLE==='admin'||ROLE==='accounting')&&s.paymentStatus!=='paid'?`<button data-settlement-pay="${s.id}" data-due="${due}" data-paid="${paid}">Ghi thanh toán</button>`:''}</td></tr>`}).join('')}</table></div></div><div class="card"><h3>Tiền cần chuẩn bị tháng ${forecast.month}</h3><table style="min-width:0"><tr><td>Thuê cố định SOL + Nắng</td><td>${fmt(forecast.fixed)}</td></tr><tr><td>Công nợ có hạn trả tháng tới</td><td>${fmt(forecast.ap)}</td></tr><tr><td>Nghĩa vụ đối soát đã khóa nhưng chưa trả</td><td>${fmt(forecast.unpaidSettlement)}</td></tr><tr><td><b>Cơ sở</b></td><td><b>${fmt(forecast.total)}</b></td></tr><tr><td>Dự phòng 10%</td><td>${fmt(forecast.total*.1)}</td></tr><tr><td><b>Mức tiền nên chuẩn bị</b></td><td><b>${fmt(forecast.total*1.1)}</b></td></tr></table><p class="muted">Chưa tự gán ngày phải trả phần chia chủ Nhàn cho đến khi hợp đồng xác nhận thời hạn thanh toán; nghĩa vụ đã khóa nhưng chưa trả được hiển thị riêng.</p></div></div>${MODE==='auth'&&(ROLE==='admin'||ROLE==='accounting')?`<div class="grid two" style="margin-top:14px"><div class="card"><h3>Đóng sổ tháng ${REPORT_MONTH}</h3><p>${lock?.status==='locked'?'<span class="tag bad">ĐÃ KHÓA</span>':'<span class="tag ok">ĐANG MỞ</span>'}</p><p class="muted">Khóa tháng sẽ chụp settlement từng villa. Hai lỗi bắt buộc phải xử lý trước: booking chưa hoàn tất và tiền thu chưa đối soát.</p><div class="row">${lock?.status==='locked'?(ROLE==='admin'?'<button id="reopenSelectedMonth">Mở khóa tháng</button>':''):`<button class="primary" id="closeSelectedMonth" ${canClose?'':'disabled'}>${c.hard?'Chưa đủ điều kiện khóa':'Đóng và khóa tháng'}</button>`}<button id="printReport">In / lưu PDF báo cáo</button></div></div><div class="card"><h3>Lịch sử đóng tháng</h3><div class="scroll"><table><tr><th>Tháng</th><th>TT</th><th>Doanh thu</th><th>LN đóng góp</th><th>Đóng lúc</th></tr>${D.closures.slice().sort((a,b)=>String(b.month).localeCompare(String(a.month))).map(x=>`<tr><td>${String(x.month).slice(0,7)}</td><td><span class="tag ${x.status==='locked'?'bad':'ok'}">${x.status==='locked'?'Khóa':'Mở'}</span></td><td>${fmt((+x.summary.room_revenue||0)+(+x.summary.other_revenue||0))}</td><td>${fmt(+x.summary.contribution_profit||0)}</td><td>${x.closedAt?new Date(x.closedAt).toLocaleString('vi-VN'):'—'}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">Chưa đóng tháng nào</td></tr>'}</table></div></div></div>`:''}</div>`}
const enter_v10=enterAuthenticated;enterAuthenticated=async function(session){await enter_v10(session);if(MODE==='auth'){await Promise.all([syncPaymentsRemote(),syncCommercialRemote(),syncSettlementsRemote()]);render()}}
const refresh_v10=refresh;refresh=async function(){await refresh_v10();if(MODE==='auth'){await Promise.all([syncPaymentsRemote(),syncCommercialRemote(),syncSettlementsRemote()]);render()}}
document.addEventListener('change',e=>{if(e.target?.id==='reportMonth'){REPORT_MONTH=e.target.value||mon();render()}});
document.addEventListener('click',e=>{if(e.target.closest('#printReport')){window.print();return}const close=e.target.closest('#closeSelectedMonth');if(close){paymentAction(async()=>{const c=monthChecklist();if(c.hard)throw new Error('Còn booking chưa hoàn tất hoặc tiền thu chưa đối soát');if(!confirm(`Đóng và khóa tháng ${REPORT_MONTH}?`))return;const {error}=await neon.rpc('close_month',{p_month:REPORT_MONTH+'-01'});if(error)throw new Error(dbError(error));await loadRemote();await syncSettlementsRemote()},'Đã đóng và khóa tháng');return}const reopen=e.target.closest('#reopenSelectedMonth');if(reopen){paymentAction(async()=>{if(!confirm(`Mở khóa tháng ${REPORT_MONTH}?`))return;const {error}=await neon.rpc('reopen_month',{p_month:REPORT_MONTH+'-01'});if(error)throw new Error(dbError(error));await loadRemote();await syncSettlementsRemote()},'Đã mở khóa tháng');return}const pay=e.target.closest('[data-settlement-pay]');if(pay){const id=+pay.dataset.settlementPay,due=+pay.dataset.due,currentPaid=+pay.dataset.paid||0,raw=prompt('Nhập tổng số tiền đã thanh toán cho đối soát này:',String(due)),amount=Number(raw);if(raw===null)return;if(!Number.isFinite(amount)||amount<0)return toast('Số tiền không hợp lệ',true);const reference=prompt('Mã giao dịch / ghi chú thanh toán (có thể để trống):','')||'';paymentAction(async()=>{const {error}=await neon.rpc('mark_settlement_paid',{p_settlement_id:id,p_paid_amount:amount,p_reference:reference||null});if(error)throw new Error(dbError(error));await syncSettlementsRemote()},amount>=due?'Đã ghi nhận thanh toán đủ':amount>currentPaid?'Đã cập nhật thanh toán':'Đã cập nhật đối soát');return}},true);
document.addEventListener('click',e=>{if(MODE!=='auth')return;const save=e.target.closest('#savePayment'),rec=e.target.closest('[data-pay-rec]'),del=e.target.closest('[data-del-payment]');if(!save&&!rec&&!del)return;e.preventDefault();e.stopImmediatePropagation();if(save){const bid=+$('pBooking').value,amount=+$('pAmount').value,date=$('pDate').value,method=$('pMethod').value,account=$('pAccount').value.trim(),reference=$('pRef').value.trim(),reconciled=$('pReconciled').checked;if(!bid||!date||amount<=0)return toast('Chọn booking, ngày thu và số tiền lớn hơn 0',true);paymentAction(async()=>{await insert('booking_payments',{booking_id:bid,payment_date:date,amount,method,receiving_account:account||null,reference:reference||null,reconciliation_status:reconciled?'reconciled':'pending',invoice_status:'undetermined',note:null});const rows=await safeQ('booking_payments');const paid=rows.filter(x=>String(x.booking_id)===String(bid)).reduce((n,x)=>n+(+x.amount||0),0);await update('bookings',bid,{amount_received:paid})},'Đã ghi nhận tiền thu');return}if(rec){const id=+rec.dataset.payRec,p=(D.payments||[]).find(x=>x.id===id);if(!p)return;paymentAction(async()=>{await update('booking_payments',id,{reconciliation_status:p.reconciled?'pending':'reconciled'})},p.reconciled?'Đã bỏ đối soát':'Đã xác nhận đối soát');return}if(del){const id=+del.dataset.delPayment,p=(D.payments||[]).find(x=>x.id===id);if(!p||!confirm('Xóa giao dịch thu này?'))return;paymentAction(async()=>{await remove('booking_payments',id);const rows=await safeQ('booking_payments');const paid=rows.filter(x=>String(x.booking_id)===String(p.bookingId)).reduce((n,x)=>n+(+x.amount||0),0);await update('bookings',p.bookingId,{amount_received:paid})},'Đã xóa giao dịch thu')}},true);
render();
