villaMonth=function(code,m=REPORT_MONTH){const bs=bookingsForMonth(m).filter(b=>b.villa===code),fs=financeForMonth(m).filter(f=>f.villa===code);let room=0,other=0,gross=0,recognizedRoom=0,ownerBase=0,soldNights=0;const {s,days}=reportBounds(m),start=new Date(s+'T00:00:00'),end=new Date(start.getFullYear(),start.getMonth()+1,1);for(const b of bs){const c=commercialForV10(b);const br=+b.room||0;room+=br;other+=+b.other||0;gross+=c.gross;recognizedRoom+=c.recognized==null?br:c.recognized;ownerBase+=c.ownerBase==null?br:c.ownerBase;const bi=new Date(b.in+'T00:00:00'),bo=new Date(b.out+'T00:00:00'),a=bi>start?bi:start,z=bo<end?bo:end;if(z>a)soldNights+=(z-a)/86400000}const expense=fs.filter(f=>f.dir==='out').reduce((n,f)=>n+(+f.amount||0),0),v=VILLAS.find(v=>v.code===code),owner=v?.model==='share'?ownerBase*(v.pct/100):0,rent=v?.model==='rent'?(+v.rent||0):0,recognized=recognizedRoom+other,profit=recognized-expense-owner-rent;return {room,other,gross,recognized,expense,owner,rent,profit,soldNights,occupancy:days?Math.min(100,soldNights/days*100):0,ownerBase}}
const monthChecklist_v10=monthChecklist;monthChecklist=function(m=REPORT_MONTH){const c=monthChecklist_v10(m),bs=bookingsForMonth(m);c.nhanMissing=bs.filter(b=>b.villa==='NHAN'&&(+b.room||0)>0&&commercialForV10(b).ownerBase==null);return c};
render();
