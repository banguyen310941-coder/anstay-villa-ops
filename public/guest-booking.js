const $=id=>document.getElementById(id);const fmt=n=>new Intl.NumberFormat('vi-VN').format(Math.round(Number(n)||0))+' ₫';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function ymd(d){return d.toLocaleDateString('en-CA')}const t=new Date(),a=new Date(t),b=new Date(t);a.setDate(t.getDate()+1);b.setDate(t.getDate()+3);$('cin').value=ymd(a);$('cout').value=ymd(b);$('cin').min=ymd(t);$('cout').min=ymd(a);
let selected=null,hold=null,timerId=null;
const reason={capacity_exceeded:'Vượt sức chứa',availability_block:'Đang khóa phòng',booked:'Đã có booking',temporarily_held:'Đang được khách khác giữ tạm',channel_mapping_not_configured:'Chưa mở bán trên website',rate_plan_not_found:'Chưa có rate plan',rate_missing:'Chưa có giá',rate_not_published_or_stop_sell:'Giá chưa phát hành / stop-sell',closed_to_arrival:'Không nhận check-in ngày này',minimum_stay_not_met:'Chưa đủ số đêm tối thiểu'};
function notice(text,bad=false){const n=$('notice');n.textContent=text;n.classList.remove('hidden');n.classList.toggle('bad',bad)}function clearNotice(){$('notice').classList.add('hidden')}
async function jsonFetch(url,opt){const r=await fetch(url,opt);const j=await r.json().catch(()=>({ok:false,error:'invalid_response'}));if(!r.ok)throw new Error(j.message||j.error||'Không xử lý được yêu cầu');return j}
function searchParams(){return {cin:$('cin').value,cout:$('cout').value,guests:+$('guests').value||1,extraAdults:+($('extraAdults')?.value||0),children612:+($('children612')?.value||0),childrenUnder6:+($('childrenUnder6')?.value||0)}}
async function search(){clearNotice();const p=searchParams();if(!p.cin||!p.cout||p.cout<=p.cin)return notice('Ngày check-out phải sau ngày check-in.',true);$('results').innerHTML='<div class="empty">Đang kiểm tra phòng và giá...</div>';$('resultCount').textContent='Đang tìm';try{const q=new URLSearchParams({check_in:p.cin,check_out:p.cout,guests:String(p.guests),channel:'WEBSITE',rate_plan:'BAR'}),data=await jsonFetch('/api/public/search?'+q);const rows=data.results||[];$('resultCount').textContent=`${data.sellable_count||0} villa có thể bán`;$('results').innerHTML=rows.map(r=>`<article class="villa ${r.sellable?'':'unavailable'}"><span class="${r.sellable?'status-ok':'status-no'}">${r.sellable?'Có thể bán':esc(reason[r.reason]||r.reason||'Chưa sẵn sàng')}</span><h3>${esc(r.villa_name||r.villa_code||'Villa')}</h3><div class="meta">${r.max_guests?`Tối đa ${r.max_guests} khách<br>`:''}${r.nights?`${r.nights} đêm · Website ANSTAY`:''}${r.message?`<br>${esc(r.message)}`:''}</div><div class="price">${r.sellable?fmt(r.room_amount):'—'}${r.sellable?'<small>Giá phòng trước phụ thu được xác nhận</small>':''}</div>${r.sellable?`<button class="primary" data-hold="${esc(r.villa_code)}">Giữ phòng 10 phút</button>`:''}</article>`).join('')||'<div class="empty">Chưa có dữ liệu villa.</div>';if(!(data.sellable_count>0))notice('Hiện chưa có villa nào đủ điều kiện bán online cho khoảng ngày này. Nếu giá đang ở Draft, ANSTAY sẽ chưa phát giá ra website.');}catch(e){$('results').innerHTML='<div class="empty">Không tải được kết quả.</div>';notice(e.message,true)}}
async function createHold(villa){const p=searchParams();clearNotice();try{const data=await jsonFetch('/api/public/hold',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({villa_code:villa,check_in:p.cin,check_out:p.cout,guests:p.guests,extra_adults:p.extraAdults,children_6_12:p.children612,children_under_6:p.childrenUnder6,channel:'WEBSITE',rate_plan:'BAR',hold_minutes:10})});if(!data.ok)throw new Error(data.error||'Không giữ được phòng');selected={villa,...p,quote:data.quote};hold=data;$('checkout').classList.remove('hidden');$('chosen').innerHTML=`<b>${esc(data.quote.villa_name)}</b> · ${p.cin} → ${p.cout} · ${p.guests} khách<br>Giá phòng: <b>${fmt(data.quote.room_amount)}</b>${data.quote.requires_manual_quote?'<br><span class="status-no">Phụ thu cần ANSTAY xác nhận thủ công</span>':`<br>Tạm tính: <b>${fmt(data.quote.total_amount)}</b>`}`;startTimer(new Date(data.expires_at));$('checkout').scrollIntoView({behavior:'smooth',block:'start'})}catch(e){notice(e.message,true);await search()}}
function startTimer(exp){clearInterval(timerId);const tick=()=>{const s=Math.max(0,Math.floor((exp-Date.now())/1000));$('timer').textContent=s?`Giữ phòng ${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`:'Hold đã hết hạn';if(!s){clearInterval(timerId);$('requestBtn').disabled=true}};tick();timerId=setInterval(tick,1000)}
async function submitRequest(){if(!hold?.hold_token)return notice('Hold không còn hợp lệ. Hãy tìm lại phòng.',true);const name=$('guestName').value.trim(),email=$('guestEmail').value.trim(),phone=$('guestPhone').value.trim(),note=$('guestNote').value.trim();if(!name||!email.includes('@'))return notice('Vui lòng nhập họ tên và email hợp lệ.',true);$('requestBtn').disabled=true;$('requestBtn').textContent='Đang gửi...';try{const data=await jsonFetch('/api/public/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({hold_token:hold.hold_token,guest_name:name,guest_email:email,guest_phone:phone,guest_note:note})});$('checkout').innerHTML=`<div class="notice"><b>ANSTAY đã nhận yêu cầu của bạn.</b><br>Mã yêu cầu: <b>${esc(data.request_code)}</b><br>Trạng thái: ${esc(data.status)}${data.total_amount!=null?` · Tạm tính ${fmt(data.total_amount)}`:''}<br><small>Yêu cầu này chưa phải booking xác nhận. ANSTAY sẽ kiểm tra phụ thu/tiền cọc và xác nhận chính thức.</small></div>`;$('statusCode').value=data.request_code;$('statusEmail').value=email;clearInterval(timerId)}catch(e){$('requestBtn').disabled=false;$('requestBtn').textContent='Gửi yêu cầu đặt villa';notice(e.message,true)}}
async function checkStatus(){const code=$('statusCode').value.trim(),email=$('statusEmail').value.trim();if(!code||!email.includes('@'))return;$('statusResult').innerHTML='<p class="hint">Đang kiểm tra...</p>';try{const q=new URLSearchParams({code,email}),d=await jsonFetch('/api/public/status?'+q);const label={pending_review:'Đang chờ ANSTAY duyệt',needs_manual_quote:'Đang chờ xác nhận phụ thu',confirmed:'Đã xác nhận booking',rejected:'Yêu cầu không được xác nhận',cancelled:'Đã hủy'}[d.status]||d.status;$('statusResult').innerHTML=`<div class="notice"><b>${esc(label)}</b><br>${esc(d.villa_name)} · ${d.check_in} → ${d.check_out}${d.total_amount!=null?`<br>Tổng giá: <b>${fmt(d.total_amount)}</b>`:''}${d.deposit_required!=null?`<br>Yêu cầu cọc: ${fmt(d.deposit_required)} · Đã ghi nhận ${fmt(d.deposit_paid)}`:''}${d.booking_code?`<br>Mã booking: <b>${esc(d.booking_code)}</b>`:''}</div>`}catch(e){$('statusResult').innerHTML=`<div class="notice bad">${esc(e.message)}</div>`}}
$('searchBtn').onclick=search;$('requestBtn').onclick=submitRequest;$('statusBtn').onclick=checkStatus;$('cin').onchange=()=>{$('cout').min=$('cin').value;if($('cout').value<=$('cin').value){const d=new Date($('cin').value+'T00:00:00');d.setDate(d.getDate()+2);$('cout').value=ymd(d)}};document.addEventListener('click',e=>{const b=e.target.closest('[data-hold]');if(b)createHold(b.dataset.hold)});