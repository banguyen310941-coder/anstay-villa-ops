/* main-0.txt */
import { createClient } from '@neondatabase/neon-js';

const AUTH_URL='https://ep-dawn-dawn-a5y0y7ou.neonauth.us-east-2.aws.neon.tech/anstay/auth';
const DATA_API_URL='https://ep-dawn-dawn-a5y0y7ou.apirest.us-east-2.aws.neon.tech/anstay/rest/v1';
const neon=createClient({auth:{url:AUTH_URL},dataApi:{url:DATA_API_URL}});

const FALLBACK_VILLAS=[
 {code:'NHAN',name:'Nhàn',model:'share',rule:'50/50 doanh thu bán phòng',rent:0,pct:50},
 {code:'SOL',name:'SOL',model:'rent',rule:'Thuê net 50M/tháng',rent:50000000,pct:0},
 {code:'SAM',name:'SAM',model:'owned',rule:'Sở hữu',rent:0,pct:0},
 {code:'GIO',name:'Gió',model:'owned',rule:'Sở hữu',rent:0,pct:0},
 {code:'NANG',name:'Nắng',model:'rent',rule:'Thuê net 25M/tháng',rent:25000000,pct:0},
 {code:'TIM',name:'Tim',model:'owned',rule:'Sở hữu',rent:0,pct:0}
];
let VILLAS=[...FALLBACK_VILLAS];
const NAV=[['dash','▦','Tổng quan','Doanh thu, booking, nhân sự và dòng tiền'],['crm','◎','CRM & Marketing','Lead và pipeline bán hàng'],['booking','▤','Booking','Lịch villa và chống bán trùng'],['ops','✓','Buồng phòng','Housekeeping và bảo trì'],['att','◷','Nhân sự & Điểm danh','Nhân viên, GPS, bảng công, điều chỉnh'],['stock','□','Kho & Mua hàng','Nhập xuất tồn và cảnh báo'],['fin','₫','Tài chính','Thu chi, công nợ và hóa đơn'],['reports','⌁','Báo cáo & Dự chi','P&L từng villa và nghĩa vụ tháng tới'],['settings','⚙','Thiết lập','Trạng thái hệ thống và sao lưu']];
const ACCESS={admin:NAV.map(x=>x[0]),sales:['dash','crm','booking'],ops:['dash','booking','ops','att','stock'],housekeeping:['dash','ops','att'],stock:['dash','stock'],accounting:['dash','fin','reports'],employee:['dash','att']};
const STORE='anstay-v06-demo';
const blank=()=>({leads:[],bookings:[],tasks:[],employees:[],attendance:[],adjustments:[],items:[],moves:[],finance:[],schedules:[],closures:[],accounts:[]});
let D=blank(),GPS=null,current='dash',MODE='locked',ROLE='employee',SESSION=null,CAL_MONTH=new Date(new Date().getFullYear(),new Date().getMonth(),1);
const $=id=>document.getElementById(id);
const loadLocal=()=>{try{return Object.assign(blank(),JSON.parse(localStorage.getItem(STORE)||'{}'))}catch{return blank()}};
function saveLocal(){localStorage.setItem(STORE,JSON.stringify(D));render()}
function fmt(n){return new Intl.NumberFormat('vi-VN').format(Math.round(Number(n)||0))+' ₫'}
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function day(d=new Date()){return d.toLocaleDateString('en-CA')}
function mon(){const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')}
function inMon(s){return s&&String(s).slice(0,7)===mon()}
function toast(t,bad=false){const e=$('toast');e.textContent=t;e.classList.toggle('bad',bad);e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2400)}
function badge(v){return v.model==='owned'?'<span class="tag ok">Sở hữu</span>':v.model==='rent'?'<span class="tag warning">Thuê</span>':'<span class="tag info">50/50</span>'}
function vname(c){return VILLAS.find(v=>String(v.code)===String(c))?.name||c||'—'}
function vid(c){return VILLAS.find(v=>String(v.code)===String(c))?.id||null}
function vcode(id){return VILLAS.find(v=>String(v.id)===String(id))?.code||id}
function vopts(extra=''){return VILLAS.map(v=>`<option value="${v.code}">${esc(v.name)}</option>`).join('')+extra}
function status(s){return {new:'Mới',contacted:'Đã liên hệ',quoted:'Đã báo giá',deposit:'Đã cọc',won:'Thắng',lost:'Thua',hold:'Giữ chỗ',confirmed:'Đã xác nhận',staying:'Đang ở',checked_out:'Đã check-out',cancelled:'Hủy',open:'Mở',completed:'Hoàn tất',pending:'Chờ duyệt',approved:'Đã duyệt',rejected:'Từ chối'}[s]||s}
function roleLabel(r){return {admin:'Quản trị',sales:'Sales',ops:'Vận hành',housekeeping:'Housekeeping',stock:'Kho',accounting:'Kế toán',employee:'Nhân viên'}[r]||r}
function dbError(error){if(!error)return null;return error.message||error.details||String(error)}
async function q(table,select='*') {const {data,error}=await neon.from(table).select(select);if(error)throw new Error(dbError(error));return data||[]}
async function safeQ(table,select='*'){try{return await q(table,select)}catch{return []}}
async function insert(table,row){const {data,error}=await neon.from(table).insert(row).select();if(error)throw new Error(dbError(error));return data?.[0]}
async function remove(table,id){const {error}=await neon.from(table).delete().eq('id',id);if(error)throw new Error(dbError(error))}
async function update(table,id,row){const {error}=await neon.from(table).update(row).eq('id',id);if(error)throw new Error(dbError(error))}

async function loadRemote(){
 const [vs,profiles,customers,leads,bookings,tasks,employees,attendance,adjustments,items,moves,finance,schedules,closures]=await Promise.all([
   q('villas'),safeQ('app_user_profiles'),safeQ('customers'),safeQ('leads'),safeQ('bookings'),safeQ('housekeeping_tasks'),safeQ('employees'),safeQ('attendance'),safeQ('attendance_adjustments'),safeQ('inventory_items'),safeQ('stock_movements'),safeQ('finance_transactions'),safeQ('employee_shift_assignments'),safeQ('monthly_closures')
 ]);
 VILLAS=vs.map(v=>({id:v.id,code:v.code,name:v.name,model:v.ownership_model==='revenue_share'?'share':v.ownership_model==='fixed_rent'?'rent':'owned',rule:v.ownership_model==='revenue_share'?`${Number(v.revenue_share_owner_pct)}% doanh thu bán phòng`:v.ownership_model==='fixed_rent'?`Thuê net ${Number(v.monthly_rent)/1e6}M/tháng`:'Sở hữu',rent:+v.monthly_rent||0,pct:+v.revenue_share_owner_pct||0,address:v.address||'',lat:v.latitude==null?null:+v.latitude,lng:v.longitude==null?null:+v.longitude,radius:+v.attendance_radius_m||200,gpsRequired:!!v.gps_required}));
 if(!VILLAS.length)VILLAS=[...FALLBACK_VILLAS];
 const cm=new Map(customers.map(c=>[String(c.id),c]));
 const myProfile=profiles.find(p=>String(p.auth_user_id)===String(SESSION?.user?.id))||profiles[0]; if(!myProfile)throw new Error('Tài khoản chưa được quản trị liên kết với hồ sơ nhân viên'); ROLE=myProfile.app_role||'employee';
 D.leads=leads.map(x=>({id:+x.id,customerId:x.customer_id,name:cm.get(String(x.customer_id))?.full_name||'—',phone:cm.get(String(x.customer_id))?.phone||'',source:x.source,campaign:x.campaign||'',status:x.status,employeeId:x.assigned_employee_id,value:+x.expected_value||0}));
 D.bookings=bookings.map(x=>({id:+x.id,code:x.booking_code,guest:cm.get(String(x.customer_id))?.full_name||x.notes||'Khách',customerId:x.customer_id,villa:vcode(x.villa_id),source:x.source,in:x.check_in_date,out:x.check_out_date,status:x.status,guests:+x.guests||1,room:+x.room_revenue||0,other:+x.other_revenue||0,received:+x.amount_received||0,invoice:x.invoice_status}));
 D.employees=employees.map(x=>({id:+x.id,code:x.employee_code,name:x.full_name,role:x.role,phone:x.phone||'',active:x.active}));
 D.tasks=tasks.map(x=>({id:+x.id,villa:vcode(x.villa_id),bookingId:x.booking_id,type:x.task_type,employeeId:x.assigned_employee_id,owner:D.employees.find(e=>String(e.id)===String(x.assigned_employee_id))?.name||'',due:x.due_at||'',note:x.notes||'',done:x.status==='completed',status:x.status}));
 const shiftRows=await safeQ('shifts'); const sm=new Map(shiftRows.map(s=>[String(s.id),s]));
 D.attendance=attendance.map(x=>({id:+x.id,employee:+x.employee_id,villa:vcode(x.villa_id),shift:sm.get(String(x.shift_id))?.code?.toLowerCase()||'flex',shiftId:x.shift_id,in:x.check_in_at,out:x.check_out_at,gps:x.check_in_lat!=null?{lat:+x.check_in_lat,lng:+x.check_in_lng}:null,distance:x.check_in_distance_m==null?null:+x.check_in_distance_m,gpsVerified:!!x.gps_verified,date:x.work_date,status:x.status}));
 D.adjustments=adjustments.map(x=>({id:+x.id,employee:+x.employee_id,date:x.work_date,reason:x.reason,status:x.status,in:x.requested_check_in,out:x.requested_check_out}));
 D.items=items.map(x=>({id:+x.id,sku:x.sku,name:x.name,unit:x.unit,min:+x.min_stock||0,cost:+x.average_cost||0}));
 D.moves=moves.map(x=>({id:+x.id,item:+x.item_id,villa:vcode(x.villa_id),type:x.movement_type,qty:+x.quantity||0,cost:+x.unit_cost||0,at:x.movement_at,note:x.note||''}));
 D.finance=finance.map(x=>({id:+x.id,date:x.transaction_date,villa:vcode(x.villa_id),dir:x.direction,cat:x.category,amount:+x.amount||0,source:x.source||'',paid:x.paid,due:x.due_date,invoice:x.invoice_status,account:x.account_name||''}));
 D.schedules=schedules.map(x=>({id:+x.id,employee:+x.employee_id,villa:vcode(x.villa_id),shiftId:x.shift_id,shift:sm.get(String(x.shift_id))?.name||'Linh hoạt',date:x.work_date,status:x.status,note:x.note||''}));
 D.closures=closures.map(x=>({month:x.month_start,status:x.status,summary:x.summary||{},closedAt:x.closed_at}));
 D.accounts=[]; if(ROLE==='admin'){const r=await neon.rpc('admin_list_employee_accounts'); if(!r.error)D.accounts=r.data||[];}
 applyAccess(); render();
}
async function refresh(){if(MODE==='auth')await loadRemote();else render()}

function calcVilla(code){const bs=D.bookings.filter(b=>b.villa===code&&inMon(b.in)&&b.status!=='cancelled'),fs=D.finance.filter(f=>f.villa===code&&inMon(f.date));const room=bs.reduce((n,b)=>n+(+b.room||0),0),other=bs.reduce((n,b)=>n+(+b.other||0),0),expense=fs.filter(f=>f.dir==='out').reduce((n,f)=>n+(+f.amount||0),0),v=VILLAS.find(v=>v.code===code),owner=v?.model==='share'?room*(v.pct/100):v?.rent||0;return {room,other,expense,owner,profit:room+other-expense-owner}}
function overlaps(v,cin,cout){return D.bookings.some(b=>b.villa===v&&['confirmed','staying'].includes(b.status)&&cin<b.out&&cout>b.in)}
function late(a){if(!a.in||a.shift==='flex')return false;const d=new Date(a.in),m=d.getHours()*60+d.getMinutes(),s={morning:420,afternoon:900,office:480}[a.shift];return s!=null&&m>s+5}
function hours(a){return a.in&&a.out?Math.max(0,(new Date(a.out)-new Date(a.in))/36e5):0}
function forecast(){const n=new Date(),s=new Date(n.getFullYear(),n.getMonth()+1,1).toLocaleDateString('en-CA'),e=new Date(n.getFullYear(),n.getMonth()+2,0).toLocaleDateString('en-CA'),ap=D.finance.filter(f=>f.dir==='out'&&!f.paid&&f.due&&f.due>=s&&f.due<=e).reduce((x,f)=>x+(+f.amount||0),0),fixed=VILLAS.reduce((n,v)=>n+(v.model==='rent'?v.rent:0),0);return {fixed,ap,total:fixed+ap}}
function cashInMonth(){return D.finance.filter(f=>f.dir==='in'&&inMon(f.date)).reduce((n,f)=>n+(+f.amount||0),0)}
function receivableMonth(){return D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled').reduce((n,b)=>n+Math.max(0,(+b.room||0)+(+b.other||0)-(+b.received||0)),0)}
function nightsInMonth(){const y=CAL_MONTH.getFullYear(),m=CAL_MONTH.getMonth(),start=new Date(y,m,1),end=new Date(y,m+1,1);let sold=0;for(const b of D.bookings){if(b.status==='cancelled')continue;const bi=new Date(b.in+'T00:00:00'),bo=new Date(b.out+'T00:00:00'),a=bi>start?bi:start,z=bo<end?bo:end;if(z>a)sold+=(z-a)/86400000}return sold}
function calKey(d){return d.toLocaleDateString('en-CA')}
function distanceText(a){return a?.distance!=null?Math.round(a.distance)+' m':'—'}

function applyAccess(){const allowed=MODE==='demo'?NAV.map(x=>x[0]):(ACCESS[ROLE]||ACCESS.employee);if(!allowed.includes(current))current=allowed[0]||'dash';renderNav()}
function renderNav(){const allowed=MODE==='demo'?NAV.map(x=>x[0]):(ACCESS[ROLE]||ACCESS.employee);$('nav').innerHTML=NAV.filter(x=>allowed.includes(x[0])).map(([k,i,n])=>`<button data-k="${k}" class="${k===current?'active':''}">${i} ${n}</button>`).join('');$('nav').onclick=e=>{const b=e.target.closest('button[data-k]');if(!b)return;current=b.dataset.k;const m=NAV.find(x=>x[0]===current);$('pageTitle').textContent=m[2];$('pageSub').textContent=m[3];$('sidebar').classList.remove('open');renderNav();render()}}

/* main-1.txt */
function dash(){const c=VILLAS.map(v=>[v,calcVilla(v.code)]),rev=c.reduce((n,x)=>n+x[1].room+x[1].other,0),bk=D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled'),att=D.attendance.filter(a=>a.date===day()),f=forecast(),today=day(),arr=D.bookings.filter(b=>b.in===today&&b.status!=='cancelled'),dep=D.bookings.filter(b=>b.out===today&&b.status!=='cancelled'),cash=cashInMonth(),ar=receivableMonth();return `<div class="view"><div class="note"><b>Dữ liệu:</b> ${MODE==='auth'?'Neon Postgres thật · RLS đang bật':'Bản demo cục bộ'} · Doanh thu P&L lấy từ booking; sổ thu tiền theo dõi riêng để tránh cộng trùng.</div><div class="grid kpis"><div class="kpi"><span>Doanh thu tháng</span><b>${fmt(rev)}</b></div><div class="kpi"><span>Tiền thu tháng</span><b>${fmt(cash)}</b></div><div class="kpi"><span>Phải thu booking</span><b>${fmt(ar)}</b></div><div class="kpi"><span>Dự chi tháng tới</span><b>${fmt(f.total)}</b></div></div><div class="grid two"><div class="card"><h3>Tình hình 6 villa</h3><div class="scroll"><table><tr><th>Villa</th><th>Mô hình</th><th>DT</th><th>Thuê/chia chủ</th><th>LN đóng góp</th></tr>${c.map(([v,x])=>`<tr><td><b>${esc(v.name)}</b></td><td>${badge(v)}</td><td>${fmt(x.room+x.other)}</td><td>${fmt(x.owner)}</td><td><b>${fmt(x.profit)}</b></td></tr>`).join('')}</table></div></div><div class="card"><h3>Hôm nay</h3><p><b>${arr.length}</b> khách đến · <b>${dep.length}</b> khách đi</p><p><b>${att.filter(x=>!x.out).length}</b> nhân viên đang trong ca</p><p class="muted">${D.adjustments.filter(x=>x.status==='pending').length} phiếu công chờ duyệt · ${D.finance.filter(x=>x.dir==='out'&&!x.paid&&x.due&&x.due<today).length} khoản chi quá hạn.</p><p class="muted">Vai trò: <b>${roleLabel(ROLE)}</b></p></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Đến / đi hôm nay</h3>${arr.concat(dep).length?`<div class="scroll"><table><tr><th>Loại</th><th>Villa</th><th>Khách</th><th>Mã</th></tr>${arr.map(b=>`<tr><td><span class="tag info">Check-in</span></td><td>${vname(b.villa)}</td><td>${esc(b.guest)}</td><td>${esc(b.code)}</td></tr>`).join('')}${dep.map(b=>`<tr><td><span class="tag warning">Check-out</span></td><td>${vname(b.villa)}</td><td>${esc(b.guest)}</td><td>${esc(b.code)}</td></tr>`).join('')}</table></div>`:'<p class="muted">Không có lượt đến/đi hôm nay.</p>'}</div><div class="card"><h3>Kiểm soát tiền</h3><p>Thuê cố định tháng: <b>${fmt(f.fixed)}</b></p><p>Booking tháng: <b>${bk.length}</b></p><p class="muted">P&L hiện ghi nhận theo tháng check-in. Khi có quy tắc kế toán chính thức, hệ thống sẽ tách doanh thu ghi nhận theo đêm/lưu trú.</p></div></div></div>`}
function crm(){const s=k=>D.leads.filter(x=>x.status===k).length;return `<div class="view"><div class="grid kpis"><div class="kpi"><span>Lead mới</span><b>${s('new')}</b></div><div class="kpi"><span>Đang chăm sóc</span><b>${s('contacted')+s('quoted')}</b></div><div class="kpi"><span>Đã cọc</span><b>${s('deposit')}</b></div><div class="kpi"><span>Thắng</span><b>${s('won')}</b></div></div><div class="grid two"><div class="card"><h3>Thêm lead</h3><div class="form"><label>Khách<input id="lName"></label><label>Điện thoại<input id="lPhone"></label><label>Nguồn<select id="lSource"><option>Facebook</option><option>Zalo</option><option>Website</option><option>Khách cũ</option><option>Đại lý</option></select></label><label>Giá trị dự kiến<input id="lValue" type="number"></label><label>Trạng thái<select id="lStatus"><option value="new">Mới</option><option value="contacted">Đã liên hệ</option><option value="quoted">Đã báo giá</option><option value="deposit">Đã cọc</option><option value="won">Thắng</option><option value="lost">Thua</option></select></label><label>Chiến dịch<input id="lCampaign"></label></div><button class="primary" id="addLead">Lưu lead</button></div><div class="card"><h3>Pipeline</h3>${[['new','Mới'],['contacted','Liên hệ'],['quoted','Báo giá'],['deposit','Đã cọc'],['won','Thắng']].map(x=>`<p>${x[1]} · <b>${s(x[0])}</b></p><div class="bar"><i style="width:${Math.min(100,s(x[0])*20)}%"></i></div>`).join('')}</div></div><div class="card" style="margin-top:14px"><h3>Danh sách lead</h3><div class="scroll"><table><tr><th>Khách</th><th>Nguồn</th><th>Trạng thái</th><th>Giá trị</th><th>Chiến dịch</th><th></th></tr>${D.leads.length?D.leads.slice().reverse().map(x=>`<tr><td><b>${esc(x.name)}</b><br>${esc(x.phone)}</td><td>${esc(x.source)}</td><td>${status(x.status)}</td><td>${fmt(x.value)}</td><td>${esc(x.campaign)}</td><td><button class="danger-btn" data-del-lead="${x.id}">Xóa</button></td></tr>`).join(''):'<tr><td colspan="6" class="empty">Chưa có lead</td></tr>'}</table></div></div></div>`}
function booking(){const cm=CAL_MONTH.toLocaleDateString('vi-VN',{month:'2-digit',year:'numeric'}),sold=nightsInMonth(),cap=VILLAS.length*new Date(CAL_MONTH.getFullYear(),CAL_MONTH.getMonth()+1,0).getDate(),occ=cap?Math.round(sold/cap*100):0;return `<div class="view"><div class="grid kpis"><div class="kpi"><span>Tháng đang xem</span><b>${cm}</b></div><div class="kpi"><span>Đêm đã bán</span><b>${sold}</b></div><div class="kpi"><span>Công suất villa</span><b>${occ}%</b></div><div class="kpi"><span>Booking active</span><b>${D.bookings.filter(b=>b.status!=='cancelled').length}</b></div></div><div class="grid two"><div class="card"><h3>Tạo booking</h3><div class="form"><label>Mã booking<input id="bCode" placeholder="BK-0001"></label><label>Khách<input id="bGuest"></label><label>Villa<select id="bVilla">${vopts()}</select></label><label>Nguồn<select id="bSource"><option>Direct</option><option>Booking.com</option><option>Agoda</option><option>Airbnb</option><option>Traveloka</option></select></label><label>Check-in<input id="bIn" type="date"></label><label>Check-out<input id="bOut" type="date"></label><label>Trạng thái<select id="bStatus"><option value="hold">Giữ chỗ</option><option value="confirmed">Đã xác nhận</option><option value="staying">Đang ở</option><option value="checked_out">Đã check-out</option><option value="cancelled">Hủy</option></select></label><label>Số khách<input id="bGuests" type="number" value="2"></label><label>DT phòng<input id="bRoom" type="number" value="0"></label><label>DT khác<input id="bOther" type="number" value="0"></label><label>Đã thu<input id="bReceived" type="number" value="0"></label><label>Hóa đơn<select id="bInvoice"><option value="undetermined">Chưa xác định</option><option value="pending">Chờ xuất</option><option value="issued">Đã xuất</option></select></label></div><button class="primary" id="addBooking">Tạo booking</button><p class="muted" id="bookMsg"></p></div><div class="card"><h3>Quy tắc</h3><p class="muted">Chặn trùng lịch ở PostgreSQL.</p><p class="muted">Booking xác nhận tự sinh housekeeping.</p><p class="muted">Tháng đã đóng sẽ khóa sửa/xóa booking của tháng đó.</p></div></div><div class="card" style="margin-top:14px"><div class="row" style="justify-content:space-between;align-items:center"><h3>Lịch booking theo tháng</h3><div class="row"><button id="calPrev">← Tháng trước</button><button id="calToday">Tháng này</button><button id="calNext">Tháng sau →</button></div></div><div class="scroll"><div id="calendar" class="calendar"></div></div></div><div class="card" style="margin-top:14px"><h3>Booking</h3><div class="scroll"><table><tr><th>Mã</th><th>Villa</th><th>Khách</th><th>Ngày</th><th>Nguồn</th><th>DT phòng</th><th>DT khác</th><th>Đã thu</th><th>Còn thu</th><th></th></tr>${D.bookings.length?D.bookings.slice().reverse().map(x=>`<tr><td><b>${esc(x.code)}</b><br>${status(x.status)}</td><td>${vname(x.villa)}</td><td>${esc(x.guest)}</td><td>${x.in} → ${x.out}</td><td>${esc(x.source)}</td><td>${fmt(x.room)}</td><td>${fmt(x.other)}</td><td>${fmt(x.received)}</td><td>${fmt(Math.max(0,x.room+x.other-x.received))}</td><td><button class="danger-btn" data-del-booking="${x.id}">Xóa</button></td></tr>`).join(''):'<tr><td colspan="10" class="empty">Chưa có booking</td></tr>'}</table></div></div></div>`}
function ops(){return `<div class="view"><div class="grid two"><div class="card"><h3>Tạo việc vận hành</h3><div class="form"><label>Villa<select id="tVilla">${vopts()}</select></label><label>Loại<select id="tType"><option value="pre_checkin">Dọn trước check-in</option><option value="post_checkout">Dọn sau check-out</option><option value="minibar">Kiểm minibar</option><option value="maintenance">Bảo trì</option><option value="incident">Sự cố</option></select></label><label>Phụ trách<select id="tEmployee"><option value="">Chưa phân công</option>${D.employees.map(e=>`<option value="${e.id}">${esc(e.code)} · ${esc(e.name)}</option>`).join('')}</select></label><label>Hạn<input id="tDue" type="datetime-local"></label><label class="full">Ghi chú<input id="tNote"></label></div><button class="primary" id="addTask">Tạo việc</button></div><div class="card"><h3>Housekeeping tự động</h3><p class="muted">Booking xác nhận từ database sẽ tạo 2 nhiệm vụ. Khi booking hủy, nhiệm vụ liên quan được đồng bộ trạng thái.</p></div></div><div class="card" style="margin-top:14px"><div class="scroll"><table><tr><th>Villa</th><th>Việc</th><th>Phụ trách</th><th>Hạn</th><th>TT</th><th></th></tr>${D.tasks.length?D.tasks.slice().reverse().map(x=>`<tr><td>${vname(x.villa)}</td><td><b>${esc(x.type)}</b><br>${esc(x.note)}</td><td>${esc(x.owner||'—')}</td><td>${x.due?new Date(x.due).toLocaleString('vi-VN'):'—'}</td><td><span class="tag ${x.done?'ok':'warning'}">${x.done?'Xong':'Mở'}</span></td><td><button data-task="${x.id}">${x.done?'Mở lại':'Hoàn tất'}</button></td></tr>`).join(''):'<tr><td colspan="6" class="empty">Chưa có việc</td></tr>'}</table></div></div></div>`}

/* main-2.txt */
function att(){const today=D.attendance.filter(a=>a.date===day()),month=D.attendance.filter(a=>inMon(a.date)),empopts=D.employees.map(e=>`<option value="${e.id}">${esc(e.code)} · ${esc(e.name)}</option>`).join(''),todaySchedules=D.schedules.filter(s=>s.date===day());return `<div class="view"><div class="note"><b>Điểm danh production:</b> giờ máy chủ + GPS + kiểm tra lịch phân ca. Villa bật “Bắt buộc GPS” sẽ chặn check-in ngoài bán kính.</div><div class="grid kpis"><div class="kpi"><span>Check-in hôm nay</span><b>${today.length}</b></div><div class="kpi"><span>Đang trong ca</span><b>${today.filter(x=>!x.out).length}</b></div><div class="kpi"><span>Đi trễ</span><b>${today.filter(late).length}</b></div><div class="kpi"><span>Ca đã xếp hôm nay</span><b>${todaySchedules.length}</b></div></div><div class="grid two"><div class="card"><h3>Chấm công của tôi</h3><div class="form"><label>Villa<select id="aVilla">${vopts()}</select></label><label>Ca<select id="aShift"><option value="1">Sáng 07:00–15:00</option><option value="2">Chiều 15:00–23:00</option><option value="3">Hành chính 08:00–17:00</option><option value="4">Linh hoạt</option></select></label></div><div class="actions"><button id="gpsBtn">Lấy GPS</button><button class="primary" id="checkInBtn">Check-in</button><button id="checkOutBtn">Check-out</button></div><p class="muted" id="attMsg">Chưa lấy GPS.</p></div><div class="card"><h3>Lịch ca hôm nay</h3>${todaySchedules.length?`<div class="scroll"><table><tr><th>NV</th><th>Villa</th><th>Ca</th><th>Ghi chú</th></tr>${todaySchedules.map(x=>{const e=D.employees.find(y=>y.id===x.employee);return `<tr><td>${esc(e?.name||'—')}</td><td>${vname(x.villa)}</td><td>${esc(x.shift)}</td><td>${esc(x.note)}</td></tr>`}).join('')}</table></div>`:'<p class="muted">Chưa có ca được xếp hôm nay.</p>'}</div></div>${ROLE==='admin'||ROLE==='ops'||MODE==='demo'?`<div class="grid two" style="margin-top:14px"><div class="card"><h3>Nhân viên</h3><div class="form"><label>Mã NV<input id="eCode"></label><label>Họ tên<input id="eName"></label><label>Vai trò<select id="eRole"><option value="housekeeping">Housekeeping</option><option value="ops">Vận hành</option><option value="sales">Sales</option><option value="accounting">Kế toán</option><option value="stock">Kho</option><option value="employee">Nhân viên</option></select></label><label>Điện thoại<input id="ePhone"></label></div><button class="primary" id="addEmployee">Thêm nhân viên</button><div class="scroll" style="margin-top:12px"><table><tr><th>Mã</th><th>Tên</th><th>Vai trò</th></tr>${D.employees.map(e=>`<tr><td>${esc(e.code)}</td><td><b>${esc(e.name)}</b></td><td>${esc(e.role)}</td></tr>`).join('')||'<tr><td colspan="3" class="empty">Chưa có dữ liệu</td></tr>'}</table></div></div><div class="card"><h3>Xếp ca</h3><div class="form"><label>Nhân viên<select id="schEmployee">${empopts}</select></label><label>Ngày<input id="schDate" type="date"></label><label>Villa<select id="schVilla">${vopts()}</select></label><label>Ca<select id="schShift"><option value="1">Sáng</option><option value="2">Chiều</option><option value="3">Hành chính</option><option value="4">Linh hoạt</option></select></label><label class="full">Ghi chú<input id="schNote"></label></div><button class="primary" id="addSchedule">Xếp ca</button><div class="scroll" style="margin-top:12px"><table><tr><th>Ngày</th><th>NV</th><th>Villa</th><th>Ca</th><th></th></tr>${D.schedules.slice().sort((a,b)=>a.date.localeCompare(b.date)).map(x=>{const e=D.employees.find(y=>y.id===x.employee);return `<tr><td>${x.date}</td><td>${esc(e?.name||'—')}</td><td>${vname(x.villa)}</td><td>${esc(x.shift)}</td><td><button class="danger-btn" data-del-sch="${x.id}">Xóa</button></td></tr>`}).join('')||'<tr><td colspan="5" class="empty">Chưa xếp ca</td></tr>'}</table></div></div></div>`:''}<div class="card" style="margin-top:14px"><h3>Nhật ký hôm nay</h3><div class="scroll"><table><tr><th>Nhân viên</th><th>Villa</th><th>Ca</th><th>Vào</th><th>Ra</th><th>Khoảng cách</th><th>GPS</th><th>TT</th></tr>${today.length?today.slice().reverse().map(a=>{const e=D.employees.find(x=>x.id===a.employee);return `<tr><td><b>${esc(e?.name||'—')}</b></td><td>${vname(a.villa)}</td><td>${a.shift}</td><td>${a.in?new Date(a.in).toLocaleTimeString('vi-VN',{hour:'2-digit',minute:'2-digit'}):'—'}</td><td>${a.out?new Date(a.out).toLocaleTimeString('vi-VN',{hour:'2-digit',minute:'2-digit'}):'—'}</td><td>${distanceText(a)}</td><td>${a.gpsVerified?'<span class="tag ok">Hợp lệ</span>':a.gps?'<span class="tag warning">Đã ghi</span>':'—'}</td><td><span class="tag ${a.out?'ok':'info'}">${a.out?'Xong':'Đang làm'}${late(a)?' · trễ':''}</span></td></tr>`}).join(''):'<tr><td colspan="8" class="empty">Chưa có chấm công hôm nay</td></tr>'}</table></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Bảng công tháng</h3><div class="scroll"><table><tr><th>Nhân viên</th><th>Ngày công</th><th>Ca xong</th><th>Đi trễ</th><th>Giờ thực tế</th></tr>${D.employees.length?D.employees.map(e=>{const a=month.filter(x=>x.employee===e.id);return `<tr><td><b>${esc(e.code)} · ${esc(e.name)}</b></td><td>${new Set(a.map(x=>x.date)).size}</td><td>${a.filter(x=>x.out).length}</td><td>${a.filter(late).length}</td><td>${a.reduce((n,x)=>n+hours(x),0).toFixed(1)}h</td></tr>`}).join(''):'<tr><td colspan="5" class="empty">Chưa có nhân viên</td></tr>'}</table></div></div><div class="card"><h3>Yêu cầu điều chỉnh công</h3><div class="form"><label>Ngày<input id="adjDate" type="date"></label><label>Vào mong muốn<input id="adjIn" type="datetime-local"></label><label>Ra mong muốn<input id="adjOut" type="datetime-local"></label><label>Lý do<input id="adjReason" placeholder="Quên chấm công / sai ca..."></label></div><button class="primary" id="addAdj">Gửi phiếu</button><div class="scroll" style="margin-top:12px"><table><tr><th>Ngày</th><th>NV</th><th>Lý do</th><th>TT</th>${ROLE==='admin'||ROLE==='ops'||MODE==='demo'?'<th></th>':''}</tr>${D.adjustments.length?D.adjustments.slice().reverse().map(x=>{const e=D.employees.find(z=>z.id===x.employee);return `<tr><td>${x.date}</td><td>${esc(e?.name||'—')}</td><td>${esc(x.reason)}</td><td><span class="tag ${x.status==='approved'?'ok':x.status==='rejected'?'bad':'warning'}">${status(x.status)}</span></td>${ROLE==='admin'||ROLE==='ops'||MODE==='demo'?`<td>${x.status==='pending'?`<button data-adj-ok="${x.id}">Duyệt</button> <button class="danger-btn" data-adj-no="${x.id}">Từ chối</button>`:'—'}</td>`:''}</tr>`}).join(''):'<tr><td colspan="5" class="empty">Chưa có phiếu</td></tr>'}</table></div></div></div>${ROLE==='admin'&&MODE==='auth'?`<div class="card" style="margin-top:14px"><h3>Liên kết tài khoản đăng nhập</h3><p class="muted">Tài khoản phải tồn tại trong Neon Auth. Nhập email rồi gán nhân viên + quyền ứng dụng.</p><div class="form"><label>Email tài khoản<input id="linkEmail" type="email"></label><label>Nhân viên<select id="linkEmployee">${empopts}</select></label><label>Quyền<select id="linkRole"><option value="employee">Nhân viên</option><option value="housekeeping">Housekeeping</option><option value="sales">Sales</option><option value="ops">Vận hành</option><option value="stock">Kho</option><option value="accounting">Kế toán</option><option value="admin">Quản trị</option></select></label></div><button class="primary" id="linkAccount">Liên kết tài khoản</button><div class="scroll" style="margin-top:12px"><table><tr><th>Email</th><th>Tài khoản</th><th>Nhân viên</th><th>Quyền</th><th>TT</th></tr>${D.accounts.map(a=>`<tr><td>${esc(a.email)}</td><td>${esc(a.display_name||'—')}</td><td>${esc(a.employee_code||'—')} · ${esc(a.employee_name||'Chưa liên kết')}</td><td>${esc(a.app_role||'—')}</td><td>${a.active?'<span class="tag ok">Active</span>':'<span class="tag warning">Chưa gán</span>'}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">Chưa có tài khoản</td></tr>'}</table></div></div>`:''}</div>`}
function stock(){const qnty=i=>D.moves.filter(m=>m.item===i).reduce((n,m)=>n+(['out','issue'].includes(m.type)?-m.qty:m.qty),0);return `<div class="view"><div class="grid two"><div class="card"><h3>Vật tư</h3><div class="form"><label>SKU<input id="sSku"></label><label>Tên<input id="sName"></label><label>ĐVT<input id="sUnit"></label><label>Tồn tối thiểu<input id="sMin" type="number" value="0"></label><label>Giá vốn<input id="sCost" type="number" value="0"></label></div><button class="primary" id="addItem">Thêm vật tư</button></div><div class="card"><h3>Nhập / xuất</h3><div class="form"><label>Vật tư<select id="mItem">${D.items.map(i=>`<option value="${i.id}">${esc(i.sku)} · ${esc(i.name)}</option>`).join('')}</select></label><label>Villa<select id="mVilla">${vopts()}</select></label><label>Loại<select id="mType"><option value="in">Nhập</option><option value="out">Xuất</option></select></label><label>Số lượng<input id="mQty" type="number" value="1"></label></div><button class="primary" id="addMove">Ghi nhận</button></div></div><div class="card" style="margin-top:14px"><div class="scroll"><table><tr><th>SKU</th><th>Vật tư</th><th>Tồn</th><th>Tối thiểu</th><th>Giá vốn</th><th>Cảnh báo</th></tr>${D.items.length?D.items.map(i=>`<tr><td>${esc(i.sku)}</td><td><b>${esc(i.name)}</b></td><td>${qnty(i.id)} ${esc(i.unit)}</td><td>${i.min}</td><td>${fmt(i.cost)}</td><td>${qnty(i.id)<i.min?'<span class="tag bad">Cần mua</span>':'<span class="tag ok">Đủ</span>'}</td></tr>`).join(''):'<tr><td colspan="6" class="empty">Chưa có vật tư</td></tr>'}</table></div></div></div>`}
function fin(){return `<div class="view"><div class="grid two"><div class="card"><h3>Ghi nhận thu / chi</h3><div class="form"><label>Ngày<input id="fDate" type="date"></label><label>Villa<select id="fVilla">${vopts()}</select></label><label>Loại<select id="fDir"><option value="in">Thu</option><option value="out">Chi</option></select></label><label>Nhóm<select id="fCat"><option value="room">Doanh thu phòng</option><option value="other">Doanh thu khác</option><option value="ota">Hoa hồng OTA</option><option value="utilities">Điện nước</option><option value="laundry">Giặt là</option><option value="maintenance">Bảo trì</option><option value="salary">Lương</option><option value="supplier">NCC</option><option value="tax">Thuế/phí</option><option value="other_expense">Chi khác</option></select></label><label>Số tiền<input id="fAmount" type="number" value="0"></label><label>Nguồn<input id="fSource" placeholder="Direct / Agoda..."></label><label>Đã thanh toán<select id="fPaid"><option value="yes">Có</option><option value="no">Chưa</option></select></label><label>Hạn thanh toán<input id="fDue" type="date"></label></div><button class="primary" id="addFin">Lưu giao dịch</button></div><div class="card"><h3>Kiểm soát</h3><p class="muted">Mọi giao dịch gắn villa để lên P&L từng căn.</p><p class="muted">Khoản chi chưa trả có hạn tháng tới tự vào dự chi.</p></div></div><div class="card" style="margin-top:14px"><div class="scroll"><table><tr><th>Ngày</th><th>Villa</th><th>Thu/Chi</th><th>Nhóm</th><th>Số tiền</th><th>TT</th><th></th></tr>${D.finance.length?D.finance.slice().reverse().map(x=>`<tr><td>${x.date}</td><td>${vname(x.villa)}</td><td><span class="tag ${x.dir==='in'?'ok':'warning'}">${x.dir==='in'?'Thu':'Chi'}</span></td><td>${esc(x.cat)}</td><td><b>${fmt(x.amount)}</b></td><td>${x.paid?'Đã TT':'Chưa TT'}</td><td><button class="danger-btn" data-del-fin="${x.id}">Xóa</button></td></tr>`).join(''):'<tr><td colspan="7" class="empty">Chưa có giao dịch</td></tr>'}</table></div></div></div>`}

/* main-3a.txt */
function reports(){const rows=VILLAS.map(v=>[v,calcVilla(v.code)]),f=forecast(),m=mon()+'-01',lock=D.closures.find(x=>x.month===m);return `<div class="view"><div class="grid kpis"><div class="kpi"><span>DT phòng</span><b>${fmt(rows.reduce((n,x)=>n+x[1].room,0))}</b></div><div class="kpi"><span>DT khác</span><b>${fmt(rows.reduce((n,x)=>n+x[1].other,0))}</b></div><div class="kpi"><span>Chi vận hành</span><b>${fmt(rows.reduce((n,x)=>n+x[1].expense,0))}</b></div><div class="kpi"><span>Thuê/chia chủ</span><b>${fmt(rows.reduce((n,x)=>n+x[1].owner,0))}</b></div></div><div class="grid two"><div class="card"><h3>P&L từng villa</h3><div class="scroll"><table><tr><th>Villa</th><th>DT phòng</th><th>DT khác</th><th>Chi</th><th>Thuê/chia</th><th>LN</th></tr>${rows.map(([v,x])=>`<tr><td><b>${esc(v.name)}</b><br>${badge(v)}</td><td>${fmt(x.room)}</td><td>${fmt(x.other)}</td><td>${fmt(x.expense)}</td><td>${fmt(x.owner)}</td><td><b>${fmt(x.profit)}</b></td></tr>`).join('')}</table></div></div><div class="card"><h3>Dự chi tháng tới</h3><table style="min-width:0"><tr><td>Thuê cố định</td><td>${fmt(f.fixed)}</td></tr><tr><td>Công nợ đến hạn</td><td>${fmt(f.ap)}</td></tr><tr><td><b>Cơ sở</b></td><td><b>${fmt(f.total)}</b></td></tr><tr><td>Thận trọng</td><td>${fmt(f.total*1.1)}</td></tr><tr><td>Tích cực</td><td>${fmt(f.total*.95)}</td></tr></table></div></div>${MODE==='auth'&&(ROLE==='admin'||ROLE==='accounting')?`<div class="grid two" style="margin-top:14px"><div class="card"><h3>Đóng tháng</h3><p>Tháng hiện tại: <b>${m.slice(0,7)}</b> · ${lock?.status==='locked'?'<span class="tag bad">ĐÃ KHÓA</span>':'<span class="tag ok">ĐANG MỞ</span>'}</p><p class="muted">Khi đóng, hệ thống chụp P&L từng villa và khóa sửa/xóa booking + thu chi của tháng theo ngày check-in/ngày giao dịch.</p><div class="row">${lock?.status==='locked'?(ROLE==='admin'?'<button id="reopenMonth">Mở khóa tháng</button>':''):'<button class="primary" id="closeMonth">Đóng và khóa tháng</button>'}</div></div><div class="card"><h3>Lịch sử đóng tháng</h3><div class="scroll"><table><tr><th>Tháng</th><th>TT</th><th>Doanh thu</th><th>LN đóng góp</th><th>Đóng lúc</th></tr>${D.closures.slice().sort((a,b)=>b.month.localeCompare(a.month)).map(x=>`<tr><td>${x.month.slice(0,7)}</td><td><span class="tag ${x.status==='locked'?'bad':'ok'}">${x.status==='locked'?'Khóa':'Mở'}</span></td><td>${fmt((+x.summary.room_revenue||0)+(+x.summary.other_revenue||0))}</td><td>${fmt(+x.summary.contribution_profit||0)}</td><td>${x.closedAt?new Date(x.closedAt).toLocaleString('vi-VN'):'—'}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">Chưa đóng tháng nào</td></tr>'}</table></div></div></div>`:''}</div>`}
function settings(){return `<div class="view"><div class="grid two"><div class="card"><h3>Trạng thái hệ thống</h3><table style="min-width:0"><tr><td>Vercel</td><td><span class="tag ok">Production</span></td></tr><tr><td>GitHub</td><td><span class="tag ok">Source main</span></td></tr><tr><td>Neon Postgres</td><td><span class="tag ok">Connected</span></td></tr><tr><td>Neon Auth + Data API</td><td><span class="tag ok">Connected</span></td></tr><tr><td>RLS</td><td><span class="tag ok">Enabled</span></td></tr><tr><td>Chế độ</td><td><b>${MODE==='auth'?'DATABASE THẬT':'DEMO CỤC BỘ'}</b></td></tr></table></div><div class="card"><h3>Sao lưu demo</h3><p class="muted">Chỉ áp dụng cho demo. Production nằm trên Neon.</p><button id="exportBtn">Xuất JSON demo</button></div></div>${(ROLE==='admin'||ROLE==='ops')&&MODE==='auth'?`<div class="card" style="margin-top:14px"><h3>GPS điểm danh từng villa</h3><p class="muted">Tôi không tự đoán tọa độ. Anh đứng tại villa, chọn villa → “Lấy GPS hiện tại” → lưu. Chỉ bật bắt buộc GPS sau khi tọa độ đúng.</p><div class="form"><label>Villa<select id="geoVilla">${vopts()}</select></label><label>Bán kính (m)<input id="geoRadius" type="number" min="20" max="2000" value="200"></label><label>Vĩ độ<input id="geoLat" type="number" step="0.000001"></label><label>Kinh độ<input id="geoLng" type="number" step="0.000001"></label><label class="full"><input id="geoRequired" type="checkbox" style="width:auto;display:inline"> Bắt buộc ở trong bán kính khi check-in</label></div><div class="row"><button id="geoCurrent">Lấy GPS hiện tại</button><button class="primary" id="geoSave">Lưu cấu hình</button></div><div class="scroll" style="margin-top:12px"><table><tr><th>Villa</th><th>Tọa độ</th><th>Bán kính</th><th>Bắt buộc</th></tr>${VILLAS.map(v=>`<tr><td><b>${esc(v.name)}</b></td><td>${v.lat!=null?`${v.lat.toFixed(6)}, ${v.lng.toFixed(6)}`:'Chưa cấu hình'}</td><td>${v.radius||200} m</td><td>${v.gpsRequired?'<span class="tag ok">Có</span>':'<span class="tag warning">Chưa</span>'}</td></tr>`).join('')}</table></div></div>`:''}<div class="card" style="margin-top:14px"><h3>Quy tắc hợp đồng</h3><div class="scroll"><table><tr><th>Villa</th><th>Mô hình</th><th>Quy tắc</th></tr>${VILLAS.map(v=>`<tr><td><b>${esc(v.name)}</b></td><td>${badge(v)}</td><td>${esc(v.rule)}</td></tr>`).join('')}</table></div><div class="note warn" style="margin-top:14px"><b>Nhàn:</b> hiện chia 50% trên doanh thu bán phòng. OTA commission, hoàn tiền, thuế/phí sẽ khóa công thức khi có hợp đồng.</div></div></div>`}
function renderCalendar(){const e=$('calendar');if(!e)return;const y=CAL_MONTH.getFullYear(),m=CAL_MONTH.getMonth(),days=new Date(y,m+1,0).getDate(),today=day();e.style.gridTemplateColumns=`120px repeat(${days},minmax(42px,1fr))`;let h='<div class="head">Villa</div>',ds=[];for(let i=1;i<=days;i++){const d=new Date(y,m,i),k=calKey(d);ds.push(k);h+=`<div class="head ${k===today?'today':''}">${String(i).padStart(2,'0')}</div>`}for(const v of VILLAS){h+=`<div class="head">${esc(v.name)}</div>`;for(const k of ds){const b=D.bookings.filter(x=>x.villa===v.code&&x.status!=='cancelled'&&k>=x.in&&k<x.out);h+=`<div class="${b.length?'booked':''} ${k===today?'today':''}" title="${b.map(x=>esc(x.guest)+' · '+esc(x.code)).join(' | ')}">${b.map(x=>esc(x.guest).split(' ')[0]).join(', ')}</div>`}}e.innerHTML=h}

/* main-3b.txt */

async function act(fn,ok){try{await fn();if(ok)toast(ok);await refresh()}catch(e){console.error(e);toast(e.message||'Có lỗi',true)}}
function bind(){
 if(current==='crm'){$('addLead')?.addEventListener('click',()=>act(async()=>{if(!$('lName').value.trim())throw new Error('Nhập tên khách');if(MODE==='demo'){D.leads.push({id:Date.now(),name:$('lName').value.trim(),phone:$('lPhone').value.trim(),source:$('lSource').value,value:+$('lValue').value||0,status:$('lStatus').value,campaign:$('lCampaign').value.trim()});return saveLocal()}const c=await insert('customers',{full_name:$('lName').value.trim(),phone:$('lPhone').value.trim()||null});await insert('leads',{customer_id:c.id,source:$('lSource').value,campaign:$('lCampaign').value.trim()||null,status:$('lStatus').value,expected_value:+$('lValue').value||0})},'Đã lưu lead'));document.querySelectorAll('[data-del-lead]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.leads=D.leads.filter(x=>x.id!==+b.dataset.delLead),saveLocal()):remove('leads',+b.dataset.delLead),'Đã xóa lead'))}
 if(current==='booking'){const t=new Date(),t2=new Date();t2.setDate(t.getDate()+1);$('bIn').value=day(t);$('bOut').value=day(t2);$('addBooking').onclick=()=>act(async()=>{if(!$('bCode').value.trim()||!$('bGuest').value.trim())throw new Error('Nhập mã booking và khách');if($('bOut').value<=$('bIn').value)throw new Error('Check-out phải sau check-in');if(MODE==='demo'){if(['confirmed','staying'].includes($('bStatus').value)&&overlaps($('bVilla').value,$('bIn').value,$('bOut').value))throw new Error('Trùng lịch villa');D.bookings.push({id:Date.now(),code:$('bCode').value.trim(),guest:$('bGuest').value.trim(),villa:$('bVilla').value,source:$('bSource').value,in:$('bIn').value,out:$('bOut').value,status:$('bStatus').value,guests:+$('bGuests').value||1,room:+$('bRoom').value||0,other:+$('bOther').value||0,received:+$('bReceived').value||0,invoice:$('bInvoice').value});return saveLocal()}const c=await insert('customers',{full_name:$('bGuest').value.trim()});await insert('bookings',{booking_code:$('bCode').value.trim(),villa_id:vid($('bVilla').value),customer_id:c.id,source:$('bSource').value,check_in_date:$('bIn').value,check_out_date:$('bOut').value,status:$('bStatus').value,guests:+$('bGuests').value||1,room_revenue:+$('bRoom').value||0,other_revenue:+$('bOther').value||0,amount_received:+$('bReceived').value||0,invoice_status:$('bInvoice').value})},'Đã tạo booking');document.querySelectorAll('[data-del-booking]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.bookings=D.bookings.filter(x=>x.id!==+b.dataset.delBooking),saveLocal()):remove('bookings',+b.dataset.delBooking),'Đã xóa booking'))}
 if(current==='booking'){
  $('calPrev')?.addEventListener('click',()=>{CAL_MONTH=new Date(CAL_MONTH.getFullYear(),CAL_MONTH.getMonth()-1,1);render()});
  $('calNext')?.addEventListener('click',()=>{CAL_MONTH=new Date(CAL_MONTH.getFullYear(),CAL_MONTH.getMonth()+1,1);render()});
  $('calToday')?.addEventListener('click',()=>{CAL_MONTH=new Date(new Date().getFullYear(),new Date().getMonth(),1);render()});
 }
 if(current==='ops'){$('addTask')?.addEventListener('click',()=>act(async()=>{if(MODE==='demo'){D.tasks.push({id:Date.now(),villa:$('tVilla').value,type:$('tType').value,employeeId:+$('tEmployee').value||null,owner:D.employees.find(e=>e.id===+$('tEmployee').value)?.name||'',due:$('tDue').value,note:$('tNote').value.trim(),done:false});return saveLocal()}await insert('housekeeping_tasks',{villa_id:vid($('tVilla').value),task_type:$('tType').value,assigned_employee_id:+$('tEmployee').value||null,due_at:$('tDue').value?new Date($('tDue').value).toISOString():null,notes:$('tNote').value.trim()||null,status:'open'})},'Đã tạo việc'));document.querySelectorAll('[data-task]').forEach(b=>b.onclick=()=>act(async()=>{const x=D.tasks.find(t=>t.id===+b.dataset.task);if(MODE==='demo'){x.done=!x.done;return saveLocal()}await update('housekeeping_tasks',x.id,{status:x.done?'open':'completed',completed_at:x.done?null:new Date().toISOString()})},'Đã cập nhật việc'))}
 if(current==='att'){
  $('adjDate').value=day();
  $('addEmployee')?.addEventListener('click',()=>act(async()=>{if(!$('eCode').value.trim()||!$('eName').value.trim())throw new Error('Nhập mã và tên');if(MODE==='demo'){D.employees.push({id:Date.now(),code:$('eCode').value.trim(),name:$('eName').value.trim(),role:$('eRole').value,phone:$('ePhone').value.trim()});return saveLocal()}await insert('employees',{employee_code:$('eCode').value.trim(),full_name:$('eName').value.trim(),role:$('eRole').value,phone:$('ePhone').value.trim()||null,active:true})},'Đã thêm nhân viên'));
  if($('schDate'))$('schDate').value=day();
  $('addSchedule')?.addEventListener('click',()=>act(async()=>{if(!+$('schEmployee').value||!$('schDate').value)throw new Error('Chọn nhân viên và ngày');if(MODE==='demo'){D.schedules.push({id:Date.now(),employee:+$('schEmployee').value,villa:$('schVilla').value,shiftId:+$('schShift').value,shift:{1:'Sáng',2:'Chiều',3:'Hành chính',4:'Linh hoạt'}[$('schShift').value],date:$('schDate').value,status:'scheduled',note:$('schNote').value.trim()});return saveLocal()}await insert('employee_shift_assignments',{employee_id:+$('schEmployee').value,villa_id:vid($('schVilla').value),shift_id:+$('schShift').value,work_date:$('schDate').value,status:'scheduled',note:$('schNote').value.trim()||null})},'Đã xếp ca'));
  document.querySelectorAll('[data-del-sch]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.schedules=D.schedules.filter(x=>x.id!==+b.dataset.delSch),saveLocal()):remove('employee_shift_assignments',+b.dataset.delSch),'Đã xóa ca'));

/* main-4a.txt */
  $('linkAccount')?.addEventListener('click',()=>act(async()=>{const email=$('linkEmail').value.trim();if(!email||!+$('linkEmployee').value)throw new Error('Nhập email và chọn nhân viên');const {error}=await neon.rpc('admin_link_employee_account',{p_email:email,p_employee_id:+$('linkEmployee').value,p_role:$('linkRole').value});if(error)throw new Error(dbError(error))},'Đã liên kết tài khoản'));

  $('gpsBtn').onclick=()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>{GPS={lat:p.coords.latitude,lng:p.coords.longitude,acc:p.coords.accuracy};$('attMsg').textContent=`GPS ${GPS.lat.toFixed(5)}, ${GPS.lng.toFixed(5)} · sai số ~${Math.round(GPS.acc)}m`},()=>{$('attMsg').textContent='Không lấy được GPS. Hãy cấp quyền vị trí.'},{enableHighAccuracy:true,timeout:10000}):$('attMsg').textContent='Thiết bị không hỗ trợ GPS';
  $('checkInBtn').onclick=()=>act(async()=>{if(MODE==='demo'){const emp=D.employees[0]?.id;if(!emp)throw new Error('Demo cần thêm nhân viên trước');D.attendance.push({id:Date.now(),employee:emp,villa:$('aVilla').value,shift:{1:'morning',2:'afternoon',3:'office',4:'flex'}[$('aShift').value],in:new Date().toISOString(),out:null,gps:GPS,date:day()});return saveLocal()}const {error}=await neon.rpc('attendance_check_in',{p_villa_id:vid($('aVilla').value),p_shift_id:+$('aShift').value,p_lat:GPS?.lat??null,p_lng:GPS?.lng??null});if(error)throw new Error(dbError(error))},'Đã check-in bằng giờ máy chủ');
  $('checkOutBtn').onclick=()=>act(async()=>{if(MODE==='demo'){const a=[...D.attendance].reverse().find(x=>!x.out);if(!a)throw new Error('Không có ca đang mở');a.out=new Date().toISOString();return saveLocal()}const {error}=await neon.rpc('attendance_check_out',{p_lat:GPS?.lat??null,p_lng:GPS?.lng??null});if(error)throw new Error(dbError(error))},'Đã check-out bằng giờ máy chủ');
  $('addAdj').onclick=()=>act(async()=>{if(!$('adjReason').value.trim())throw new Error('Nhập lý do');if(MODE==='demo'){const emp=D.employees[0]?.id;if(!emp)throw new Error('Demo cần thêm nhân viên');D.adjustments.push({id:Date.now(),employee:emp,date:$('adjDate').value,reason:$('adjReason').value.trim(),status:'pending'});return saveLocal()}const {error}=await neon.rpc('attendance_request_adjustment',{p_work_date:$('adjDate').value,p_check_in:$('adjIn').value?new Date($('adjIn').value).toISOString():null,p_check_out:$('adjOut').value?new Date($('adjOut').value).toISOString():null,p_reason:$('adjReason').value.trim()});if(error)throw new Error(dbError(error))},'Đã gửi phiếu điều chỉnh');
  document.querySelectorAll('[data-adj-ok]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.adjustments.find(x=>x.id===+b.dataset.adjOk).status='approved',saveLocal()):update('attendance_adjustments',+b.dataset.adjOk,{status:'approved',reviewed_at:new Date().toISOString()}),'Đã duyệt'));
  document.querySelectorAll('[data-adj-no]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.adjustments.find(x=>x.id===+b.dataset.adjNo).status='rejected',saveLocal()):update('attendance_adjustments',+b.dataset.adjNo,{status:'rejected',reviewed_at:new Date().toISOString()}),'Đã từ chối'));
 }
 if(current==='stock'){$('addItem').onclick=()=>act(async()=>{if(!$('sSku').value.trim()||!$('sName').value.trim())throw new Error('Nhập SKU và tên');if(MODE==='demo'){D.items.push({id:Date.now(),sku:$('sSku').value.trim(),name:$('sName').value.trim(),unit:$('sUnit').value.trim()||'đv',min:+$('sMin').value||0,cost:+$('sCost').value||0});return saveLocal()}await insert('inventory_items',{sku:$('sSku').value.trim(),name:$('sName').value.trim(),unit:$('sUnit').value.trim()||'đv',min_stock:+$('sMin').value||0,average_cost:+$('sCost').value||0,active:true})},'Đã thêm vật tư');$('addMove').onclick=()=>act(async()=>{if(!+$('mItem').value)throw new Error('Chưa có vật tư');if(MODE==='demo'){D.moves.push({id:Date.now(),item:+$('mItem').value,villa:$('mVilla').value,type:$('mType').value,qty:+$('mQty').value||0});return saveLocal()}await insert('stock_movements',{item_id:+$('mItem').value,villa_id:vid($('mVilla').value),movement_type:$('mType').value,quantity:+$('mQty').value||0,unit_cost:D.items.find(i=>i.id===+$('mItem').value)?.cost||0})},'Đã ghi nhận kho')}
 if(current==='fin'){$('fDate').value=day();$('addFin').onclick=()=>act(async()=>{if(+$('fAmount').value<=0)throw new Error('Nhập số tiền');if(MODE==='demo'){D.finance.push({id:Date.now(),date:$('fDate').value,villa:$('fVilla').value,dir:$('fDir').value,cat:$('fCat').value,amount:+$('fAmount').value,source:$('fSource').value.trim(),paid:$('fPaid').value==='yes',due:$('fDue').value});return saveLocal()}await insert('finance_transactions',{transaction_date:$('fDate').value,villa_id:vid($('fVilla').value),direction:$('fDir').value,category:$('fCat').value,amount:+$('fAmount').value,source:$('fSource').value.trim()||null,paid:$('fPaid').value==='yes',due_date:$('fDue').value||null,invoice_status:'undetermined'})},'Đã lưu giao dịch');document.querySelectorAll('[data-del-fin]').forEach(b=>b.onclick=()=>act(async()=>MODE==='demo'?(D.finance=D.finance.filter(x=>x.id!==+b.dataset.delFin),saveLocal()):remove('finance_transactions',+b.dataset.delFin),'Đã xóa giao dịch'))}
 if(current==='reports'){
  $('closeMonth')?.addEventListener('click',()=>act(async()=>{if(!confirm('Đóng và khóa tháng hiện tại? Sau đó booking/thu chi của tháng sẽ không sửa/xóa được.'))return;const {error}=await neon.rpc('close_month',{p_month:mon()+'-01'});if(error)throw new Error(dbError(error))},'Đã đóng và khóa tháng'));
  $('reopenMonth')?.addEventListener('click',()=>act(async()=>{if(!confirm('Mở khóa tháng hiện tại để chỉnh sửa dữ liệu?'))return;const {error}=await neon.rpc('reopen_month',{p_month:mon()+'-01'});if(error)throw new Error(dbError(error))},'Đã mở khóa tháng'));
 }
 if(current==='settings'){
  $('exportBtn')?.addEventListener('click',()=>{const a=document.createElement('a'),blob=new Blob([JSON.stringify({version:'0.6-demo',data:D},null,2)],{type:'application/json'});a.href=URL.createObjectURL(blob);a.download='anstay-demo-'+day()+'.json';a.click()});

/* main-4b.txt */
  function fillGeo(){const v=VILLAS.find(x=>x.code===$('geoVilla')?.value);if(!v)return;$('geoLat').value=v.lat??'';$('geoLng').value=v.lng??'';$('geoRadius').value=v.radius||200;$('geoRequired').checked=!!v.gpsRequired}
  $('geoVilla')?.addEventListener('change',fillGeo);fillGeo();
  $('geoCurrent')?.addEventListener('click',()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>{$('geoLat').value=p.coords.latitude.toFixed(6);$('geoLng').value=p.coords.longitude.toFixed(6);toast('Đã lấy GPS hiện tại')},()=>toast('Không lấy được GPS',true),{enableHighAccuracy:true,timeout:10000}):toast('Thiết bị không hỗ trợ GPS',true));
  $('geoSave')?.addEventListener('click',()=>act(async()=>{const lat=parseFloat($('geoLat').value),lng=parseFloat($('geoLng').value),radius=+$('geoRadius').value;if(!Number.isFinite(lat)||!Number.isFinite(lng))throw new Error('Cần tọa độ hợp lệ');if(radius<20||radius>2000)throw new Error('Bán kính 20–2000 m');await update('villas',vid($('geoVilla').value),{latitude:lat,longitude:lng,attendance_radius_m:radius,gps_required:$('geoRequired').checked})},'Đã lưu GPS villa'));
 }
}
function render(){const map={dash,crm,booking,ops,att,stock,fin,reports,settings};$('content').innerHTML=(map[current]||dash)();renderCalendar();bind()}

function authMessage(t,error=false){$('authMsg').textContent=t;$('authMsg').classList.toggle('error',error)}
async function enterAuthenticated(session){SESSION=session;MODE='auth';$('authGate').classList.add('hidden');$('userPill').textContent=(session?.user?.name||session?.user?.email||'ANSTAY')+' · đang tải';try{await loadRemote();$('userPill').textContent=`${session?.user?.name||session?.user?.email||'ANSTAY'} · ${roleLabel(ROLE)}`;toast('Đã kết nối dữ liệu thật')}catch(e){console.error(e);MODE='locked';$('authGate').classList.remove('hidden');authMessage('Đăng nhập thành công nhưng chưa đọc được Data API: '+e.message,true)}}
async function checkSession(){try{const res=await neon.auth.getSession();const session=res?.data||res;if(session?.user)return enterAuthenticated(session)}catch(e){console.warn(e)}$('authGate').classList.remove('hidden')}
$('loginBtn').onclick=async()=>{const email=$('authEmail').value.trim(),password=$('authPassword').value;if(!email||!password)return authMessage('Nhập email và mật khẩu.',true);authMessage('Đang đăng nhập...');try{const res=await neon.auth.signIn.email({email,password});if(res?.error)throw new Error(res.error.message||'Đăng nhập chưa thành công');const s=await neon.auth.getSession();const session=s?.data||s;if(!session?.user)throw new Error('Không tạo được phiên đăng nhập');await enterAuthenticated(session)}catch(e){authMessage(e.message||'Đăng nhập chưa thành công.',true)}};
$('resetReqBtn').onclick=async()=>{const email=$('authEmail').value.trim();if(!email)return authMessage('Nhập email quản trị trước.',true);authMessage('Đang gửi liên kết...');try{const r=await fetch(AUTH_URL+'/request-password-reset',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,redirectTo:location.origin+location.pathname})});if(!r.ok){const j=await r.json().catch(()=>({}));throw new Error(j?.message||'Chưa gửi được email')}authMessage('Đã yêu cầu gửi email đặt lại mật khẩu.')}catch(e){authMessage(e.message,true)}};
$('resetBtn').onclick=async()=>{const token=new URLSearchParams(location.search).get('token'),a=$('newPassword').value,b=$('newPassword2').value;if(!token)return authMessage('Thiếu token reset.',true);if(a.length<8||a!==b)return authMessage('Mật khẩu tối thiểu 8 ký tự và hai ô phải giống nhau.',true);try{const r=await fetch(AUTH_URL+'/reset-password',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({newPassword:a,token})});if(!r.ok){const j=await r.json().catch(()=>({}));throw new Error(j?.message||'Không đặt được mật khẩu')}history.replaceState({},'',location.pathname);$('resetPane').classList.add('hidden');$('loginPane').classList.remove('hidden');authMessage('Đã đặt mật khẩu. Bây giờ đăng nhập.')}catch(e){authMessage(e.message,true)}};
$('demoBtn').onclick=()=>{MODE='demo';ROLE='admin';D=loadLocal();VILLAS=[...FALLBACK_VILLAS];$('authGate').classList.add('hidden');$('userPill').textContent='Demo cục bộ';applyAccess();render();toast('Đang ở chế độ demo')};
$('logoutBtn').onclick=async()=>{if(MODE==='auth')try{await neon.auth.signOut()}catch{}MODE='locked';ROLE='employee';SESSION=null;D=blank();$('userPill').textContent='Chưa đăng nhập';$('authGate').classList.remove('hidden');authMessage('Đã đăng xuất.')};
$('menuBtn').onclick=()=>$('sidebar').classList.toggle('open');
$('monthPill').textContent=new Date().toLocaleDateString('vi-VN',{month:'2-digit',year:'numeric'});

$('showSignupBtn')?.addEventListener('click',()=>{$('loginPane').classList.add('hidden');$('signupPane').classList.remove('hidden');authMessage('Tạo tài khoản nhân viên. Sau đó quản trị sẽ gán quyền.')});
$('backLoginBtn')?.addEventListener('click',()=>{$('signupPane').classList.add('hidden');$('loginPane').classList.remove('hidden');authMessage('Đăng nhập để sử dụng ANSTAY.')});
$('signupBtn')?.addEventListener('click',async()=>{const name=$('signupName').value.trim(),email=$('signupEmail').value.trim(),password=$('signupPassword').value;if(!name||!email||password.length<8)return authMessage('Nhập họ tên, email và mật khẩu tối thiểu 8 ký tự.',true);authMessage('Đang tạo tài khoản...');try{const r=await neon.auth.signUp.email({name,email,password});if(r?.error)throw new Error(r.error.message||'Không tạo được tài khoản');try{await neon.auth.signOut()}catch{}$('signupPane').classList.add('hidden');$('loginPane').classList.remove('hidden');$('authEmail').value=email;authMessage('Đã tạo tài khoản. Báo quản trị liên kết email với hồ sơ nhân viên trước khi đăng nhập.')}catch(e){authMessage(e.message||'Không tạo được tài khoản',true)}});

/* main-5.txt */
const params=new URLSearchParams(location.search),resetToken=params.get('token');
D=loadLocal();
if(resetToken){
  applyAccess();render();
  $('loginPane').classList.add('hidden');$('resetPane').classList.remove('hidden');$('authLead').textContent='Đặt mật khẩu mới cho tài khoản quản trị.';
  checkSession();
}else if(params.get('demo')==='1'){
  MODE='demo';ROLE='admin';VILLAS=[...FALLBACK_VILLAS];
  $('authGate').classList.add('hidden');$('userPill').textContent='Demo cục bộ';
  applyAccess();render();
  setTimeout(()=>toast('Đang xem bản demo ANSTAY'),50);
}else{
  applyAccess();render();checkSession();
}

/* main-6.txt */
function soldNightsVilla(code,monthDate=new Date(new Date().getFullYear(),new Date().getMonth(),1)){const y=monthDate.getFullYear(),m=monthDate.getMonth(),start=new Date(y,m,1),end=new Date(y,m+1,1);let sold=0;for(const b of D.bookings){if(b.villa!==code||b.status==='cancelled')continue;const bi=new Date(b.in+'T00:00:00'),bo=new Date(b.out+'T00:00:00'),a=bi>start?bi:start,z=bo<end?bo:end;if(z>a)sold+=(z-a)/86400000}return sold}
function occupancyVilla(code,monthDate=new Date(new Date().getFullYear(),new Date().getMonth(),1)){const days=new Date(monthDate.getFullYear(),monthDate.getMonth()+1,0).getDate();return days?Math.round(soldNightsVilla(code,monthDate)/days*100):0}
function bookingSourceRows(){const map={};D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled').forEach(b=>{const k=b.source||'Khác';map[k]=(map[k]||0)+1});return Object.entries(map).sort((a,b)=>b[1]-a[1])}
function ownerSettlementRow(v,x){if(v.model==='share')return {basis:x.room,payable:x.owner,anstay:x.room-x.owner+x.other,label:`${v.pct}% DT phòng`,note:'Tạm tính trên DT phòng; chưa trừ OTA/hoàn/thuế cho đến khi hợp đồng chốt công thức.'};if(v.model==='rent')return {basis:0,payable:v.rent,anstay:x.room+x.other-v.rent,label:'Thuê cố định',note:'Nghĩa vụ thuê net hàng tháng.'};return {basis:0,payable:0,anstay:x.room+x.other,label:'Sở hữu',note:'Không có nghĩa vụ thuê/chia doanh thu với chủ nhà.'}}
function dash(){const rows=VILLAS.map(v=>[v,calcVilla(v.code)]),rev=rows.reduce((n,x)=>n+x[1].room+x[1].other,0),bk=D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled'),att=D.attendance.filter(a=>a.date===day()),f=forecast(),today=day(),arr=D.bookings.filter(b=>b.in===today&&b.status!=='cancelled'),dep=D.bookings.filter(b=>b.out===today&&b.status!=='cancelled'),cash=cashInMonth(),ar=receivableMonth(),overdueTasks=D.tasks.filter(t=>!t.done&&t.due&&new Date(t.due)<new Date()),overduePay=D.finance.filter(x=>x.dir==='out'&&!x.paid&&x.due&&x.due<today),src=bookingSourceRows();return `<div class="view"><div class="note"><b>ANSTAY Control Center:</b> ${MODE==='auth'?'dữ liệu Neon Postgres thật · RLS đang bật':'bản demo cục bộ'} · Dashboard ưu tiên việc cần xử lý, tiền phải thu và nghĩa vụ villa.</div><div class="grid kpis"><div class="kpi"><span>Doanh thu tháng</span><b>${fmt(rev)}</b></div><div class="kpi"><span>Phải thu booking</span><b>${fmt(ar)}</b></div><div class="kpi"><span>Việc quá hạn</span><b>${overdueTasks.length}</b></div><div class="kpi"><span>Tiền cần chuẩn bị tháng tới</span><b>${fmt(f.total)}</b></div></div><div class="grid two"><div class="card"><h3>Hiệu quả 6 villa</h3><div class="scroll"><table><tr><th>Villa</th><th>Công suất</th><th>Doanh thu</th><th>Thuê/chia chủ</th><th>LN đóng góp</th></tr>${rows.map(([v,x])=>`<tr><td><b>${esc(v.name)}</b><br>${badge(v)}</td><td><b>${occupancyVilla(v.code)}%</b><br><span class="muted">${soldNightsVilla(v.code)} đêm</span></td><td>${fmt(x.room+x.other)}</td><td>${fmt(x.owner)}</td><td><b>${fmt(x.profit)}</b></td></tr>`).join('')}</table></div></div><div class="card"><h3>Việc cần xử lý</h3><p><span class="tag ${arr.length?'info':'ok'}">${arr.length} check-in hôm nay</span> <span class="tag ${dep.length?'warning':'ok'}">${dep.length} check-out hôm nay</span></p><p><b>${overdueTasks.length}</b> việc buồng phòng/bảo trì quá hạn.</p><p><b>${overduePay.length}</b> khoản chi quá hạn thanh toán.</p><p><b>${D.adjustments.filter(x=>x.status==='pending').length}</b> phiếu điều chỉnh công chờ duyệt.</p><p><b>${att.filter(x=>!x.out).length}</b> nhân viên đang trong ca.</p></div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Khách đến / đi hôm nay</h3>${arr.concat(dep).length?`<div class="scroll"><table><tr><th>Loại</th><th>Villa</th><th>Khách</th><th>Mã</th></tr>${arr.map(b=>`<tr><td><span class="tag info">Check-in</span></td><td>${vname(b.villa)}</td><td>${esc(b.guest)}</td><td>${esc(b.code)}</td></tr>`).join('')}${dep.map(b=>`<tr><td><span class="tag warning">Check-out</span></td><td>${vname(b.villa)}</td><td>${esc(b.guest)}</td><td>${esc(b.code)}</td></tr>`).join('')}</table></div>`:'<p class="muted">Không có lượt đến/đi hôm nay.</p>'}</div><div class="card"><h3>Nguồn booking tháng này</h3>${src.length?src.map(([name,n])=>`<p style="display:flex;justify-content:space-between;gap:12px"><span>${esc(name)}</span><b>${n} booking</b></p>`).join(''):'<p class="muted">Chưa có booking trong tháng.</p>'}<hr><p>Tiền thu ghi nhận tháng: <b>${fmt(cash)}</b></p><p>Booking active tháng: <b>${bk.length}</b></p></div></div></div>`}
function reports(){const rows=VILLAS.map(v=>[v,calcVilla(v.code)]),f=forecast(),m=mon()+'-01',lock=D.closures.find(x=>x.month===m),src=bookingSourceRows(),totalRoom=rows.reduce((n,x)=>n+x[1].room,0),totalOther=rows.reduce((n,x)=>n+x[1].other,0),totalExpense=rows.reduce((n,x)=>n+x[1].expense,0),totalOwner=rows.reduce((n,x)=>n+x[1].owner,0),totalProfit=rows.reduce((n,x)=>n+x[1].profit,0);return `<div class="view"><div class="grid kpis"><div class="kpi"><span>DT phòng</span><b>${fmt(totalRoom)}</b></div><div class="kpi"><span>DT khác</span><b>${fmt(totalOther)}</b></div><div class="kpi"><span>Thuê/chia chủ</span><b>${fmt(totalOwner)}</b></div><div class="kpi"><span>LN đóng góp</span><b>${fmt(totalProfit)}</b></div></div><div class="grid two"><div class="card"><h3>P&L từng villa</h3><div class="scroll"><table><tr><th>Villa</th><th>Công suất</th><th>DT phòng</th><th>DT khác</th><th>Chi vận hành</th><th>Thuê/chia</th><th>LN</th></tr>${rows.map(([v,x])=>`<tr><td><b>${esc(v.name)}</b><br>${badge(v)}</td><td>${occupancyVilla(v.code)}%</td><td>${fmt(x.room)}</td><td>${fmt(x.other)}</td><td>${fmt(x.expense)}</td><td>${fmt(x.owner)}</td><td><b>${fmt(x.profit)}</b></td></tr>`).join('')}</table></div></div><div class="card"><h3>Dự chi tháng tới</h3><table style="min-width:0"><tr><td>Thuê cố định</td><td>${fmt(f.fixed)}</td></tr><tr><td>Công nợ đến hạn</td><td>${fmt(f.ap)}</td></tr><tr><td><b>Cơ sở</b></td><td><b>${fmt(f.total)}</b></td></tr><tr><td>Thận trọng +10%</td><td>${fmt(f.total*1.1)}</td></tr><tr><td>Tích cực -5%</td><td>${fmt(f.total*.95)}</td></tr></table><div class="note" style="margin-top:12px"><b>Mục tiêu quản trị:</b> số cơ sở là lượng tiền tối thiểu ANSTAY nên chuẩn bị trước khi bước sang tháng mới.</div></div></div><div class="card" style="margin-top:14px"><h3>Đối soát chủ nhà / nghĩa vụ villa — ${m.slice(0,7)}</h3><div class="scroll"><table><tr><th>Villa</th><th>Mô hình</th><th>Căn cứ</th><th>DT phòng</th><th>DT khác</th><th>Phải trả chủ/thuê</th><th>Phần còn lại trước chi vận hành</th><th>Ghi chú</th></tr>${rows.map(([v,x])=>{const s=ownerSettlementRow(v,x);return `<tr><td><b>${esc(v.name)}</b></td><td>${badge(v)}<br><span class="muted">${esc(s.label)}</span></td><td>${s.basis?fmt(s.basis):'—'}</td><td>${fmt(x.room)}</td><td>${fmt(x.other)}</td><td><b>${fmt(s.payable)}</b></td><td>${fmt(s.anstay)}</td><td class="muted">${esc(s.note)}</td></tr>`}).join('')}</table></div><div class="note warn" style="margin-top:12px"><b>Nhàn:</b> bảng hiện tạm tính 50% trên doanh thu bán phòng. Không tự ý trừ hoa hồng OTA, hoàn tiền hay thuế/phí khỏi căn cứ chia cho đến khi hợp đồng xác nhận.</div></div><div class="grid two" style="margin-top:14px"><div class="card"><h3>Cơ cấu nguồn booking</h3>${src.length?`<div class="scroll"><table><tr><th>Nguồn</th><th>Số booking</th><th>Tỷ trọng</th></tr>${src.map(([name,n])=>`<tr><td>${esc(name)}</td><td>${n}</td><td>${Math.round(n/Math.max(1,src.reduce((s,x)=>s+x[1],0))*100)}%</td></tr>`).join('')}</table></div>`:'<p class="muted">Chưa có dữ liệu.</p>'}</div><div class="card"><h3>Tóm tắt quản trị tháng</h3><p>Doanh thu gộp: <b>${fmt(totalRoom+totalOther)}</b></p><p>Chi vận hành ghi nhận: <b>${fmt(totalExpense)}</b></p><p>Nghĩa vụ thuê/chia chủ: <b>${fmt(totalOwner)}</b></p><p>LN đóng góp tạm tính: <b>${fmt(totalProfit)}</b></p><p class="muted">Hiện ghi nhận doanh thu theo tháng check-in; sẽ chuyển sang phân bổ theo đêm khi khóa chính sách kế toán.</p></div></div>${MODE==='auth'&&(ROLE==='admin'||ROLE==='accounting')?`<div class="grid two" style="margin-top:14px"><div class="card"><h3>Đóng tháng</h3><p>Tháng hiện tại: <b>${m.slice(0,7)}</b> · ${lock?.status==='locked'?'<span class="tag bad">ĐÃ KHÓA</span>':'<span class="tag ok">ĐANG MỞ</span>'}</p><p class="muted">Khi đóng, hệ thống chụp P&L từng villa và khóa sửa/xóa booking + thu chi của tháng theo ngày check-in/ngày giao dịch.</p><div class="row">${lock?.status==='locked'?(ROLE==='admin'?'<button id="reopenMonth">Mở khóa tháng</button>':''):'<button class="primary" id="closeMonth">Đóng và khóa tháng</button>'}</div></div><div class="card"><h3>Lịch sử đóng tháng</h3><div class="scroll"><table><tr><th>Tháng</th><th>TT</th><th>Doanh thu</th><th>LN đóng góp</th><th>Đóng lúc</th></tr>${D.closures.slice().sort((a,b)=>b.month.localeCompare(a.month)).map(x=>`<tr><td>${x.month.slice(0,7)}</td><td><span class="tag ${x.status==='locked'?'bad':'ok'}">${x.status==='locked'?'Khóa':'Mở'}</span></td><td>${fmt((+x.summary.room_revenue||0)+(+x.summary.other_revenue||0))}</td><td>${fmt(+x.summary.contribution_profit||0)}</td><td>${x.closedAt?new Date(x.closedAt).toLocaleString('vi-VN'):'—'}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">Chưa đóng tháng nào</td></tr>'}</table></div></div></div>`:''}</div>`}

/* main-7.txt */
D.payments=D.payments||[];
function bookingPayments(id){return (D.payments||[]).filter(p=>String(p.bookingId)===String(id))}
function paidForBooking(b){const ps=bookingPayments(b.id);return ps.length?ps.reduce((n,p)=>n+(+p.amount||0),0):(+b.received||0)}
function cashInMonth(){const pay=(D.payments||[]).filter(p=>inMon(p.date)).reduce((n,p)=>n+(+p.amount||0),0);const other=D.finance.filter(f=>f.dir==='in'&&inMon(f.date)&&!['room','room_revenue'].includes(f.cat)).reduce((n,f)=>n+(+f.amount||0),0);if(MODE==='demo'&&!D.payments.length)return D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled').reduce((n,b)=>n+(+b.received||0),0)+other;return pay+other}
function receivableMonth(){return D.bookings.filter(b=>inMon(b.in)&&b.status!=='cancelled').reduce((n,b)=>n+Math.max(0,(+b.room||0)+(+b.other||0)-paidForBooking(b)),0)}
async function syncPaymentsRemote(){if(MODE!=='auth'){D.payments=D.payments||[];return}const rows=await safeQ('booking_payments');D.payments=rows.map(x=>({id:+x.id,bookingId:+x.booking_id,date:x.payment_date,amount:+x.amount||0,method:x.method||'',account:x.receiving_account||'',reference:x.reference||'',reconciled:x.reconciliation_status==='reconciled',note:x.note||''}))}
function paymentMethodLabel(x){return {cash:'Tiền mặt',transfer:'Chuyển khoản',card:'Thẻ',ota:'OTA thu hộ',other:'Khác'}[x]||x||'—'}
function enhanceBookingPayments(){if(current!=='booking'||$('paymentLedger'))return;const view=$('content')?.querySelector('.view');if(!view)return;const active=D.bookings.filter(b=>b.status!=='cancelled');const total=(D.payments||[]).filter(p=>inMon(p.date)).reduce((n,p)=>n+(+p.amount||0),0);const unreconciled=(D.payments||[]).filter(p=>!p.reconciled).reduce((n,p)=>n+(+p.amount||0),0);const card=document.createElement('div');card.id='paymentLedger';card.className='card';card.style.marginTop='14px';card.innerHTML=`<div class="row" style="justify-content:space-between;align-items:center"><div><h3>Sổ thu tiền booking</h3><p class="muted">Booking là nguồn doanh thu; bảng này là nguồn tiền thực thu. Tách riêng để không cộng trùng doanh thu với dòng tiền.</p></div><div><span class="tag info">Thu tháng ${fmt(total)}</span> <span class="tag ${unreconciled?'warning':'ok'}">Chưa đối soát ${fmt(unreconciled)}</span></div></div><div class="grid two"><div><div class="form"><label>Booking<select id="pBooking">${active.map(b=>`<option value="${b.id}">${esc(b.code)} · ${vname(b.villa)} · ${esc(b.guest)}</option>`).join('')}</select></label><label>Ngày thu<input id="pDate" type="date" value="${day()}"></label><label>Số tiền<input id="pAmount" type="number" min="0" value="0"></label><label>Phương thức<select id="pMethod"><option value="transfer">Chuyển khoản</option><option value="cash">Tiền mặt</option><option value="card">Thẻ</option><option value="ota">OTA thu hộ</option><option value="other">Khác</option></select></label><label>Tài khoản / quỹ<input id="pAccount" placeholder="VCB / Tiền mặt..."></label><label>Mã tham chiếu<input id="pRef" placeholder="Mã GD / payout..."></label><label class="full"><span><input id="pReconciled" type="checkbox" style="width:auto;display:inline;margin-right:6px">Đã đối soát ngân hàng/OTA</span></label></div><button class="primary" id="savePayment" ${active.length?'':'disabled'}>Ghi nhận tiền thu</button></div><div><h3>Công nợ booking</h3><div class="scroll"><table><tr><th>Booking</th><th>Tổng phải thu</th><th>Đã thu</th><th>Còn thu</th></tr>${active.slice().sort((a,b)=>String(b.in).localeCompare(String(a.in))).slice(0,12).map(b=>{const due=(+b.room||0)+(+b.other||0),paid=paidForBooking(b);return `<tr><td><b>${esc(b.code)}</b><br>${vname(b.villa)}</td><td>${fmt(due)}</td><td>${fmt(paid)}</td><td><b>${fmt(Math.max(0,due-paid))}</b></td></tr>`}).join('')||'<tr><td colspan="4" class="empty">Chưa có booking</td></tr>'}</table></div></div></div><div class="scroll" style="margin-top:14px"><table><tr><th>Ngày</th><th>Booking</th><th>Số tiền</th><th>Phương thức</th><th>Tài khoản</th><th>Tham chiếu</th><th>Đối soát</th><th></th></tr>${(D.payments||[]).slice().sort((a,b)=>String(b.date).localeCompare(String(a.date))).slice(0,50).map(p=>{const b=D.bookings.find(x=>String(x.id)===String(p.bookingId));return `<tr><td>${p.date}</td><td><b>${esc(b?.code||p.bookingId)}</b><br>${vname(b?.villa)}</td><td><b>${fmt(p.amount)}</b></td><td>${esc(paymentMethodLabel(p.method))}</td><td>${esc(p.account)}</td><td>${esc(p.reference)}</td><td><button data-pay-rec="${p.id}">${p.reconciled?'✓ Đã đối soát':'Chưa đối soát'}</button></td><td><button class="danger-btn" data-del-payment="${p.id}">Xóa</button></td></tr>`}).join('')||'<tr><td colspan="8" class="empty">Chưa có giao dịch thu booking</td></tr>'}</table></div>`;view.appendChild(card)}
async function paymentAction(fn,ok){try{await fn();toast(ok);if(MODE==='auth')await syncPaymentsRemote();render()}catch(e){console.error(e);toast(e?.message||String(e),true)}}
const render_v07=render;render=function(){render_v07();enhanceBookingPayments()};
const enter_v07=enterAuthenticated;enterAuthenticated=async function(session){await enter_v07(session);if(MODE==='auth'){await syncPaymentsRemote();render()}};
const refresh_v07=refresh;refresh=async function(){await refresh_v07();if(MODE==='auth'){await syncPaymentsRemote();render()}};
document.addEventListener('click',e=>{const save=e.target.closest('#savePayment');if(save){const bid=+$('pBooking').value,amount=+$('pAmount').value,date=$('pDate').value,method=$('pMethod').value,account=$('pAccount').value.trim(),reference=$('pRef').value.trim(),reconciled=$('pReconciled').checked;if(!bid||!date||amount<=0)return toast('Chọn booking, ngày thu và số tiền lớn hơn 0',true);paymentAction(async()=>{if(MODE==='auth'){await insert('booking_payments',{booking_id:bid,payment_date:date,amount,method,receiving_account:account||null,reference:reference||null,reconciliation_status:reconciled?'reconciled':'unmatched',invoice_status:'undetermined',note:null});const currentPayments=await safeQ('booking_payments');const paid=currentPayments.filter(x=>String(x.booking_id)===String(bid)).reduce((n,x)=>n+(+x.amount||0),0);await update('bookings',bid,{amount_received:paid})}else{const id=Date.now();D.payments.push({id,bookingId:bid,date,amount,method,account,reference,reconciled});const b=D.bookings.find(x=>String(x.id)===String(bid));if(b)b.received=paidForBooking(b);localStorage.setItem(STORE,JSON.stringify(D))}},'Đã ghi nhận tiền thu');return}const rec=e.target.closest('[data-pay-rec]');if(rec){const id=+rec.dataset.payRec,p=(D.payments||[]).find(x=>x.id===id);if(!p)return;paymentAction(async()=>{if(MODE==='auth')await update('booking_payments',id,{reconciliation_status:!p.reconciled?'reconciled':'unmatched'});else{p.reconciled=!p.reconciled;localStorage.setItem(STORE,JSON.stringify(D))}},p.reconciled?'Đã bỏ đối soát':'Đã xác nhận đối soát');return}const del=e.target.closest('[data-del-payment]');if(del){const id=+del.dataset.delPayment,p=(D.payments||[]).find(x=>x.id===id);if(!p||!confirm('Xóa giao dịch thu này?'))return;paymentAction(async()=>{if(MODE==='auth'){await remove('booking_payments',id);const rows=await safeQ('booking_payments');const paid=rows.filter(x=>String(x.booking_id)===String(p.bookingId)).reduce((n,x)=>n+(+x.amount||0),0);await update('bookings',p.bookingId,{amount_received:paid})}else{D.payments=D.payments.filter(x=>x.id!==id);const b=D.bookings.find(x=>String(x.id)===String(p.bookingId));if(b)b.received=(D.payments||[]).filter(x=>String(x.bookingId)===String(p.bookingId)).reduce((n,x)=>n+(+x.amount||0),0);localStorage.setItem(STORE,JSON.stringify(D))}},'Đã xóa giao dịch thu')}});
render();

